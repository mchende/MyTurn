---
phase: 06-audio-classroom-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - src/app/lesson/[sessionId]/page.tsx
  - src/features/classroom-shell/classroom-shell.tsx
  - src/features/classroom-shell/podium-view-model.ts
  - src/features/classroom-shell/stage-panel.tsx
  - src/features/classroom-shell/audio-preflight-card.tsx
  - src/features/classroom-shell/use-classroom-audio-runtime.ts
  - test/unit/classroom-shell.test.tsx
  - test/unit/classroom-orchestrator.test.ts
autonomous: true
requirements:
  - AUDIO-01
  - AUDIO-02
  - AUDIO-03
  - VOICE-01
  - VOICE-02
  - VOICE-03
  - CLAS-05
  - CLAS-07
  - PLAT-03
  - PLAT-04
must_haves:
  truths:
    - "预检必须发生在正式课堂 UI 进入前，但允许跳过后继续上课。"
    - "老师/Bobby 自动播报与常驻字幕必须共存，不允许出现遮挡主课件的大音频遮罩。"
    - "讲台区继续只有一个孩子侧 CTA；按钮文案和可用性由音频 runtime 驱动。"
    - "失败兜底是轻提示和 retry，不是工具式错误弹层。"
    - "新增音频态 UI 后，老师区、讲台区、课件区和顶部席位区仍要保持完整可见或可滚动。"
  artifacts:
    - path: "src/features/classroom-shell/audio-preflight-card.tsx"
      provides: "进课前轻量音频预检 gate，可听见测试、麦克风检查、skip 风险提示"
      exports:
        - "AudioPreflightCard"
    - path: "src/features/classroom-shell/use-classroom-audio-runtime.ts"
      provides: "shell 可消费的 preflight / playback / recording view-model"
      exports:
        - "useClassroomAudioRuntime"
    - path: "src/features/classroom-shell/classroom-shell.tsx"
      provides: "课堂主壳对 preflight、播放态、录音态和 retry 的内嵌渲染"
      exports:
        - "ClassroomShell"
  key_links:
    - from: "src/app/lesson/[sessionId]/page.tsx"
      to: "src/features/classroom-shell/classroom-shell.tsx"
      via: "课堂入口在 route boundary 决定 reward / preflight / session props"
      pattern: "searchParams|reward|sessionId"
    - from: "src/features/classroom-shell/use-classroom-audio-runtime.ts"
      to: "src/features/classroom-shell/podium-view-model.ts"
      via: "单 CTA 录音状态与讲台 copy"
      pattern: "recording|retry|buttonLabel|podiumStatus"
    - from: "src/features/classroom-shell/classroom-shell.tsx"
      to: "src/features/classroom-shell/stage-panel.tsx"
      via: "轻量播放态和录音态嵌入现有课堂布局"
      pattern: "playing|recording|preflight|retry"
---

<objective>
把 06-01 的共享音频底座接进真实课堂：课前轻量预检、老师/Bobby 自动播报、讲台单 CTA 录音入口和轻量失败兜底都要在现有课堂壳里成立。

Purpose: 06-02 的任务是把产品真正做成“会发声并允许孩子开口”的小课表面，而不是只拥有底层 runtime。
Output: 课堂入口 preflight gate、shell 内嵌播放/录音 UI、focused shell unit 回归。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-audio-classroom-runtime/06-CONTEXT.md
@.planning/phases/06-audio-classroom-runtime/06-RESEARCH.md
@.planning/phases/06-audio-classroom-runtime/06-VALIDATION.md
@.planning/phases/06-audio-classroom-runtime/06-01-PLAN.md
@src/app/lesson/[sessionId]/page.tsx
@src/features/classroom-shell/classroom-shell.tsx
@src/features/classroom-shell/podium-view-model.ts
@src/features/classroom-shell/stage-panel.tsx
@test/unit/classroom-shell.test.tsx

<interfaces>
From src/features/classroom-shell/podium-view-model.ts:
```ts
type PodiumViewModel = {
  confirmationButtonLabel: string | null;
  podiumCaption: string;
  podiumStatus: string;
  showConfirmationButton: boolean;
  turnLabel: string;
};
```

Expected from 06-01:
```ts
type ClassroomAudioRuntimeViewModel = {
  preflightStatus: string;
  playbackStatus: string;
  recordingStatus: string;
  canStartRecording: boolean;
  startStudentRecording: () => Promise<void>;
  retryAudioStep: () => Promise<void>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现进课前轻量音频预检 gate，并允许 skip 后继续课堂</name>
  <files>src/app/lesson/[sessionId]/page.tsx, src/features/classroom-shell/audio-preflight-card.tsx, src/features/classroom-shell/classroom-shell.tsx, test/unit/classroom-shell.test.tsx</files>
  <read_first>
    .planning/phases/06-audio-classroom-runtime/06-RESEARCH.md
    src/app/lesson/[sessionId]/page.tsx
    src/features/classroom-shell/classroom-shell.tsx
    test/unit/classroom-shell.test.tsx
  </read_first>
  <behavior>
    - Test 1: 首次进入课堂时会先看到轻量 preflight gate，而不是立刻进入主课堂。
    - Test 2: gate 提供至少一个播放检查动作和一个麦克风检查动作；检查成功后进入课堂主壳。
    - Test 3: 若用户选择 skip，课堂仍可进入，但会留下明确的风险提示，不伪装成完全通过。
    - Test 4: preflight 不应是全屏设备设置页，也不应遮挡后续课堂主课件。
  </behavior>
  <action>先扩 `test/unit/classroom-shell.test.tsx`，再新增 `src/features/classroom-shell/audio-preflight-card.tsx` 并把它接到 `src/features/classroom-shell/classroom-shell.tsx` 或课堂 client wrapper。保持 route boundary 简单：`src/app/lesson/[sessionId]/page.tsx` 继续负责 session 数据与 reward query，不把复杂 preflight 状态塞到 server component。preflight gate 应更像课堂准备卡，而不是设置页；copy 保持孩子友好、英文优先、风险提示清楚。skip 路径必须清晰可测，不得让用户被困在课前卡片里。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-shell.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test/unit/classroom-shell.test.tsx` 明确覆盖 preflight pass 和 skip 两条路径。
    - `audio-preflight-card.tsx` 以轻卡片形式存在，不出现大遮罩或独立设置页。
    - 进课后仍能继续使用现有 lesson/reward/home return contract。
  </acceptance_criteria>
  <done>课前轻量音频准备流程成立，课堂不会一开始就撞上“没声音/没权限”的硬失败。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把老师/Bobby 播放态和孩子单 CTA 录音入口接进课堂壳</name>
  <files>src/features/classroom-shell/classroom-shell.tsx, src/features/classroom-shell/podium-view-model.ts, src/features/classroom-shell/stage-panel.tsx, src/features/classroom-shell/use-classroom-audio-runtime.ts, test/unit/classroom-shell.test.tsx, test/unit/classroom-orchestrator.test.ts</files>
  <read_first>
    .planning/phases/06-audio-classroom-runtime/06-01-PLAN.md
    src/features/classroom-shell/classroom-shell.tsx
    src/features/classroom-shell/podium-view-model.ts
    src/features/classroom-shell/stage-panel.tsx
    test/unit/classroom-shell.test.tsx
  </read_first>
  <behavior>
    - Test 1: 老师/Bobby 播放时，教师区/讲台区会出现轻量播放态，但课件主屏仍然可见，字幕继续显示。
    - Test 2: 当轮到孩子作答时，讲台区继续只出现一个 CTA；其文案可在“开始说 / 录音中 / 再试一次”之间切换。
    - Test 3: 录音失败、空录音或权限失败时，课堂给出柔和 retry 提示，不弹出强阻断错误层。
    - Test 4: Bobby 在 `picture-talk` 不会触发播放态；老师区与讲台区不会因为音频模式出现额外头像拉伸或布局裁切。
  </behavior>
  <action>接入 06-01 的 runtime view-model 到 `classroom-shell.tsx`。播放态优先放在老师区、讲台 live 区和现有 stage/podium 文案里，不新增会挡住 board 的音频 overlay。更新 `podium-view-model.ts`，让单 CTA 从手动确认参与过渡到真实录音入口，但仍保持一个按钮。必要时只在 `stage-panel.tsx` 补轻量的播放/录音状态可视化，不扩展独立工具栏。失败提示和 retry 必须像课堂里的柔和接住，而不是系统报警。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-shell.test.tsx test/unit/classroom-orchestrator.test.ts test/unit/classroom-audio-runtime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `classroom-shell.tsx` 在音频态下仍保持主课件、老师区、讲台区和顶部席位区完整可见或可滚动。
    - `podium-view-model.ts` 仍只暴露一个孩子侧 CTA。
    - `classroom-shell.test.tsx` 覆盖播放态、录音态、失败 retry 和 Bobby 边界。
    - `picture-talk` 下不会出现 Bobby 播放态或 Bobby copy。
  </acceptance_criteria>
  <done>课堂 UI 已能消费真实音频 runtime，孩子能看到也能理解“现在是谁在说、什么时候轮到我说”。</done>
</task>

</tasks>

<verification>
本计划的风险不是“控件不够多”，而是把课堂做回工具页。任何实现如果新增遮挡主课件的大 overlay、把讲台 CTA 拆成多个按钮、或让音频态破坏当前响应式合同，都视为未完成。
</verification>

<success_criteria>
- 课前轻量预检成立，并且 skip 后仍能继续课堂。
- 老师/Bobby 自动播报、常驻字幕和轻量播放态在壳层成立。
- 讲台区继续单 CTA，孩子的录音入口和失败重试都可理解、可用。
- 音频态 UI 不破坏已经修好的课堂布局。
</success_criteria>

<output>
Phase 6 的产品表面完成接线，课堂已经从“静态原型”进化到“会发声、能准备录音”的网页课堂。
</output>
