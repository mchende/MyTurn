---
phase: 06-audio-classroom-runtime
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01-PLAN.md
  - 06-02-PLAN.md
files_modified:
  - test/unit/classroom-shell.test.tsx
  - test/unit/classroom-audio-runtime.test.ts
  - test/e2e/audio-classroom-runtime.spec.ts
  - test/e2e/classroom-entry.spec.ts
  - playwright.config.ts
  - src/features/classroom-shell/classroom-shell.tsx
autonomous: true
requirements:
  - AUDIO-01
  - AUDIO-02
  - AUDIO-03
  - VOICE-01
  - VOICE-02
  - VOICE-03
  - CLAS-05
  - CLAS-07
  - PLAT-03
  - PLAT-04
must_haves:
  truths:
    - "Phase 6 的最终 gate 是 focused 回归，不是全量重跑所有历史套件。"
    - "音频模式下的响应式回归必须覆盖平板横屏和中等宽度，而不是只看大屏全尺寸。"
    - "浏览器验证优先证明 preflight、播放态和录音入口闭环，不把真实硬件麦克风当成 CI 成功标准。"
    - "若自动化需要权限覆盖，应使用 Playwright 官方 permission 能力或等价 mock adapter，不要手动点系统弹窗。"
  artifacts:
    - path: "test/e2e/audio-classroom-runtime.spec.ts"
      provides: "preflight -> playback -> single CTA recording 的 focused browser smoke"
      exports:
        - "audio classroom runtime smoke"
    - path: "test/unit/classroom-shell.test.tsx"
      provides: "音频模式下课堂布局、播放态、录音态与可滚动合同回归"
      exports:
        - "audio shell coverage"
  key_links:
    - from: "playwright.config.ts"
      to: "test/e2e/audio-classroom-runtime.spec.ts"
      via: "Chromium permission / launch config for focused audio smoke"
      pattern: "permissions|microphone|launchOptions"
    - from: "test/e2e/audio-classroom-runtime.spec.ts"
      to: "src/features/classroom-shell/classroom-shell.tsx"
      via: "preflight、自动播报、录音 CTA 和 retry 的真实浏览器断言"
      pattern: "preflight|recording|retry|completedSession"
---

<objective>
把音频课堂 runtime 的 focused 回归和响应式收口做完，确认 Phase 6 可以安全进入 execute-phase 的“完成”状态。

Purpose: 06-03 不是再扩新能力，而是验证音频模式没有把课堂壳、讲台区和课堂节奏做坏。
Output: focused unit/e2e 覆盖、音频模式响应式回归和可执行的 Phase 6 收尾门。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-audio-classroom-runtime/06-CONTEXT.md
@.planning/phases/06-audio-classroom-runtime/06-RESEARCH.md
@.planning/phases/06-audio-classroom-runtime/06-VALIDATION.md
@.planning/phases/06-audio-classroom-runtime/06-01-PLAN.md
@.planning/phases/06-audio-classroom-runtime/06-02-PLAN.md
@src/features/classroom-shell/classroom-shell.tsx
@test/unit/classroom-shell.test.tsx
@test/e2e/classroom-entry.spec.ts
@playwright.config.ts

<interfaces>
Expected browser-facing contract from 06-02:
```ts
type AudioClassroomUiState = {
  preflightVisible: boolean;
  playbackSpeaker: 'teacher' | 'bobby' | null;
  recordingButtonLabel: string | null;
  canRetryAudio: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 收口音频模式下的响应式和可滚动合同</name>
  <files>src/features/classroom-shell/classroom-shell.tsx, test/unit/classroom-shell.test.tsx, test/unit/classroom-audio-runtime.test.ts</files>
  <read_first>
    .planning/phases/06-audio-classroom-runtime/06-02-PLAN.md
    src/features/classroom-shell/classroom-shell.tsx
    test/unit/classroom-shell.test.tsx
  </read_first>
  <behavior>
    - Test 1: preflight 卡、播放态和录音态出现时，平板横屏依然能同时看到顶部席位、课件区、老师区和讲台区。
    - Test 2: 中等宽度视口下，课堂仍优先完整可见或可滚动，不因为新增音频态标签/提示而裁切侧栏。
    - Test 3: 轻量播放态、录音态和 retry 提示都不遮挡课件主图，不新增大遮罩。
  </behavior>
  <action>先补 `test/unit/classroom-shell.test.tsx` 的 RED 断言，再做最小范围 shell 修订。重点只处理音频模式引入的新 UI 占位、断点和滚动合同，不做新的视觉主题改版。任何需要加的新状态标签，都优先塞进现有老师区、讲台区和 stage slot，而不是占用主 board 的中央空间。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-shell.test.tsx test/unit/classroom-audio-runtime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `classroom-shell.test.tsx` 明确断言音频态下的可滚动和布局存在性。
    - 音频态 UI 没有重新引入固定高度裁切或大遮罩层。
    - 新增文案、状态条和 CTA 不会再次拉伸老师/学生头像。
  </acceptance_criteria>
  <done>音频模式不会把课堂页重新做坏，Phase 6 可以在真实设备宽度上稳定展示。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 补 focused browser smoke，证明 preflight -> playback -> 单 CTA 录音入口闭环</name>
  <files>test/e2e/audio-classroom-runtime.spec.ts, test/e2e/classroom-entry.spec.ts, playwright.config.ts</files>
  <read_first>
    .planning/phases/06-audio-classroom-runtime/06-RESEARCH.md
    playwright.config.ts
    test/e2e/classroom-entry.spec.ts
  </read_first>
  <behavior>
    - Test 1: 课堂入口会先出现 preflight gate，用户完成或跳过后才能进入主课堂。
    - Test 2: 进入课堂后，老师会触发自动播放态；在 repeat 的 Bobby 环节，Bobby 播放态会出现，但 picture-talk 不会。
    - Test 3: 轮到孩子时出现单 CTA 录音入口；失败时出现 retry 或 skip-friendly 课堂化反馈。
    - Test 4: 浏览器测试不依赖真实麦克风硬件，而是通过 Playwright 权限、mock adapter 或受控 fake runtime 达成稳定回归。
  </behavior>
  <action>新增 `test/e2e/audio-classroom-runtime.spec.ts`。优先走受控、低波动的自动化路径：能用 Playwright context permission 的地方就用官方方式，不能稳定覆盖的录音数据则通过 test adapter/mock runtime 注入。保留 `classroom-entry.spec.ts` 作为入口基线；新 spec 只补音频 runtime 闭环，不扩成整节课 full-loop。若 Playwright 配置需要为 Chromium 项目补 microphone permission 或 launch option，改动必须局部、可注释，并避免影响其他现有 smoke。</action>
  <verify>
    <automated>npm run test:e2e -- test/e2e/classroom-entry.spec.ts test/e2e/audio-classroom-runtime.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `audio-classroom-runtime.spec.ts` 覆盖 preflight pass/skip、老师/Bobby 播放态和学生录音入口。
    - focused e2e 能在不依赖真实硬件的前提下稳定运行。
    - 对 `playwright.config.ts` 的修改不影响现有非音频 smoke。
  </acceptance_criteria>
  <done>Phase 6 的关键用户路径已有 focused browser 证据，后续 Phase 7/8 可以在这个基线上继续扩语音识别闭环。</done>
</task>

</tasks>

<verification>
本计划的失败信号很明确：如果只能在大屏、真硬件、人工点击系统弹窗的条件下跑通，就不算 Phase 6 完成。自动化必须证明“课前准备、老师发声、孩子单 CTA 录音入口”在网页里能稳定成立。
</verification>

<success_criteria>
- 音频模式下课堂布局在平板横屏和中等宽度视口都稳定。
- focused browser smoke 能稳定证明 preflight、自动播报和单 CTA 录音入口闭环。
- Phase 6 完成后，执行者可以直接进入 Phase 7 的 transcript 接线，而无需回头重构音频 UI 或底层 runtime。
</success_criteria>

<output>
Focused validation gate ready: Phase 6 can be executed and later verified without large historical reruns.
</output>
