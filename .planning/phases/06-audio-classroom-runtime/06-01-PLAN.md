---
phase: 06-audio-classroom-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/classroom-shell/classroom-audio-runtime.ts
  - src/features/classroom-shell/classroom-audio-service.ts
  - src/features/classroom-shell/use-classroom-orchestrator.ts
  - src/features/classroom-shell/teacher-script.ts
  - src/features/classroom-shell/bobby-script.ts
  - test/unit/classroom-audio-runtime.test.ts
  - test/unit/teacher-script.test.ts
  - test/unit/bobby-script.test.ts
autonomous: true
requirements:
  - AUDIO-01
  - AUDIO-02
  - VOICE-01
  - VOICE-03
  - CLAS-05
  - CLAS-07
must_haves:
  truths:
    - "Phase 6 先建立共享音频 runtime contract，不重写现有 lesson state machine。"
    - "老师与 Bobby 的 spoken source 继续分别来自 `teacher-script.ts` 的 `spokenModel` 与 `bobby-script.ts` 的 `spokenLine`，不新建第三套音频文案。"
    - "课堂音频运行时必须是单通道顺序 owner：先播、再录、再等，不允许播放和录音同时抢状态。"
    - "Bobby 仍只允许在 `repeat-after-teacher` 的 `ai_model` 发声，这条边界要在 runtime / adapter 层被保护。"
    - "Phase 6 的录音 contract 只负责拿到浏览器可用音频与失败态，不在 06-01 里接 transcript 或 judgment。"
  artifacts:
    - path: "src/features/classroom-shell/classroom-audio-runtime.ts"
      provides: "共享播放/录音 runtime 状态、preflight 结果、错误态与录音输出 contract"
      exports:
        - "createClassroomAudioRuntime"
        - "ClassroomAudioStatus"
        - "ClassroomAudioErrorReason"
    - path: "src/features/classroom-shell/classroom-audio-service.ts"
      provides: "老师/Bobby 共用的音频能力接口与 provider adapter 注入边界"
      exports:
        - "ClassroomAudioService"
        - "playScriptedAudio"
        - "resolveRecordingMimeType"
    - path: "test/unit/classroom-audio-runtime.test.ts"
      provides: "单通道顺序调度、play reject、permission/recording failure 与 MIME fallback 的 baseline coverage"
      exports:
        - "runtime contract coverage"
  key_links:
    - from: "src/features/classroom-shell/use-classroom-orchestrator.ts"
      to: "src/features/classroom-shell/classroom-audio-runtime.ts"
      via: "现有 lesson phase 驱动音频 runtime 的 play / record gating"
      pattern: "phase|activeSpeaker|activeSeat|stageId"
    - from: "src/features/classroom-shell/teacher-script.ts"
      to: "src/features/classroom-shell/classroom-audio-service.ts"
      via: "`spokenModel` 作为老师发声唯一输入"
      pattern: "spokenModel|visibleCaption"
    - from: "src/features/classroom-shell/bobby-script.ts"
      to: "src/features/classroom-shell/classroom-audio-service.ts"
      via: "`spokenLine` 作为 Bobby 发声唯一输入，并保持阶段边界"
      pattern: "spokenLine|ai_model|repeat-after-teacher"
---

<objective>
先把 Phase 6 最脆弱的底层合同钉死：共享音频 service、单通道 runtime、浏览器麦克风/录音 contract 和失败态基线。

Purpose: 06-01 不直接做课堂 UI polish，而是先消除“老师播音、Bobby 边界、录音 MIME、播放失败、权限失败”这些底座不确定性。
Output: 可被 06-02 直接消费的 audio runtime contract、focused unit baseline、script-to-audio input contract。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-audio-classroom-runtime/06-CONTEXT.md
@.planning/phases/06-audio-classroom-runtime/06-RESEARCH.md
@.planning/phases/06-audio-classroom-runtime/06-VALIDATION.md
@src/features/classroom-shell/use-classroom-orchestrator.ts
@src/features/classroom-shell/teacher-script.ts
@src/features/classroom-shell/bobby-script.ts
@test/unit/classroom-orchestrator.test.ts
@test/unit/teacher-script.test.ts

<interfaces>
From src/features/classroom-shell/teacher-script.ts:
```ts
type TeacherScriptLine = {
  spokenModel: string;
  visibleCaption: string;
  hintLabel: string;
};
```

From src/features/classroom-shell/bobby-script.ts:
```ts
type BobbyScriptLine = {
  spokenLine: string;
  hintLabel: string;
  leadInPauseMs: number;
  hesitationBeatMs: number;
};
```

From MDN getUserMedia / MediaRecorder:
```ts
const stream = await navigator.mediaDevices.getUserMedia({ audio: true, video: false });
const recorder = new MediaRecorder(stream, options);
recorder.start();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立共享播放/录音 runtime 与浏览器音频失败态合同</name>
  <files>src/features/classroom-shell/classroom-audio-runtime.ts, src/features/classroom-shell/classroom-audio-service.ts, test/unit/classroom-audio-runtime.test.ts</files>
  <read_first>
    .planning/phases/06-audio-classroom-runtime/06-CONTEXT.md
    .planning/phases/06-audio-classroom-runtime/06-RESEARCH.md
    src/features/classroom-shell/use-classroom-orchestrator.ts
    test/unit/classroom-orchestrator.test.ts
  </read_first>
  <behavior>
    - Test 1: runtime 只能以单通道方式推进：老师/Bobby 播放未结束前，学生录音不能进入 `recording`。
    - Test 2: 若 `HTMLMediaElement.play()` promise reject，runtime 会进入明确的 `playback_blocked` 或等价失败态，并暴露可重试 contract，而不是假装老师已经播完。
    - Test 3: 预检或录音启动时，`getUserMedia({ audio: true, video: false })` 的 `NotAllowedError`、`NotFoundError`、空 constraints / insecure context 等都会被映射成课堂可消费的失败原因。
    - Test 4: runtime 会通过 `MediaRecorder.isTypeSupported()` 选择录音 MIME；无法启动录音或中途出错时，失败态与清理逻辑可重复运行。
    - Test 5: `MediaRecorder.start()` / `dataavailable` / `stop` 产出的 blob、mimeType 和 duration 会作为 Phase 7 可接线的 artifact 暴露，但本计划不提交 transcript。
  </behavior>
  <action>先在 `test/unit/classroom-audio-runtime.test.ts` 写 RED 用例，再新增 `src/features/classroom-shell/classroom-audio-runtime.ts` 与 `src/features/classroom-shell/classroom-audio-service.ts`。runtime 需要是纯 contract-first 设计，可以注入播放 adapter 和 mediaDevices / MediaRecorder 依赖，避免 unit test 真正碰浏览器硬件。runtime 至少要区分 `preflight`, `ready`, `playing_teacher`, `playing_bobby`, `recording_student`, `recording_failed`, `playback_failed` 这类业务态；命名可微调，但必须能被 shell 清楚消费。录音 MIME 优先通过 `MediaRecorder.isTypeSupported()` 选择浏览器支持值，不写死单一格式；每轮录音停止后必须 stop tracks 并清理 recorder/stream 引用，防止下一轮状态残留。runtime 不负责渲染 copy，只负责提供稳定状态、错误原因、录音 artifact 和重试入口。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-audio-runtime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/classroom-shell/classroom-audio-runtime.ts` 存在单通道播放/录音状态与错误原因枚举或等价类型。
    - `src/features/classroom-shell/classroom-audio-service.ts` 提供可注入的播放接口，而不是把 provider 细节塞进 shell。
    - `test/unit/classroom-audio-runtime.test.ts` 明确覆盖 play reject、permission denied、无设备、MIME fallback、录音成功与清理。
    - runtime 对外暴露录音 artifact，但没有 transcript / judgment 接线。
  </acceptance_criteria>
  <done>Phase 6 已拥有可测试的音频底座，06-02 不需要再猜播放/录音失败时到底该呈现什么状态。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 锁定老师/Bobby 的 script-to-audio 边界，不让角色越权</name>
  <files>src/features/classroom-shell/teacher-script.ts, src/features/classroom-shell/bobby-script.ts, test/unit/teacher-script.test.ts, test/unit/bobby-script.test.ts</files>
  <read_first>
    .planning/phases/06-audio-classroom-runtime/06-CONTEXT.md
    src/features/classroom-shell/teacher-script.ts
    src/features/classroom-shell/bobby-script.ts
    test/unit/teacher-script.test.ts
  </read_first>
  <behavior>
    - Test 1: 老师在 warmup、teacher prompt、encourage、fallback、wrap_up 和 completion reward 有可播放 spoken contract；字幕仍继续消费 `visibleCaption`。
    - Test 2: Bobby 只在 `repeat-after-teacher` 的 `ai_model` 暴露可播放 spoken contract；进入 `picture-talk` 或其他 phase 时不应生成可播放 Bobby cue。
    - Test 3: 脚本层能为 runtime 提供 speaker persona / key / line identity 等稳定输入，避免运行时每次凭 UI 文案猜测。
  </behavior>
  <action>补齐 `test/unit/teacher-script.test.ts` 和新增 `test/unit/bobby-script.test.ts` 的 RED 用例，然后只做最小范围的脚本 contract 修订。目标不是重写台词，而是让音频 runtime 拿到足够稳定的播放输入，例如 `spokenModel` / `spokenLine`、speaker id、可缓存 key 或等价 cue identity。老师和 Bobby 的 spoken contract 必须继续与孩子侧常驻字幕分离；任何让 Bobby 在 `picture-talk` 出声的改动都视为失败。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/teacher-script.test.ts test/unit/bobby-script.test.ts test/unit/classroom-audio-runtime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test/unit/bobby-script.test.ts` 明确断言 Bobby 在 `picture-talk` 和非 `ai_model` 阶段不发声。
    - 老师脚本测试明确覆盖 warmup / wrap_up / completion reward 的可播放 contract。
    - audio runtime 不需要再从 UI copy 反推 speaker 和 spoken line。
  </acceptance_criteria>
  <done>老师/Bobby 的音频输入边界被钉死，06-02 可以专心接 UI 和 preflight，而不是再回到底层角色 contract。</done>
</task>

</tasks>

<verification>
本计划的核心风险是把音频底层实现成“壳组件直连浏览器 API”的一次性代码。任何实现如果让 shell 直接持有 recorder/stream 生命周期、让 Bobby 越界发声、或把 play reject 当作成功，都视为未完成。
</verification>

<success_criteria>
- 共享 audio runtime 已能表达播放、录音、失败与重试合同。
- 老师/Bobby 的 script-to-audio 输入边界清晰，Bobby 角色约束被硬保护。
- 浏览器权限和录音失败态都能被 unit test 稳定重现，不依赖真实硬件。
</success_criteria>

<output>
共享音频底座、脚本音频输入合同和 focused unit baseline ready for 06-02 shell wiring.
</output>
