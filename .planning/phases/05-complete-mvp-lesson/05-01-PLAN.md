---
phase: 05-complete-mvp-lesson
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/classroom-shell/classroom-orchestrator.ts
  - src/features/classroom-shell/use-classroom-orchestrator.ts
  - src/features/classroom-shell/classroom-judgment.ts
  - test/unit/classroom-judgment.test.ts
  - test/unit/lesson-schema.test.ts
  - test/unit/classroom-orchestrator.test.ts
autonomous: true
requirements:
  - SPKG-02
  - PLAT-02
must_haves:
  truths:
    - "孩子进入课堂后先经历老师带班开场，再进入完整的 repeat-after-teacher 主段。"
    - "repeat-after-teacher 五题完整结束后，课堂自然升级到 picture-talk 五题主段，不把看图作答降级成收尾插段，并继续通过 `stageId: 'picture-talk'` 的 judged path 运行。"
    - "picture-talk 的语义相近通过仍然来自 lesson metadata 里的 `pictureTalk.semanticAccepts` 与 `judgeStudentAttempt()`，而不是按钮直通、开放描述或新评分引擎。"
    - "最后一个 picture-talk 题结束后，状态机会进入明确的 `wrap_up -> completion_reward -> lesson_complete` 收口链，并导出统一的结课停留时长给后续 UI/route 消费。"
    - "课堂节拍继续由中心 timing source 控制，并明确区分默认 demo pacing 与 automated test pacing，避免 15 分钟课堂只剩状态闭环没有节奏落点。"
  artifacts:
    - path: "src/features/classroom-shell/classroom-orchestrator.ts"
      provides: "warmup -> guided main -> wrap_up -> completion_reward -> lesson_complete 的 lesson-level 状态图，以及集中 timing profile / scale 与共享结课停留常量"
      exports:
        - "ClassroomOrchestratorPhase"
        - "CLASSROOM_TIMINGS"
        - "LESSON_COMPLETE_HOLD_MS"
        - "classroomOrchestratorReducer"
        - "createInitialClassroomState"
    - path: "src/features/classroom-shell/classroom-judgment.ts"
      provides: "picture-talk 继续通过 `judgeStudentAttempt()` 消费 `pictureTalk.semanticAccepts` 的纯判断边界"
      exports:
        - "judgeStudentAttempt"
    - path: "src/features/classroom-shell/use-classroom-orchestrator.ts"
      provides: "集中 scheduler、stage badge/prompt、lesson complete 信号与共享 hold contract"
      exports:
        - "useClassroomOrchestrator"
  key_links:
    - from: "src/features/classroom-shell/use-classroom-orchestrator.ts"
      to: "src/features/classroom-shell/classroom-orchestrator.ts"
      via: "单一 setTimeout scheduler + phase_timer_completed + LESSON_COMPLETE_HOLD_MS contract"
      pattern: "CLASSROOM_TIMINGS|CLASSROOM_TIMING_PROFILES|LESSON_COMPLETE_HOLD_MS|phase_timer_completed|completion_reward|lesson_complete"
    - from: "src/features/lesson-config/lesson-schema.ts"
      to: "src/features/classroom-shell/classroom-judgment.ts"
      via: "`pictureTalk.semanticAccepts` -> `judgeStudentAttempt()`"
      pattern: "pictureTalk|semanticAccepts|judgeStudentAttempt"
    - from: "test/unit/classroom-orchestrator.test.ts"
      to: "src/features/classroom-shell/classroom-orchestrator.ts"
      via: "warmup、guided main、wrap-up、completion 断言"
      pattern: "warmup|completion_reward|lesson_complete|LESSON_COMPLETE_HOLD_MS"
---

<objective>
把已验证的 guided speaking 主流程包进一节完整的四段式课堂：老师带班开场、完整复述主段、完整看图主段、老师结课收束与完成态。

Purpose: Phase 5 的第一落点不是再造新能力，而是先把 D-01 到 D-11 里最脆弱的状态机、judgment 和 pacing 合同钉死，让后续 UI 收口只消费已定义好的 lesson-complete contract。
Output: lesson-level reducer/judgment/timing contract、共享 `LESSON_COMPLETE_HOLD_MS`、四段式课堂的 focused unit 回归。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-complete-mvp-lesson/05-CONTEXT.md
@.planning/phases/05-complete-mvp-lesson/05-RESEARCH.md
@.planning/phases/05-complete-mvp-lesson/05-VALIDATION.md
@.planning/phases/03-guided-speaking-flow/03-03-SUMMARY.md
@.planning/phases/04-hints-and-judgment/04-02-SUMMARY.md
@.planning/phases/04-hints-and-judgment/04-03-SUMMARY.md
@src/features/lesson-config/lesson-schema.ts
@src/features/classroom-shell/classroom-orchestrator.ts
@src/features/classroom-shell/use-classroom-orchestrator.ts
@src/features/classroom-shell/classroom-judgment.ts
@test/unit/classroom-orchestrator.test.ts
@test/unit/classroom-judgment.test.ts
@test/unit/lesson-schema.test.ts

<interfaces>
From src/features/lesson-config/lesson-schema.ts:
```ts
export const lessonStageSchema = z.object({
  id: z.enum(['warmup', 'repeat-after-teacher', 'picture-talk', 'wrap-up']),
  itemIds: z.array(z.string().min(1)).min(1),
});
```

From src/features/classroom-shell/classroom-orchestrator.ts:
```ts
export const GUIDED_STAGE_IDS = ['repeat-after-teacher', 'picture-talk'] as const;
export const LESSON_COMPLETE_HOLD_MS = 3000;

export type ClassroomOrchestratorPhase =
  | 'teacher_prompt'
  | 'ai_model'
  | 'student_wait'
  | 'teacher_encourage'
  | 'teacher_fallback_model'
  | 'teacher_echo'
  | 'teacher_feedback'
  | 'move_next'
  | 'wrap_up';
```

From src/features/classroom-shell/classroom-judgment.ts:
```ts
export function judgeStudentAttempt(input: {
  lessonItem: LessonItem;
  stageId: JudgmentStageId;
  transcript: string | null | undefined;
  attemptIndex: number;
}): StudentAttemptJudgment;
```

From src/features/classroom-shell/use-classroom-orchestrator.ts:
```ts
return {
  ...state,
  stageBadge,
  stagePrompt,
  teacherHint,
  teacherMessage,
  podiumViewModel,
  confirmStudentParticipation: () => void,
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立四段式完整 lesson 的 reducer、judgment 与共享 hold 合同</name>
  <files>src/features/classroom-shell/classroom-orchestrator.ts, src/features/classroom-shell/use-classroom-orchestrator.ts, src/features/classroom-shell/classroom-judgment.ts, test/unit/classroom-orchestrator.test.ts, test/unit/classroom-judgment.test.ts, test/unit/lesson-schema.test.ts</files>
  <read_first>
    .planning/phases/05-complete-mvp-lesson/05-CONTEXT.md
    .planning/phases/05-complete-mvp-lesson/05-RESEARCH.md
    .planning/phases/05-complete-mvp-lesson/05-VALIDATION.md
    src/features/lesson-config/lesson-schema.ts
    src/features/classroom-shell/classroom-orchestrator.ts
    src/features/classroom-shell/use-classroom-orchestrator.ts
    src/features/classroom-shell/classroom-judgment.ts
    test/unit/classroom-orchestrator.test.ts
    test/unit/classroom-judgment.test.ts
    test/unit/lesson-schema.test.ts
  </read_first>
  <behavior>
    - Test 1: per D-01/D-03，初始状态不是直接进入 `teacher_prompt`，而是先进入老师主导的 `warmup`；`GUIDED_STAGE_IDS` 仍然只等于 `['repeat-after-teacher', 'picture-talk']`。
    - Test 2: per SPKG-02 + D-05/D-06/D-07，picture-talk 仍然通过 `stageId: 'picture-talk'` 调用 `judgeStudentAttempt()`，并且 picture 判断继续消费 `lesson.items[*].pictureTalk.semanticAccepts`；语义相近表达通过，不允许把确认按钮当作 auto-pass。
    - Test 3: per D-01/D-02/D-05/D-06/D-07，完整跑完五个 repeat item 和五个 picture item 后，reducer 依次进入 `wrap_up -> completion_reward -> lesson_complete`，不会把 picture-talk 降级回 repeat，也不会把 warmup / wrap-up 塞进 judged queue。
    - Test 4: per D-09/D-10/D-11，`rewardVisible` 只在 `completion_reward` 为 `true`，不会在每题 `teacher_feedback` 时亮起；`lesson_complete` 是稳定结束态而不是继续跑 timer 的中间态，并且共享 `LESSON_COMPLETE_HOLD_MS` 常量固定为 3000。
    - Test 5: per “打磨的 15 分钟课堂”目标，lesson-level 节拍必须继续由单一中心 timing profile / scale 驱动，默认 demo pacing 与 automated test pacing 都从同一 timing source 派生，而不是在组件里散落 magic numbers。
  </behavior>
  <action>先在 `test/unit/classroom-orchestrator.test.ts`、`test/unit/classroom-judgment.test.ts`、`test/unit/lesson-schema.test.ts` 写 RED 用例，再改 `src/features/classroom-shell/classroom-orchestrator.ts`、`src/features/classroom-shell/use-classroom-orchestrator.ts`，必要时只做最小范围的 `src/features/classroom-shell/classroom-judgment.ts` 兼容修订。保持 `GUIDED_STAGE_IDS` 精确为 `['repeat-after-teacher', 'picture-talk']`，不要把 `warmup` 或 `wrap-up` 加进 judged speaking 队列。明确保住 SPKG-02 judged path：`student_attempt_submitted` 在 picture 主段时必须继续以 `stageId: 'picture-talk'` 调用 `judgeStudentAttempt()`，并由 lesson metadata 中的 `pictureTalk.semanticAccepts` 决定语义相近通过；不要改成按钮直通、开放描述、LLM 评分或新语义引擎。将 `ClassroomOrchestratorPhase` 扩展为包含 `warmup`、`completion_reward`、`lesson_complete` 三个新值；保留现有 `wrap_up`，但把它从 terminal dead end 改成老师 closing 的 timed phase。必须在 `src/features/classroom-shell/classroom-orchestrator.ts` 明确导出 `LESSON_COMPLETE_HOLD_MS = 3000`，作为 lesson 完成态停留的共享合同；05-02 的 auto-return 只能消费这个导出，不能再自写第二份 3000ms。将中心 timing source 升级为显式 profile / scale，例如 `CLASSROOM_TIMING_PROFILES = { demo: {...}, test: {...} }` 或等价 `CLASSROOM_TIMING_SCALE` 设计；默认 demo pacing 必须使用 `warmup: 2400`、`wrap_up: 2200`、`completion_reward: 1400`，其余现有阶段继续集中定义，automated test pacing 也必须从同一中心 source 派生。hook 继续只保留一个 `setTimeout` scheduler；当 `state.phase === 'lesson_complete'` 时不再注册新 timer。`createInitialClassroomState()` 必须从 `phase: 'warmup'` 启动，`activeSpeaker: 'teacher'`、`activeSeat: null`、`rewardVisible: false`。`advanceTimedPhase()` 的具体转移必须是：`warmup -> teacher_prompt`；最终 `move_next` 完成最后一个 picture-talk item 后进入 `wrap_up`；`wrap_up -> completion_reward` 时设置 `rewardVisible: true`；`completion_reward -> lesson_complete` 时设置 `rewardVisible: false` 并保留课堂完成态。不要在本计划处理老师/讲台/课堂壳 UI polish；这些 child-facing closeout surface 留给 05-02 消费已导出的 contract。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-orchestrator.test.ts test/unit/classroom-judgment.test.ts test/unit/lesson-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/classroom-shell/classroom-orchestrator.ts` 中的 `ClassroomOrchestratorPhase` 可 grep 到 `'warmup'`、`'completion_reward'`、`'lesson_complete'`。
    - `src/features/classroom-shell/classroom-orchestrator.ts` 中的 `GUIDED_STAGE_IDS` 仍然精确是 `['repeat-after-teacher', 'picture-talk']`。
    - `src/features/classroom-shell/classroom-orchestrator.ts` 可 grep 到 `LESSON_COMPLETE_HOLD_MS = 3000`。
    - `src/features/classroom-shell/classroom-orchestrator.ts` 或相邻 timing 模块可 grep 到 `CLASSROOM_TIMING_PROFILES` 或 `CLASSROOM_TIMING_SCALE` 这类中心 pacing 定义。
    - `src/features/classroom-shell/classroom-orchestrator.ts` 与 `src/features/classroom-shell/classroom-judgment.ts` 可 grep 到 `judgeStudentAttempt`、`picture-talk`、`semanticAccepts` 的 judged path 保护。
    - `test/unit/classroom-orchestrator.test.ts` 包含针对 `warmup`、`completion_reward`、`lesson_complete` 的用例名和断言。
    - `test/unit/classroom-judgment.test.ts` 与 `test/unit/lesson-schema.test.ts` 明确覆盖 `pictureTalk.semanticAccepts` 的语义相近通过合同。
    - `src/features/classroom-shell/use-classroom-orchestrator.ts` 仍只存在一个 `window.setTimeout` 调度 effect。
  </acceptance_criteria>
  <done>完整课堂的 phase graph、SPKG-02 judged path、`LESSON_COMPLETE_HOLD_MS` 和中心 pacing source 都已明确，执行者不需要再猜 warmup、主段、semantic pass、reward 和完成态分别归谁控制。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 暴露可消费的 completion state 与 pacing contract，供 05-02 接 UI 收口</name>
  <files>src/features/classroom-shell/use-classroom-orchestrator.ts, test/unit/classroom-orchestrator.test.ts</files>
  <read_first>
    .planning/phases/05-complete-mvp-lesson/05-CONTEXT.md
    .planning/phases/05-complete-mvp-lesson/05-RESEARCH.md
    src/features/classroom-shell/classroom-orchestrator.ts
    src/features/classroom-shell/use-classroom-orchestrator.ts
    test/unit/classroom-orchestrator.test.ts
  </read_first>
  <behavior>
    - Test 1: hook 层必须暴露稳定的 `lesson_complete` 完成信号与对应 stage badge / prompt key，供 05-02 的 teacher/podium/shell UI 直接消费，不需要再猜状态。
    - Test 2: hook 层必须暴露或透传 `LESSON_COMPLETE_HOLD_MS` / 等价 completion hold contract，让 05-02 的 auto-return 精确引用同一个值。
    - Test 3: hook 继续只保留单 scheduler 与 judged speaking surface，不在 05-01 额外引入 teacher/podium/shell UI 关闭逻辑。
  </behavior>
  <action>不要在这个任务继续写 teacher-script、podium-view-model、classroom-shell 的 child-facing 文案和壳层细节，把 UI 收口面留给 05-02。这里只修改 `src/features/classroom-shell/use-classroom-orchestrator.ts` 与对应 unit coverage：让 hook 对外暴露稳定的 completion contract，至少包括 `stageBadge` / `stagePrompt` 能区分 `warmup`、`wrap_up`、`completion_reward`、`lesson_complete`，并且对 05-02 提供 `LESSON_COMPLETE_HOLD_MS` 的可消费来源，可以是直接 re-export，也可以是显式返回字段，但必须做到 05-02 无需重新定义结课停留时长。保持 hook 继续使用单一 scheduler，不新增 teacher/podium/shell UI closeout 逻辑，不在本任务引入第二套渲染层状态。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/classroom-shell/use-classroom-orchestrator.ts` 可 grep 到 `Class warmup`、`Class closing`、`Class complete` 三个 stage badge / prompt 字符串。
    - `src/features/classroom-shell/use-classroom-orchestrator.ts` 可 grep 到 `LESSON_COMPLETE_HOLD_MS` 的 re-export 或消费。
    - `test/unit/classroom-orchestrator.test.ts` 明确断言 hook 层对 `lesson_complete` 和共享 hold contract 的暴露。
  </acceptance_criteria>
  <done>05-01 现在只产出状态机、judgment、hook 与共享 hold 合同；后续 UI 收口可以在 05-02 直接消费这些导出，不需要再回头改 plan 1。</done>
</task>

</tasks>

<verification>
本计划的核心风险不是功能不够，而是把 Phase 3/4 的稳定主流程打散。任何实现如果改了 `GUIDED_STAGE_IDS`、绕开 `judgeStudentAttempt()` / `pictureTalk.semanticAccepts` judged path、把 reward 插进每题反馈，或者把 pacing magic numbers 散到多个组件里，都视为未完成。
</verification>

<success_criteria>
- 课堂先 warmup，再完整跑完 repeat-after-teacher 与 picture-talk，再进入老师结课收束。
- picture-talk 仍然是老师问、孩子答、通过 `pictureTalk.semanticAccepts` + `judgeStudentAttempt()` 达成语义相近可过的低门槛主段，不是中途插入的小尾巴。
- reward 只在整节课结束时出现一次，随后课堂停在明确完成态。
- `LESSON_COMPLETE_HOLD_MS` 与中心 timing profile / scale 由 05-01 明确产出，既能支撑更像完整短课的 demo pacing，也能被 05-02 复用而不重复定义。
</success_criteria>

<output>
After completion, create `.planning/phases/05-complete-mvp-lesson/05-01-SUMMARY.md`
</output>
