---
phase: 05-complete-mvp-lesson
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/app/(marketing)/page.tsx
  - src/app/lesson/[sessionId]/page.tsx
  - src/features/classroom-shell/classroom-shell.tsx
  - src/features/homepage/homepage-shell.tsx
  - src/features/schedule/get-today-schedule-view-model.ts
  - test/unit/homepage-shell.test.tsx
  - test/unit/get-today-schedule-view-model.test.ts
  - test/e2e/complete-mvp-lesson.spec.ts
autonomous: true
requirements:
  - PLAT-02
  - SPKG-02
must_haves:
  truths:
    - "结课后孩子会留在课堂完成态约 3 秒，然后自动回到首页，不会跳去额外结果页。"
    - "回到首页后，刚上完的场次会显示明确完成态和余温文案，同时真实 `accessState` 仍保持时间真相。"
    - "较窄视口下首页的 hero 与 session timeline 仍然直接可见，不再被固定三栏和 `overflow-hidden` 裁掉。"
  artifacts:
    - path: "src/app/(marketing)/page.tsx"
      provides: "读取 `completedSession` query 并把 completion overlay 传入首页 view-model"
      exports:
        - "default"
    - path: "src/features/schedule/get-today-schedule-view-model.ts"
      provides: "保留真实 accessState 的 recently-completed overlay contract"
      exports:
        - "getTodayScheduleViewModel"
    - path: "src/features/homepage/homepage-shell.tsx"
      provides: "just-completed hero/timeline 状态与中窄视口 responsive reflow"
      exports:
        - "HomepageShell"
    - path: "test/e2e/complete-mvp-lesson.spec.ts"
      provides: "homepage -> lesson -> complete -> auto-return 的浏览器闭环回归"
  key_links:
    - from: "src/features/classroom-shell/classroom-shell.tsx"
      to: "src/app/(marketing)/page.tsx"
      via: "router.replace('/?completedSession=...')"
      pattern: "completedSession|router\\.replace"
    - from: "src/app/(marketing)/page.tsx"
      to: "src/features/schedule/get-today-schedule-view-model.ts"
      via: "completedSession query -> overlay options"
      pattern: "completedSession|isRecentlyCompleted"
    - from: "src/features/homepage/homepage-shell.tsx"
      to: "test/unit/homepage-shell.test.tsx"
      via: "just-completed hero pill, timeline label, responsive class contract"
      pattern: "刚完成这节课|刚完成|xl:flex-row|overflow-y-auto"
---

<objective>
把课堂内完成态接回网页端闭环：lesson 自动回首页、首页保留“刚完成这节课”的余温，并修掉首页在中窄视口下的布局短板。

Purpose: 这是 `PLAT-02` 的收口计划。完成后，孩子可以只在网页里经历“进教室 -> 上完课 -> 下课回首页”的完整闭环，不需要原生 App，也不会落到额外结果页。
Output: homepage completion overlay contract、responsive homepage reflow、lesson auto-return wiring、新的 full-loop Playwright smoke。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-complete-mvp-lesson/05-CONTEXT.md
@.planning/phases/05-complete-mvp-lesson/05-RESEARCH.md
@.planning/phases/05-complete-mvp-lesson/05-VALIDATION.md
@.planning/phases/05-complete-mvp-lesson/05-01-PLAN.md
@src/app/(marketing)/page.tsx
@src/app/lesson/[sessionId]/page.tsx
@src/features/classroom-shell/classroom-shell.tsx
@src/features/homepage/homepage-shell.tsx
@src/features/schedule/get-today-schedule-view-model.ts
@test/unit/homepage-shell.test.tsx
@test/e2e/classroom-entry.spec.ts
@test/e2e/guided-speaking-flow.spec.ts

<interfaces>
From src/features/schedule/get-today-schedule-view-model.ts:
```ts
export type TodayScheduleSessionViewModel = {
  sessionId: string;
  accessState: SessionAccessState;
  startTimeLabel: string;
  countdownLabel: string;
  campLabel: string;
  learnerLabel: string;
  attendanceLabel: string;
};
```

From src/app/(marketing)/page.tsx:
```ts
export default async function HomePage() {
  const viewModel = getTodayScheduleViewModel();
  return <HomepageShell viewModel={viewModel} />;
}
```

From src/app/lesson/[sessionId]/page.tsx:
```ts
export default async function LessonPage({ params, searchParams }: LessonPageProps) {
  const { sessionId } = await params;
  return <ClassroomShell sessionId={sessionId} ... />;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立首页 recently-completed overlay 合同并完成响应式重排</name>
  <files>src/app/(marketing)/page.tsx, src/features/schedule/get-today-schedule-view-model.ts, src/features/homepage/homepage-shell.tsx, test/unit/get-today-schedule-view-model.test.ts, test/unit/homepage-shell.test.tsx</files>
  <read_first>
    .planning/phases/05-complete-mvp-lesson/05-CONTEXT.md
    .planning/phases/05-complete-mvp-lesson/05-RESEARCH.md
    src/app/(marketing)/page.tsx
    src/features/schedule/get-today-schedule-view-model.ts
    src/features/homepage/homepage-shell.tsx
    test/unit/homepage-shell.test.tsx
  </read_first>
  <behavior>
    - Test 1: per D-13，首页 route 接收 `completedSession` query 后，只把匹配场次标成 recently completed overlay，不改真实 `accessState`。
    - Test 2: per D-13，`HomepageShell` 优先聚焦 `isRecentlyCompleted === true` 的 session，并显示 `刚完成这节课` 的 hero 标识和 `刚完成` 的 timeline 标签。
    - Test 3: per D-12，首页在中窄视口下改为纵向/单主列重排，hero 和 session timeline 仍直接可见，不再依赖 `h-screen + overflow-hidden + 固定横向双列`。
  </behavior>
  <action>先新增 `test/unit/get-today-schedule-view-model.test.ts`，并扩展 `test/unit/homepage-shell.test.tsx`。把 `src/app/(marketing)/page.tsx` 改成读取 `searchParams?: Promise<{ completedSession?: string }>`，并把 `completedSession` 传给 `getTodayScheduleViewModel(now, { completedSessionId })`。在 `src/features/schedule/get-today-schedule-view-model.ts` 中新增第二个参数 `options?: { completedSessionId?: string | null }`，并把 `TodayScheduleSessionViewModel` 扩展出 `isRecentlyCompleted: boolean` 字段；该字段只能由 `session.sessionId === completedSessionId` 计算得到，`accessState` 必须保持原值，不允许新增 `just_completed` 这类枚举。`HomepageShell` 必须把焦点 session 计算为 `viewModel.sessions.find((session) => session.isRecentlyCompleted) ?? viewModel.nextSession ?? viewModel.sessions[0]`。当焦点 session 是 recently completed 时，hero pill 文案固定为 `刚完成这节课`，timeline 对应行标签固定为 `刚完成`，hero 主文案追加 `今天这节课已经上完啦`。同时把首页布局改成 mobile-first：根 `<main>` 使用 `min-h-screen bg-[#F9FAFB] text-[#1F2937] xl:h-screen xl:overflow-hidden`；主 wrapper 使用 `flex min-h-screen flex-col xl:h-screen xl:flex-row`；主内容区使用 `flex flex-1 flex-col gap-6 overflow-y-auto p-6 xl:flex-row xl:gap-8 xl:overflow-hidden xl:p-8`。不要用 JS resize listener，不要把 recently completed 写进 `SessionAccessState`。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/get-today-schedule-view-model.test.ts test/unit/homepage-shell.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(marketing)/page.tsx` 可 grep 到 `completedSession`，且把该值传入 `getTodayScheduleViewModel(`。
    - `src/features/schedule/get-today-schedule-view-model.ts` 可 grep 到 `isRecentlyCompleted: boolean`。
    - `src/features/homepage/homepage-shell.tsx` 可 grep 到 `刚完成这节课`、`刚完成`、`xl:flex-row`、`overflow-y-auto`。
    - `test/unit/get-today-schedule-view-model.test.ts` 明确断言 recently-completed overlay 不会改写原始 `accessState`。
  </acceptance_criteria>
  <done>首页已经能表达“刚上完这节课”的网页余温，并且中窄视口下不再丢失中部和右侧内容。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接通 lesson 自动回首页并补 full-loop E2E 回归门</name>
  <files>src/features/classroom-shell/classroom-shell.tsx, src/app/lesson/[sessionId]/page.tsx, test/e2e/complete-mvp-lesson.spec.ts</files>
  <read_first>
    .planning/phases/05-complete-mvp-lesson/05-CONTEXT.md
    .planning/phases/05-complete-mvp-lesson/05-RESEARCH.md
    .planning/phases/05-complete-mvp-lesson/05-01-PLAN.md
    src/features/classroom-shell/classroom-shell.tsx
    src/app/lesson/[sessionId]/page.tsx
    test/e2e/classroom-entry.spec.ts
    test/e2e/guided-speaking-flow.spec.ts
  </read_first>
  <behavior>
    - Test 1: per D-10/D-11，`classroom.phase === 'lesson_complete'` 后，客户端只启动一个 3000ms timer，再 `router.replace('/?completedSession=${sessionId}', { scroll: false })` 回首页。
    - Test 2: per PLAT-02，完整浏览器流程是 `首页 -> lesson -> 五次 I said it -> 五次 I answered -> reward -> lesson_complete -> 首页 recently completed`，没有单独结果页或中途跳转。
    - Test 3: per SPKG-02，完整闭环 E2E 必须经过 picture-talk 主段，且 picture-talk 仍保持老师提问、孩子回答、语义相近可过的现有合同，不允许跳过后半程。
  </behavior>
  <action>先创建 `test/e2e/complete-mvp-lesson.spec.ts` 的 RED 用例，再接通实现。`src/features/classroom-shell/classroom-shell.tsx` 必须引入 `useRouter` 和 `useEffect`，只在 `classroom.phase === 'lesson_complete'` 时启动一个 `window.setTimeout(() => router.replace('/?completedSession=' + sessionId, { scroll: false }), 3000)`，并在 cleanup 里清掉 timer；不要在其他 phase 启动 redirect timer，也不要在 homepage 或 lesson page 再挂第二条 3000ms 链。`src/app/lesson/[sessionId]/page.tsx` 继续保留现有 `notFound()` 和 `ClassroomShell` 渲染合同，不新增 `results`、`summary`、`completed` 等中间路由。`test/e2e/complete-mvp-lesson.spec.ts` 要明确断言以下字符串和状态：课堂里先出现 `GREAT JOB!`，随后出现 `Class complete` 与 `See you next time.`，然后 URL 命中 `/\\?completedSession=weekday-1700/`，最后首页显示 `刚完成这节课` 与 `刚完成`。E2E 中必须实际点击 5 次 `I said it` 和 5 次 `I answered`，不要通过 query 参数绕过完整 lesson。</action>
  <verify>
    <automated>npm run test:e2e -- test/e2e/classroom-entry.spec.ts test/e2e/guided-speaking-flow.spec.ts test/e2e/complete-mvp-lesson.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/classroom-shell/classroom-shell.tsx` 可 grep 到 `router.replace('/?completedSession=` 和 `3000`。
    - `test/e2e/complete-mvp-lesson.spec.ts` 文件存在，且可 grep 到 `GREAT JOB!`、`Class complete`、`刚完成这节课`、`刚完成`。
    - `src/app/lesson/[sessionId]/page.tsx` 仍然直接渲染 `ClassroomShell`，没有新增 `redirect(` 或新结果页路由引用。
  </acceptance_criteria>
  <done>网页闭环已经打通，用户可以只在浏览器里完成“进班、上完、下课、回首页”的完整 MVP lesson。</done>
</task>

</tasks>

<verification>
Phase 5 的网页闭环必须由一个 completion owner 串起来。若 redirect timer 同时出现在 classroom shell、lesson route 和 homepage，或者 recently completed 污染了真实 access state，执行即视为失败。
</verification>

<success_criteria>
- lesson 在 `lesson_complete` 后约 3 秒自动回首页，不出现额外结果页。
- 首页对刚完成场次显示 `刚完成这节课` / `刚完成`，同时真实 `accessState` 保持不变。
- homepage -> lesson -> complete -> homepage 的 Playwright 闭环回归可自动通过。
</success_criteria>

<output>
After completion, create `.planning/phases/05-complete-mvp-lesson/05-02-SUMMARY.md`
</output>
