---
phase: 05-complete-mvp-lesson
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/app/(marketing)/page.tsx
  - src/features/schedule/get-today-schedule-view-model.ts
  - src/features/homepage/homepage-shell.tsx
  - test/unit/get-today-schedule-view-model.test.ts
  - test/unit/homepage-shell.test.tsx
autonomous: true
requirements:
  - PLAT-02
must_haves:
  truths:
    - "回到首页后，刚上完的场次会显示明确完成态和余温文案，同时真实 `accessState` 仍保持时间真相。"
    - "首页在较窄视口下改为响应式重排后，hero 与 session timeline 仍然直接可见，不再被固定三栏和 `overflow-hidden` 裁掉。"
  artifacts:
    - path: "src/app/(marketing)/page.tsx"
      provides: "读取 `completedSession` query 并把 completion overlay 传入首页 view-model"
      exports:
        - "default"
    - path: "src/features/schedule/get-today-schedule-view-model.ts"
      provides: "保留真实 accessState 的 recently-completed overlay contract"
      exports:
        - "getTodayScheduleViewModel"
    - path: "src/features/homepage/homepage-shell.tsx"
      provides: "just-completed hero/timeline 状态与中窄视口 responsive reflow"
      exports:
        - "HomepageShell"
  key_links:
    - from: "src/app/(marketing)/page.tsx"
      to: "src/features/schedule/get-today-schedule-view-model.ts"
      via: "completedSession query -> overlay options"
      pattern: "completedSession|isRecentlyCompleted"
    - from: "src/features/homepage/homepage-shell.tsx"
      to: "test/unit/homepage-shell.test.tsx"
      via: "just-completed hero pill, timeline label, responsive class contract"
      pattern: "刚完成这节课|刚完成|xl:flex-row|overflow-y-auto"
---

<objective>
建立首页 recently-completed overlay 合同并完成响应式重排，让网页端能稳稳接住课堂完成后的回首页余温。

Purpose: 这是 Phase 5 的首页收口计划。完成后，首页可以表达“刚上完一节课”的状态，同时修掉中窄视口下中部/右侧像消失一样的布局问题。
Output: homepage completion overlay contract、responsive homepage reflow、focused unit coverage。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-complete-mvp-lesson/05-CONTEXT.md
@.planning/phases/05-complete-mvp-lesson/05-RESEARCH.md
@.planning/phases/05-complete-mvp-lesson/05-VALIDATION.md
@.planning/phases/05-complete-mvp-lesson/05-01-PLAN.md
@src/app/(marketing)/page.tsx
@src/features/schedule/get-today-schedule-view-model.ts
@src/features/homepage/homepage-shell.tsx
@test/unit/homepage-shell.test.tsx

<interfaces>
From src/features/schedule/get-today-schedule-view-model.ts:
```ts
export type TodayScheduleSessionViewModel = {
  sessionId: string;
  accessState: SessionAccessState;
  startTimeLabel: string;
  countdownLabel: string;
  campLabel: string;
  learnerLabel: string;
  attendanceLabel: string;
};
```

From src/app/(marketing)/page.tsx:
```ts
export default async function HomePage() {
  const viewModel = getTodayScheduleViewModel();
  return <HomepageShell viewModel={viewModel} />;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立首页 recently-completed overlay 的 view-model 合同</name>
  <files>src/app/(marketing)/page.tsx, src/features/schedule/get-today-schedule-view-model.ts, test/unit/get-today-schedule-view-model.test.ts</files>
  <read_first>
    .planning/phases/05-complete-mvp-lesson/05-CONTEXT.md
    .planning/phases/05-complete-mvp-lesson/05-RESEARCH.md
    src/app/(marketing)/page.tsx
    src/features/schedule/get-today-schedule-view-model.ts
  </read_first>
  <behavior>
    - Test 1: per D-13，首页 route 接收 `completedSession` query 后，只把匹配场次标成 recently completed overlay，不改真实 `accessState`。
    - Test 2: 首页 focus session 计算优先 recent-complete 场次，再回退到 `nextSession` 与默认首场。
  </behavior>
  <action>先新增 `test/unit/get-today-schedule-view-model.test.ts` 的 RED 用例，再修改 `src/app/(marketing)/page.tsx` 与 `src/features/schedule/get-today-schedule-view-model.ts`。把 `HomePage` 改成读取 `searchParams?: Promise<{ completedSession?: string }>`，并把 `completedSession` 传给 `getTodayScheduleViewModel(undefined, { completedSessionId })`。在 `getTodayScheduleViewModel` 中新增第二个参数 `options?: { completedSessionId?: string | null }`，并把 `TodayScheduleSessionViewModel` 扩展出 `isRecentlyCompleted: boolean`；该字段只能由 `session.sessionId === completedSessionId` 计算得到，`accessState` 必须保持原值，不允许新增 `just_completed` 这类枚举，也不允许修改 time selector 真相。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/get-today-schedule-view-model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(marketing)/page.tsx` 可 grep 到 `completedSession`，且把该值传入 `getTodayScheduleViewModel(`。
    - `src/features/schedule/get-today-schedule-view-model.ts` 可 grep 到 `isRecentlyCompleted: boolean`。
    - `test/unit/get-today-schedule-view-model.test.ts` 明确断言 recently-completed overlay 不会改写原始 `accessState`。
  </acceptance_criteria>
  <done>首页 view-model 已经能在不污染真实时间状态的前提下表达“刚完成一节课”的 overlay。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 完成 homepage shell 的余温展示与响应式重排</name>
  <files>src/features/homepage/homepage-shell.tsx, test/unit/homepage-shell.test.tsx</files>
  <read_first>
    .planning/phases/05-complete-mvp-lesson/05-CONTEXT.md
    .planning/phases/05-complete-mvp-lesson/05-RESEARCH.md
    src/features/homepage/homepage-shell.tsx
    src/features/schedule/get-today-schedule-view-model.ts
    test/unit/homepage-shell.test.tsx
  </read_first>
  <behavior>
    - Test 1: per D-13，`HomepageShell` 优先聚焦 `isRecentlyCompleted === true` 的 session，并显示 `刚完成这节课` 的 hero 标识和 `刚完成` 的 timeline 标签。
    - Test 2: per D-12，首页在中窄视口下改为纵向/单主列重排，hero 和 session timeline 仍直接可见，不再依赖 `h-screen + overflow-hidden + 固定横向双列`。
  </behavior>
  <action>先扩展 `test/unit/homepage-shell.test.tsx` 写 RED，再修改 `src/features/homepage/homepage-shell.tsx`。焦点 session 计算必须是 `viewModel.sessions.find((session) => session.isRecentlyCompleted) ?? viewModel.nextSession ?? viewModel.sessions[0]`。当焦点 session 是 recently completed 时，hero pill 文案固定为 `刚完成这节课`，timeline 对应行标签固定为 `刚完成`，hero 主文案追加 `今天这节课已经上完啦`。同时把首页布局改成 mobile-first：根 `<main>` 使用 `min-h-screen bg-[#F9FAFB] text-[#1F2937] xl:h-screen xl:overflow-hidden`；主 wrapper 使用 `flex min-h-screen flex-col xl:h-screen xl:flex-row`；主内容区使用 `flex flex-1 flex-col gap-6 overflow-y-auto p-6 xl:flex-row xl:gap-8 xl:overflow-hidden xl:p-8`。不要用 JS resize listener，不要再让中窄视口依赖固定三栏硬撑。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/homepage-shell.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/homepage/homepage-shell.tsx` 可 grep 到 `刚完成这节课`、`刚完成`、`xl:flex-row`、`overflow-y-auto`。
    - `test/unit/homepage-shell.test.tsx` 明确覆盖 recently-completed hero/timeline 与响应式 class contract。
  </acceptance_criteria>
  <done>首页已经能表达“刚上完这节课”的网页余温，并且中窄视口下不再丢失中部和右侧内容。</done>
</task>

</tasks>

<verification>
本计划只负责首页余温与首页响应式收口。若实现污染了真实 `accessState`、引入新的 schedule state 枚举、或继续依赖固定三栏 + `overflow-hidden` 导致主内容在中窄视口下不可见，则视为失败。
</verification>

<success_criteria>
- 首页 route 能接收 `completedSession` query 并把它转成 recently-completed overlay。
- 首页 hero 与 timeline 能清楚表达“刚完成这节课”的余温。
- 首页在较窄视口下仍能直接看到 hero 和 session timeline，不再像内容消失一样。
</success_criteria>

<output>
After completion, create `.planning/phases/05-complete-mvp-lesson/05-02-SUMMARY.md`
</output>
