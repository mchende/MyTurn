---
phase: 05-complete-mvp-lesson
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/app/lesson/[sessionId]/page.tsx
  - src/features/classroom-shell/use-classroom-orchestrator.ts
  - src/features/classroom-shell/teacher-script.ts
  - src/features/classroom-shell/podium-view-model.ts
  - src/features/classroom-shell/classroom-shell.tsx
  - test/unit/classroom-orchestrator.test.ts
  - test/unit/teacher-script.test.ts
  - test/unit/classroom-shell.test.tsx
  - test/e2e/guided-speaking-flow.spec.ts
  - test/e2e/complete-mvp-lesson.spec.ts
  - test/e2e/classroom-entry.spec.ts
autonomous: true
requirements:
  - PLAT-02
  - SPKG-02
must_haves:
  truths:
    - "结课后孩子会留在课堂完成态约 3 秒，然后自动回到首页，不会跳去额外结果页。"
    - "完整闭环仍然经过 picture-talk judged 主段，语义相近通过继续由 `pictureTalk.semanticAccepts` 和 `judgeStudentAttempt()` 保障，而不是被自动跳过。"
    - "看图题作答成功后，老师只给一句很短的课堂式肯定 `Nice answer.`，并在当前 `teacher_feedback` 结束后立即进入下一题。"
    - "teacher / podium / shell 的 warmup、wrap-up、completion child-facing surface 继续保持单 CTA 和无答案泄露。"
  artifacts:
    - path: "src/features/classroom-shell/classroom-shell.tsx"
      provides: "消费 `LESSON_COMPLETE_HOLD_MS` 的 teacher/podium/shell closeout surface 与 auto-return"
      exports:
        - "ClassroomShell"
    - path: "src/features/classroom-shell/teacher-script.ts"
      provides: "warmup / closing / completion copy 与 picture-talk success 的短 praise 合同"
      exports:
        - "getTeacherScriptLine"
    - path: "test/e2e/complete-mvp-lesson.spec.ts"
      provides: "homepage -> lesson -> complete -> auto-return 的浏览器闭环回归"
  key_links:
    - from: "src/features/classroom-shell/classroom-shell.tsx"
      to: "src/features/classroom-shell/classroom-orchestrator.ts"
      via: "`LESSON_COMPLETE_HOLD_MS` -> router.replace('/?completedSession=...')"
      pattern: "LESSON_COMPLETE_HOLD_MS|completedSession|router\\.replace"
    - from: "src/features/classroom-shell/teacher-script.ts"
      to: "src/features/classroom-shell/classroom-orchestrator.ts"
      via: "picture-talk 成功后的短 praise 只经过一次 `teacher_feedback`，随后立即 `move_next`"
      pattern: "Nice answer\\.|teacher_feedback|move_next|picture-talk"
    - from: "test/e2e/guided-speaking-flow.spec.ts"
      to: "src/features/classroom-shell/classroom-judgment.ts"
      via: "picture-talk success 的快速预检仍经过 judged path"
      pattern: "Nice answer\\.|I answered|picture-talk|semanticAccepts|judgeStudentAttempt"
---

<objective>
消费共享 hold 合同，完成 classroom closeout surface、自动回首页与 full-loop 网页验证。

Purpose: 这是 Phase 5 的课堂闭环计划。完成后，用户可以在网页里完成“进教室 -> 上完课 -> 下课 -> 自动回首页”的完整 lesson，不需要额外结果页。
Output: teacher/podium/shell closeout surface、lesson auto-return wiring、focused smoke + full-loop Playwright regression。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-complete-mvp-lesson/05-CONTEXT.md
@.planning/phases/05-complete-mvp-lesson/05-RESEARCH.md
@.planning/phases/05-complete-mvp-lesson/05-VALIDATION.md
@.planning/phases/05-complete-mvp-lesson/05-01-PLAN.md
@.planning/phases/05-complete-mvp-lesson/05-02-PLAN.md
@src/app/lesson/[sessionId]/page.tsx
@src/features/classroom-shell/classroom-orchestrator.ts
@src/features/classroom-shell/use-classroom-orchestrator.ts
@src/features/classroom-shell/teacher-script.ts
@src/features/classroom-shell/podium-view-model.ts
@src/features/classroom-shell/classroom-shell.tsx
@test/unit/classroom-orchestrator.test.ts
@test/unit/teacher-script.test.ts
@test/unit/classroom-shell.test.tsx
@test/e2e/classroom-entry.spec.ts
@test/e2e/guided-speaking-flow.spec.ts

<interfaces>
From src/app/lesson/[sessionId]/page.tsx:
```ts
export default async function LessonPage({ params, searchParams }: LessonPageProps) {
  const { sessionId } = await params;
  return <ClassroomShell sessionId={sessionId} ... />;
}
```

From src/features/classroom-shell/classroom-orchestrator.ts:
```ts
export const LESSON_COMPLETE_HOLD_MS = 3000;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 完成 teacher/podium/shell closeout surface 与自动回首页接线</name>
  <files>src/features/classroom-shell/use-classroom-orchestrator.ts, src/features/classroom-shell/teacher-script.ts, src/features/classroom-shell/podium-view-model.ts, src/features/classroom-shell/classroom-shell.tsx, src/app/lesson/[sessionId]/page.tsx, test/unit/classroom-orchestrator.test.ts, test/unit/teacher-script.test.ts, test/unit/classroom-shell.test.tsx</files>
  <read_first>
    .planning/phases/05-complete-mvp-lesson/05-CONTEXT.md
    .planning/phases/05-complete-mvp-lesson/05-RESEARCH.md
    .planning/phases/05-complete-mvp-lesson/05-01-PLAN.md
    src/features/classroom-shell/classroom-orchestrator.ts
    src/features/classroom-shell/use-classroom-orchestrator.ts
    src/features/classroom-shell/teacher-script.ts
    src/features/classroom-shell/podium-view-model.ts
    src/features/classroom-shell/classroom-shell.tsx
    src/app/lesson/[sessionId]/page.tsx
    test/unit/classroom-orchestrator.test.ts
    test/unit/teacher-script.test.ts
    test/unit/classroom-shell.test.tsx
  </read_first>
  <behavior>
    - Test 1: per D-10/D-11，`classroom.phase === 'lesson_complete'` 后，客户端只启动一个 `LESSON_COMPLETE_HOLD_MS` timer，再 `router.replace('/?completedSession=${sessionId}', { scroll: false })` 回首页。
    - Test 2: per D-08，看图题在 `picture-talk` 成功后只允许出现一句简短课堂式肯定 `Nice answer.`，并且同一条成功路径在当前 `teacher_feedback` 结束后立即进入 `move_next`；不得插入额外奖励、第二段 praise 或延长型 feedback block。
    - Test 3: per D-03/D-04，teacher/podium/shell 的 warmup、wrap-up、completion child-facing surface 真正落地，继续保持单 CTA 和无答案泄露。
  </behavior>
  <action>先补 `test/unit/classroom-orchestrator.test.ts`、`test/unit/teacher-script.test.ts`、`test/unit/classroom-shell.test.tsx` 的 RED 用例，再接通实现。`src/features/classroom-shell/use-classroom-orchestrator.ts`、`src/features/classroom-shell/teacher-script.ts`、`src/features/classroom-shell/podium-view-model.ts`、`src/features/classroom-shell/classroom-shell.tsx` 必须消费 05-01 已导出的状态与 `LESSON_COMPLETE_HOLD_MS` 合同。`teacher-script.ts` 中 `warmup` 的老师 `visibleCaption` 固定为 `Hello, class. Cora is here.`、`hintLabel` 为 `Class hello`；`wrap_up` 的老师 `visibleCaption` 固定为 `Great work today. Class is all done.`、`hintLabel` 为 `Class closing`；`lesson_complete` 的老师 `visibleCaption` 固定为 `You finished class. See you next time.`、`hintLabel` 为 `Class complete`。per D-08，`picture-talk` 成功题的 `teacher_feedback` 文案固定为一句短肯定 `Nice answer.`，同一路径在该 feedback 结束后必须直接进入 `move_next`；不要在 picture-talk success path 里插入 `GREAT JOB!`、第二段 praise、额外 celebration phase 或更长的 teacher_feedback。`use-classroom-orchestrator.ts` 中的 stage badge / prompt 也要固定为：`warmup -> badge: 'Class warmup', prompt: 'Say hello. Class is starting.'`；`wrap_up -> badge: 'Class closing', prompt: 'Cora is saying goodbye.'`；`completion_reward -> badge: 'Reward time', prompt: 'Celebrate this brave lesson.'`；`lesson_complete -> badge: 'Class complete', prompt: 'See you next time.'`。`podium-view-model.ts` 里对应阶段的 `podiumCaption` / `podiumStatus` 必须分别出现 `Class hello`、`Class closing`、`Class complete` 这三个字符串，并且 `showConfirmationButton` 只允许在 `student_wait` 或 `teacher_echo` 为 `true`。`src/features/classroom-shell/classroom-shell.tsx` 必须引入 `useRouter` 和 `useEffect`，只在 `classroom.phase === 'lesson_complete'` 时启动一个完成态停留 timer，再 `router.replace('/?completedSession=' + sessionId, { scroll: false })` 回首页；这个停留时长必须直接引用 `LESSON_COMPLETE_HOLD_MS`，不能再自写第二份 3000ms，并在 cleanup 里清掉 timer。不要在其他 phase 启动 redirect timer，也不要在 homepage 或 lesson page 再挂第二条定时链。`src/app/lesson/[sessionId]/page.tsx` 继续保留现有 `notFound()` 和 `ClassroomShell` 渲染合同，不新增 `results`、`summary`、`completed` 等中间路由。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-orchestrator.test.ts test/unit/teacher-script.test.ts test/unit/classroom-shell.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/classroom-shell/classroom-shell.tsx` 可 grep 到 `router.replace('/?completedSession=`，并且完成停留时长直接引用 `LESSON_COMPLETE_HOLD_MS`。
    - `src/features/classroom-shell/teacher-script.ts` 可 grep 到 `Hello, class. Cora is here.`、`Great work today. Class is all done.`、`You finished class. See you next time.`。
    - `src/features/classroom-shell/teacher-script.ts` 可 grep 到 `Nice answer.`，且 picture-talk success path 的测试可 grep 到 `move_next`。
    - `src/features/classroom-shell/podium-view-model.ts` 可 grep 到 `Class hello`、`Class closing`、`Class complete`。
    - `src/app/lesson/[sessionId]/page.tsx` 仍然直接渲染 `ClassroomShell`，没有新增结果页路由引用。
  </acceptance_criteria>
  <done>课堂完成态、自动回首页和短反馈 closeout surface 已经落地，但还未跑完整浏览器闭环。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 先跑快速 picture-talk smoke，再补 full-loop E2E 闭环门</name>
  <files>test/e2e/guided-speaking-flow.spec.ts, test/e2e/complete-mvp-lesson.spec.ts, test/e2e/classroom-entry.spec.ts</files>
  <read_first>
    .planning/phases/05-complete-mvp-lesson/05-CONTEXT.md
    .planning/phases/05-complete-mvp-lesson/05-RESEARCH.md
    src/features/classroom-shell/classroom-orchestrator.ts
    src/features/classroom-shell/classroom-shell.tsx
    test/e2e/guided-speaking-flow.spec.ts
    test/e2e/classroom-entry.spec.ts
  </read_first>
  <behavior>
    - Test 1: quick smoke 必须先断言 picture-talk 成功后出现 `Nice answer.`，并且紧接着进入下一题而不是停留在长反馈。
    - Test 2: per PLAT-02，完整浏览器流程是 `首页 -> lesson -> 五次 I said it -> 五次 I answered -> reward -> lesson_complete -> 首页 recently completed`，没有单独结果页或中途跳转。
    - Test 3: per SPKG-02，完整闭环 E2E 必须经过 picture-talk 主段，且 picture-talk 仍保持老师提问、孩子回答、语义相近可过的现有 judged 合同，不允许跳过后半程。
  </behavior>
  <action>先扩展 `test/e2e/guided-speaking-flow.spec.ts`，作为快速预检门，明确断言 picture-talk success 后出现 `Nice answer.`，并且紧接着进入下一题而不是长反馈。再新增 `test/e2e/complete-mvp-lesson.spec.ts`，断言完整闭环：课堂里只在结课 reward 阶段出现 `GREAT JOB!`，随后出现 `Class complete` 与 `See you next time.`，然后 URL 命中 `/\\?completedSession=weekday-1700/`，最后首页显示 `刚完成这节课` 与 `刚完成`。E2E 中必须实际点击 5 次 `I said it` 和 5 次 `I answered`，并保留对 picture-talk judged path 的保护：`I answered` 不能绕开 `stageId: 'picture-talk'`、`pictureTalk.semanticAccepts` 和 `judgeStudentAttempt()` 的现有合同。不要通过 query 参数绕过完整 lesson。保留 `test/e2e/classroom-entry.spec.ts` 作为最终合并门的一部分，但 task-level 快速预检只先跑 `guided-speaking-flow.spec.ts`。</action>
  <verify>
    <automated>npm run test:e2e -- test/e2e/guided-speaking-flow.spec.ts && npm run test:e2e -- test/e2e/classroom-entry.spec.ts test/e2e/complete-mvp-lesson.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test/e2e/guided-speaking-flow.spec.ts` 可 grep 到 `Nice answer.`、`picture-talk`、`I answered`。
    - `test/e2e/complete-mvp-lesson.spec.ts` 文件存在，且可 grep 到 `GREAT JOB!`、`Class complete`、`刚完成这节课`、`刚完成`。
    - `test/e2e/complete-mvp-lesson.spec.ts` 可 grep 到 `I answered`、`picture-talk`、`semanticAccepts` 或 `judgeStudentAttempt` 这类 semantic-pass 保护关键词。
  </acceptance_criteria>
  <done>Phase 5 既有快速 picture-talk success smoke，也有首页到课堂再回首页的 full-loop E2E 门。</done>
</task>

</tasks>

<verification>
Phase 5 的网页闭环必须由一个 completion owner 串起来。若 redirect timer 同时出现在 classroom shell、lesson route 和 homepage，recently completed 污染了真实 access state，picture-talk 成功后没有保持 `Nice answer.` + 立即 `move_next` 的短反馈合同，或者 full-loop E2E 跳过了 picture-talk judged 主段，执行即视为失败。
</verification>

<success_criteria>
- lesson 在 `lesson_complete` 后约 3 秒自动回首页，不出现额外结果页。
- 首页对刚完成场次显示 `刚完成这节课` / `刚完成`，同时真实 `accessState` 保持不变。
- homepage -> lesson -> complete -> homepage 的 Playwright 闭环回归可自动通过，并且 picture-talk 的 semantic-near-enough pass 仍由既有 judged path 保底。
- picture-talk 成功题只出现一句短肯定 `Nice answer.`，随后立即切到下一题，不把每题成功做成显式奖励节点。
</success_criteria>

<output>
After completion, create `.planning/phases/05-complete-mvp-lesson/05-03-SUMMARY.md`
</output>
