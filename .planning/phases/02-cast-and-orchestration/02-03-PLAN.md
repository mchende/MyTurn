---
phase: 02-cast-and-orchestration
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/features/classroom-shell/bobby-script.ts
  - src/features/classroom-shell/podium-view-model.ts
  - src/features/classroom-shell/use-classroom-orchestrator.ts
  - src/features/classroom-shell/classroom-shell.tsx
  - src/features/classroom-shell/student-seat-strip.tsx
  - test/unit/bobby-script.test.ts
  - test/unit/classroom-shell.test.tsx
  - test/e2e/classroom-entry.spec.ts
autonomous: true
requirements:
  - TEAC-01
  - CLAS-02
  - CLAS-04
  - AICL-01
  - AICL-02
must_haves:
  truths:
    - "用户持续看到固定三层结构：老师固定区、顶部席位、下方讲台区。"
    - "老师脚本真实接入课堂壳体，用户在每个轮次都能看到/听到与当前 phase 对应的带班过渡话术。"
    - "Bobby 在每个目标项中都先于真实孩子出场示范，给孩子准备空间。"
    - "Bobby 的表现有轻微停顿/犹豫但整体不抢戏；孩子沉默时仍由老师而不是 Bobby 收场。"
  artifacts:
    - path: "src/features/classroom-shell/bobby-script.ts"
      provides: "Bobby persona contract 与不完美 envelope"
      exports:
        - "getBobbyScriptLine"
        - "BOBBY_RESPONSE_ENVELOPE"
    - path: "src/features/classroom-shell/podium-view-model.ts"
      provides: "seat/podium/reward 的派生展示数据"
    - path: "src/features/classroom-shell/classroom-shell.tsx"
      provides: "接入 orchestrator、teacher-script、bobby-script 的课堂页面"
    - path: "test/unit/classroom-shell.test.tsx"
      provides: "席位保留、Bobby 前置示范、奖励 gate 的 UI 回归"
  key_links:
    - from: "src/features/classroom-shell/classroom-shell.tsx"
      to: "src/features/classroom-shell/teacher-script.ts"
      via: "phase-driven teacher line wiring into shell copy"
      pattern: "getTeacherScriptLine|teacherScriptLine|spokenLine"
    - from: "src/features/classroom-shell/use-classroom-orchestrator.ts"
      to: "src/features/classroom-shell/bobby-script.ts"
      via: "phase-driven Bobby line selection"
      pattern: "getBobbyScriptLine"
    - from: "src/features/classroom-shell/classroom-shell.tsx"
      to: "src/features/classroom-shell/podium-view-model.ts"
      via: "seat strip + podium caption wiring"
      pattern: "buildPodiumViewModel|podiumViewModel"
    - from: "test/e2e/classroom-entry.spec.ts"
      to: "src/app/lesson/[sessionId]/page.tsx"
      via: "homepage to lesson smoke"
      pattern: "/lesson/"
---

<objective>
把老师脚本、Bobby 人设、讲台/席位派生视图和 Phase 2 shell wiring 一起接到新的 orchestrator 上，并恢复自动化验证闭环。

Purpose: 这是把“固定三人小班结构”“老师持续带班过渡”和“先看 Bobby、再轮到我”真正变成用户可见课堂体验的收口计划。
Output: Bobby script 模块、讲台 view-model、接入 teacher-script 的 classroom shell、UI/E2E 回归测试。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-cast-and-orchestration/02-CONTEXT.md
@.planning/phases/02-cast-and-orchestration/02-RESEARCH.md
@.planning/phases/02-cast-and-orchestration/02-01-PLAN.md
@.planning/phases/02-cast-and-orchestration/02-02-PLAN.md
@src/features/classroom-shell/classroom-shell.tsx
@src/features/classroom-shell/student-seat-strip.tsx
@src/features/classroom-shell/use-classroom-orchestrator.ts
@test/unit/classroom-shell.test.tsx
@test/e2e/classroom-entry.spec.ts
@UI/课堂页.png
@UI/ai_studio_code_课堂页.html

<interfaces>
From 02-02 contract:
```ts
type TeacherScriptLine = {
  spokenLine: string;
  hintLabel: string;
  debugTargetText?: string;
};
```

From src/features/classroom-shell/student-seat-strip.tsx:
```tsx
type StudentSeatStripProps = {
  activeSeat: 'me' | 'ai' | null;
  sessionId: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 Bobby persona 合同与非救场护栏</name>
  <files>src/features/classroom-shell/bobby-script.ts, test/unit/bobby-script.test.ts</files>
  <read_first>
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    .planning/phases/02-cast-and-orchestration/02-CONTEXT.md
    .planning/phases/02-cast-and-orchestration/02-RESEARCH.md
    src/features/classroom-shell/use-classroom-orchestrator.ts
  </read_first>
  <behavior>
    - Test 1: per D-13/D-14/D-15，Bobby 回答包含轻微停顿/犹豫 envelope，但输出仍完整、节奏受控。
    - Test 2: per D-06/D-18，Bobby 只允许出现在 `ai_model` 或等价示范 phase，不允许出现在孩子沉默补救路径。
  </behavior>
  <action>创建 `bobby-script.ts`，输出 Bobby 的示范文案、停顿参数和 persona metadata；实现轻微不完美而非破碎输出，例如通过固定的短停顿、语气词或轻微 hesitancy token 表达，而不是故意说错内容。测试里明确禁止在 `student_wait`、`teacher_encourage`、`teacher_echo` 等沉默处理阶段调用 Bobby script。若 UI 参考没有更细的语气展示形式，仅在数据层表达 envelope，不新增未被批准的语音可视化控件。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/bobby-script.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/classroom-shell/bobby-script.ts` 导出 persona API 和不完美 envelope 常量。
    - `test/unit/bobby-script.test.ts` 包含“Bobby not rescue student silence”之类的断言。
    - grep `hesitation|pause|beat` 可在 Bobby script 或测试中找到至少一种轻微不完美实现字段。
  </acceptance_criteria>
  <done>Bobby 的“可信同龄同学”行为被独立建模，且不会越权接管老师控场。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接线 classroom shell、席位/讲台 view-model 与单测</name>
  <files>src/features/classroom-shell/podium-view-model.ts, src/features/classroom-shell/use-classroom-orchestrator.ts, src/features/classroom-shell/classroom-shell.tsx, src/features/classroom-shell/student-seat-strip.tsx, test/unit/classroom-shell.test.tsx</files>
  <read_first>
    src/features/classroom-shell/classroom-shell.tsx
    src/features/classroom-shell/student-seat-strip.tsx
    src/features/classroom-shell/use-classroom-orchestrator.ts
    src/features/classroom-shell/bobby-script.ts
    src/features/classroom-shell/teacher-script.ts
    src/features/classroom-shell/lesson-board.tsx
    test/unit/classroom-shell.test.tsx
    UI/课堂页.png
    UI/ai_studio_code_课堂页.html
    .planning/phases/02-cast-and-orchestration/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: per D-01/D-02/D-03，顶部席位长期保留“我 + Bobby + 空位”，有人上讲台时顶部原席位仍保留并显示半透明/讲台中状态。
    - Test 2: per D-04/D-05/D-06，Bobby 在每个目标项都固定先示范，随后才轮到“我”上讲台。
    - Test 3: per D-07/D-08/D-09/TEAC-01，`classroom-shell` 在 `teacher_prompt`、`teacher_encourage`、`teacher_feedback` 等 phase 接入对应 teacher script line，而不是回退成通用占位提示。
    - Test 4: per D-21/D-22，只有显式 participation signal 时才出现强奖励；普通收束不机械展示庆祝层。
  </behavior>
  <action>新增 `podium-view-model.ts` 统一计算 seat strip、讲台 caption、当前 speaker、奖励确认文案；然后在 `classroom-shell.tsx` 中将旧 `useClassroomFlow` 切换为 `useClassroomOrchestrator`，接入 `teacher-script` 与 `bobby-script` 输出，让 shell 的 teacher copy 明确消费 `getTeacherScriptLine` 一类接口来落实 TEAC-01。更新 `student-seat-strip.tsx` 保留 D-02 的固定三席结构与 D-03 的“原席位不消失”规则。讲台和文案必须避免暴露 child-facing target text，只允许调试字段默认关闭地存在。严格保持现有 Gemini 课堂页布局，不发明新的 side panel、floating controls 或替代视觉体系；若某个 AI 同学微交互缺少设计细节，fallback 仅允许复用已批准的 shell transitions 与现有 Motion 进退场节奏，不新增新的可见交互 affordance、动画 motif 或装饰性提示。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-shell.test.tsx test/unit/classroom-orchestrator.test.ts test/unit/teacher-script.test.ts test/unit/bobby-script.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/classroom-shell/classroom-shell.tsx` 不再 import `useClassroomFlow`，改为 import `useClassroomOrchestrator`。
    - `test/unit/classroom-shell.test.tsx` 覆盖 teacher script line 已接线到 shell 的 phase-driven 文案断言。
    - `src/features/classroom-shell/student-seat-strip.tsx` 仍保留 3 个固定席位，其中空位为长期存在的班级结构。
    - Bobby fallback 若缺少外部 UI 细节，仅复用现有 shell transitions，不新增新按钮、新悬浮提示或新动画主题。
    - `test/unit/classroom-shell.test.tsx` 覆盖 Bobby 先示范、seat 保留、teacher wiring、默认无强奖励四类断言。
  </acceptance_criteria>
  <done>Phase 2 的课堂壳体真正表现出固定小班结构、Bobby 前置示范和非机械奖励节奏。</done>
</task>

<task type="auto">
  <name>Task 3: 恢复浏览器 smoke 验证并锁定 Phase 2 回归</name>
  <files>test/e2e/classroom-entry.spec.ts</files>
  <read_first>
    test/e2e/classroom-entry.spec.ts
    src/app/lesson/[sessionId]/page.tsx
    src/features/classroom-shell/classroom-shell.tsx
    playwright.config.ts
    .planning/phases/02-cast-and-orchestration/02-VALIDATION.md
  </read_first>
  <action>更新 `test/e2e/classroom-entry.spec.ts`，在保留 Phase 1 首页进课链路的前提下，增加至少一个 Phase 2 可见断言：例如进入课堂后能看到固定席位结构、Cora 控场文案、Bobby 前置示范或默认无答案文本泄露。不要把验证写成脆弱的动画帧断言，重点锁定课堂编排结果和关键可见文案；如 Bobby 细微停顿的表现难以稳定断言，只验证其沿用既有 shell transitions 的可见结果，不新增新的视觉交互模式。</action>
  <verify>
    <automated>npm run test:unit && npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `test/e2e/classroom-entry.spec.ts` 仍覆盖 `我的课堂 -> 进入课堂 -> /lesson/[sessionId]` 基本链路。
    - E2E 新增至少一条 Phase 2 行为断言，且不是纯截图比较。
    - 全量命令 `npm run test:unit && npm run test:e2e` 通过。
  </acceptance_criteria>
  <done>Phase 2 完成后，浏览器入口和课堂编排行为都有自动化回归保护。</done>
</task>

</tasks>

<verification>
计划完成后应先跑全量 unit，再跑 Playwright smoke。若 E2E 因动画时序波动失败，应回到可见行为断言收紧选择器，而不是放宽到无意义的存在性检查。
</verification>

<success_criteria>
- Bobby 行为具备独立 persona contract，并严格受老师主流程约束。
- 老师脚本在 shell 中完成 phase-driven wiring，使 TEAC-01 不只停留在数据模块层。
- 课堂页可见固定三层角色结构、Bobby 前置示范和默认关闭的强奖励 gate。
- `npm run test:unit && npm run test:e2e` 成为 Phase 2 的执行完成门槛。
</success_criteria>

<output>
After completion, create `.planning/phases/02-cast-and-orchestration/02-03-SUMMARY.md`
</output>
