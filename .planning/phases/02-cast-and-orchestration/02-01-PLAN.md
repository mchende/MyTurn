---
phase: 02-cast-and-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/classroom-shell/classroom-orchestrator.ts
  - src/features/classroom-shell/use-classroom-orchestrator.ts
  - test/unit/classroom-orchestrator.test.ts
autonomous: true
requirements:
  - CLAS-02
  - CLAS-03
must_haves:
  truths:
    - "课堂状态明确知道当前是谁在带班、谁在讲台、当前处于哪一个固定轮次。"
    - "每个目标项都按老师引导 -> Bobby 示范 -> 我上台 -> 老师反馈/沉默接住 -> 收束推进的确定性顺序运行。"
    - "沉默处理由老师主导，不存在孩子沉默后 Bobby 接管的分支。"
  artifacts:
    - path: "src/features/classroom-shell/classroom-orchestrator.ts"
      provides: "纯 reducer、事件、时序常量与初始状态工厂"
      exports:
        - "classroomOrchestratorReducer"
        - "createInitialClassroomState"
        - "CLASSROOM_TIMINGS"
    - path: "src/features/classroom-shell/use-classroom-orchestrator.ts"
      provides: "薄 scheduler hook，把 timer 推进与 reducer 隔离"
      exports:
        - "useClassroomOrchestrator"
    - path: "test/unit/classroom-orchestrator.test.ts"
      provides: "轮次推进、沉默处理、奖励 gate 的回归测试"
  key_links:
    - from: "src/features/classroom-shell/use-classroom-orchestrator.ts"
      to: "src/features/classroom-shell/classroom-orchestrator.ts"
      via: "useReducer + timer dispatch"
      pattern: "useReducer|dispatch"
    - from: "test/unit/classroom-orchestrator.test.ts"
      to: "src/features/classroom-shell/classroom-orchestrator.ts"
      via: "direct reducer assertions"
      pattern: "classroomOrchestratorReducer|createInitialClassroomState"
---

<objective>
建立 Phase 2 的课堂编排核心合同：把当前线性自动轮播升级成可测试、可扩展的 reducer + scheduler 结构。

Purpose: 先把“老师主导的一次点一个人作答”做成稳定规则，后续老师脚本、Bobby 行为和 UI 联动才能在同一套状态图上实现。
Output: 新的 orchestrator reducer、hook 与针对轮转/沉默分支的单测。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cast-and-orchestration/02-CONTEXT.md
@.planning/phases/02-cast-and-orchestration/02-RESEARCH.md
@.planning/phases/02-cast-and-orchestration/02-VALIDATION.md
@.planning/phases/01-classroom-shell/01-04-SUMMARY.md
@src/features/classroom-shell/use-classroom-flow.ts
@src/features/lesson-config/lesson-schema.ts
@content/lessons/week-01/lesson-01.ts

<interfaces>
From src/features/lesson-config/lesson-schema.ts:
```ts
export type LessonItem = z.infer<typeof lessonItemSchema>;
export type Lesson = z.infer<typeof lessonSchema>;
```

From src/features/classroom-shell/use-classroom-flow.ts:
```ts
export type ClassroomFlowPhase =
  | 'teacher_prompt'
  | 'ai_model'
  | 'student_turn'
  | 'teacher_feedback'
  | 'celebration'
  | 'wrap_up';

export const CLASSROOM_FLOW_DURATIONS: Record<Exclude<ClassroomFlowPhase, 'wrap_up'>, number>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 orchestrator contracts 与 Wave 0 单测</name>
  <files>src/features/classroom-shell/classroom-orchestrator.ts, test/unit/classroom-orchestrator.test.ts</files>
  <read_first>
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/phases/02-cast-and-orchestration/02-CONTEXT.md
    .planning/phases/02-cast-and-orchestration/02-RESEARCH.md
    .planning/phases/02-cast-and-orchestration/02-VALIDATION.md
    src/features/classroom-shell/use-classroom-flow.ts
    src/features/lesson-config/lesson-schema.ts
    content/lessons/week-01/lesson-01.ts
  </read_first>
  <behavior>
    - Test 1: per D-04/D-05，单个 item 从 `teacher_prompt` 固定推进到 `ai_model` 再到 `student_turn`，当前讲台 speaker 始终唯一。
    - Test 2: per D-17/D-18/D-19，孩子沉默时按 `short_wait -> teacher_encourage -> teacher_echo -> move_next` 推进，过程中不得产生 Bobby rescue phase。
    - Test 3: per D-21/D-22，强奖励不是固定必经 phase，只能由显式 participation signal 打开。
  </behavior>
  <action>新建 `classroom-orchestrator.ts`，定义 Phase 2 使用的明确状态与事件：至少包含 `teacher_prompt`、`ai_model`、`student_wait`、`teacher_encourage`、`teacher_echo`、`teacher_feedback`、`move_next`、`wrap_up`，并新增 `CLASSROOM_TIMINGS` 常量表来集中管理等待/鼓励/带读/收束时长。先在 `test/unit/classroom-orchestrator.test.ts` 写失败测试，覆盖 D-04、D-05、D-17、D-18、D-21；不要沿用当前“每题固定 celebration”旧模型。若现有 UI 参考无法提供更细的微交互时长，按 02-RESEARCH.md 的建议把时序收敛为常量表，并在测试中断言事件顺序而不是断言动画细节。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test/unit/classroom-orchestrator.test.ts` 存在，并包含 `teacher_prompt`、`student_wait`、`teacher_echo`、`move_next` 等阶段断言。
    - `src/features/classroom-shell/classroom-orchestrator.ts` 导出 `CLASSROOM_TIMINGS`、`createInitialClassroomState`、`classroomOrchestratorReducer`。
    - grep `student_silent_timeout|teacher_echo_complete` 可在 orchestrator 或测试文件中找到对应事件名。
  </acceptance_criteria>
  <done>Wave 0 测试先定义清楚课堂轮转和沉默分支，执行器无需再猜测 Phase 2 的状态图。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 reducer 与薄 scheduler hook</name>
  <files>src/features/classroom-shell/classroom-orchestrator.ts, src/features/classroom-shell/use-classroom-orchestrator.ts</files>
  <read_first>
    src/features/classroom-shell/classroom-orchestrator.ts
    test/unit/classroom-orchestrator.test.ts
    src/features/classroom-shell/use-classroom-flow.ts
    src/features/lesson-config/lesson-schema.ts
    .planning/phases/02-cast-and-orchestration/02-CONTEXT.md
    .planning/phases/02-cast-and-orchestration/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: hook 初始化后以 item 0 和 `teacher_prompt` 启动，并暴露当前 speaker、itemIndex、reward gate、debug 可见性等派生状态。
    - Test 2: timer effect 仅负责 dispatch 下一事件，不在组件层拼接多段裸 `setTimeout`。
  </behavior>
  <action>实现 `classroomOrchestratorReducer` 和 `useClassroomOrchestrator`：用 `useReducer` 驱动状态，用单一 effect 根据当前 phase 查 `CLASSROOM_TIMINGS` 后 dispatch 下一事件。Hook 必须返回一个稳定的 view-model 基础结构，至少包含 `phase`、`currentItem`、`currentItemIndex`、`activeSeat`、`activeSpeaker`、`rewardVisible`、`participationState`、`debugTargetText`。保留 `showReward` 之类的显式覆盖入口以兼容现有 lesson route smoke，但不要再把它做成主流程默认分支；per D-16/D-17，沉默后由老师推进到下一环，不能卡死在当前题。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/classroom-shell/use-classroom-orchestrator.ts` 包含 `useReducer(`，且仅有一个集中调度 timer effect。
    - `src/features/classroom-shell/classroom-orchestrator.ts` 中不存在固定 `celebration` 必经序列数组。
    - 运行 `npm run test:unit -- test/unit/classroom-orchestrator.test.ts` 通过。
  </acceptance_criteria>
  <done>Phase 2 已有可复用的编排核心，后续脚本和 UI 只需消费 hook 提供的派生状态。</done>
</task>

</tasks>

<verification>
优先验证 reducer 本身，而不是先改 UI。计划完成后，`test/unit/classroom-orchestrator.test.ts` 应成为 Phase 2 编排规则的第一道回归护栏。
</verification>

<success_criteria>
- Phase 2 的固定轮转和沉默处理被 reducer 明确定义，不再散落在组件中的线性定时器里。
- 强奖励从“固定 phase”改为“显式 gate”，为 D-21/D-22 留出实现空间。
- 后续计划可以直接依赖 `useClassroomOrchestrator` 的合同，不需要重新定义状态字段。
</success_criteria>

<output>
After completion, create `.planning/phases/02-cast-and-orchestration/02-01-SUMMARY.md`
</output>
