---
phase: 02-cast-and-orchestration
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/features/classroom-shell/teacher-script.ts
  - src/features/classroom-shell/lesson-board.tsx
  - test/unit/teacher-script.test.ts
  - test/unit/lesson-board.test.tsx
autonomous: true
requirements:
  - TEAC-01
  - CLAS-03
must_haves:
  truths:
    - "老师在每个轮次都输出明显的带班话术，而不是只显示通用节奏提示。"
    - "儿童主视图不会直接看到目标词句文本。"
    - "老师点名、过渡、鼓励与收束都保持英文主流程。"
  artifacts:
    - path: "src/features/classroom-shell/teacher-script.ts"
      provides: "老师话术模板池与 phase -> line 生成逻辑"
      exports:
        - "getTeacherScriptLine"
        - "getTeacherHint"
    - path: "src/features/classroom-shell/lesson-board.tsx"
      provides: "移除 child-facing target text 的课堂主屏"
    - path: "test/unit/lesson-board.test.tsx"
      provides: "LessonBoard 默认不泄露 target text、仅 debug guard 可见的渲染回归"
    - path: "test/unit/teacher-script.test.ts"
      provides: "英文话术和 child-facing leakage 的回归测试"
  key_links:
    - from: "src/features/classroom-shell/teacher-script.ts"
      to: "src/features/classroom-shell/use-classroom-orchestrator.ts"
      via: "phase-driven script selection"
      pattern: "getTeacherScriptLine"
    - from: "src/features/classroom-shell/lesson-board.tsx"
      to: "src/features/classroom-shell/classroom-shell.tsx"
      via: "board props without child-facing target text"
      pattern: "debugTargetText|stagePrompt"
    - from: "test/unit/lesson-board.test.tsx"
      to: "src/features/classroom-shell/lesson-board.tsx"
      via: "render assertions for default board output"
      pattern: "queryByText|debugTargetText|showDebugTarget"
---

<objective>
补齐老师主导的英文话术合同，并修正当前课堂主屏直接泄露目标词句的问题。

Purpose: Phase 2 的课堂感不只来自轮次顺序，还来自 Cora 持续带班；同时必须落实 D-10/D-11/D-12，确保孩子不是看着答案开口。
Output: 可复用的老师脚本模块、老师与 LessonBoard 的针对性单测，以及不再直接显示目标词句的 LessonBoard。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-cast-and-orchestration/02-CONTEXT.md
@.planning/phases/02-cast-and-orchestration/02-RESEARCH.md
@.planning/phases/02-cast-and-orchestration/02-01-PLAN.md
@src/features/classroom-shell/lesson-board.tsx
@src/features/classroom-shell/use-classroom-orchestrator.ts

<interfaces>
From 02-01 contract:
```ts
type ClassroomPhase =
  | 'teacher_prompt'
  | 'ai_model'
  | 'student_wait'
  | 'teacher_encourage'
  | 'teacher_echo'
  | 'teacher_feedback'
  | 'move_next'
  | 'wrap_up';
```

From src/features/classroom-shell/lesson-board.tsx:
```tsx
type LessonBoardProps = {
  currentItem: { imageAlt: string; imageSrc: string; text: string };
  currentItemIndex: number;
  progressCount: number;
  stageBadge: string;
  stagePrompt: string;
  sessionStatus: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立老师话术模块与泄露防护测试</name>
  <files>src/features/classroom-shell/teacher-script.ts, test/unit/teacher-script.test.ts</files>
  <read_first>
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    .planning/phases/02-cast-and-orchestration/02-CONTEXT.md
    .planning/phases/02-cast-and-orchestration/02-RESEARCH.md
    src/features/classroom-shell/use-classroom-orchestrator.ts
    src/features/classroom-shell/lesson-board.tsx
  </read_first>
  <behavior>
    - Test 1: per D-07/D-08/D-09，老师对 `teacher_prompt`、`teacher_encourage`、`teacher_echo`、`teacher_feedback`、`move_next` 均能产出英文主流程话术。
    - Test 2: per D-10/D-11/D-12，child-facing script metadata 不直接返回目标词句，只允许 debug/teacher-only 字段携带目标文本。
  </behavior>
  <action>创建 `teacher-script.ts`，用短句、强引导型英文模板实现 Cora 的话术池，每个关键 phase 至少提供 2-3 个可轮换变体，但句长保持儿童启蒙课堂节奏。脚本输出分为 `spokenLine`、`hintLabel`、`debugTargetText` 之类的明确字段，避免把目标词句混进 child-facing copy。若当前 UI 参考没有更细的按钮/面板交互说明，保持脚本为纯数据模块，不擅自发明新面板或新 controls。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/teacher-script.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/classroom-shell/teacher-script.ts` 导出 `getTeacherScriptLine` 或等价命名的 phase->script API。
    - `test/unit/teacher-script.test.ts` 断言 child-facing 文本不包含 lesson item 的 `text` 字段，debug 字段才允许包含。
    - grep `Good job|Let's say it together|Next one` 能在老师脚本或测试中找到至少一组英文控场模板。
  </acceptance_criteria>
  <done>执行器可以稳定生成老师英文带班文案，并且 child-facing 文本泄露规则有自动化护栏。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 调整 LessonBoard 为儿童安全主视图</name>
  <files>src/features/classroom-shell/lesson-board.tsx, test/unit/lesson-board.test.tsx</files>
  <read_first>
    src/features/classroom-shell/lesson-board.tsx
    src/features/classroom-shell/teacher-script.ts
    .planning/phases/02-cast-and-orchestration/02-CONTEXT.md
    UI/课堂页.png
    UI/ai_studio_code_课堂页.html
  </read_first>
  <behavior>
    - Test 1: 默认渲染 LessonBoard 时，看得到图片、进度、阶段提示，但看不到 `currentItem.text` 或等价 target text。
    - Test 2: 仅当显式传入 `showDebugTarget` 与 `debugTargetText` 一类 debug-only props 时，测试态才允许看到目标词句；默认路径不得泄露。
  </behavior>
  <action>先新增 `test/unit/lesson-board.test.tsx`，用 render 断言锁住默认 no-leak 路径，再修改 `LessonBoard`，移除当前底部直接展示 `currentItem.text` 的 child-facing 大字文案，保留图片、进度、阶段提示和状态标签；如需保留目标词句，只能通过默认关闭的 `debugTargetText` / `showDebugTarget` 之类的可选 props 暴露，默认值必须让正常课堂画面不显示答案。严格沿用现有页面壳体和 Gemini 参考布局，不新增替代设计系统；若某个 debug 入口位置在现有参考里没有依据，只做代码层可选 prop，不在默认 UI 中发明新按钮。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/lesson-board.test.tsx test/unit/teacher-script.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/classroom-shell/lesson-board.tsx` 默认渲染路径中不再直接输出 `currentItem.text`。
    - `test/unit/lesson-board.test.tsx` 明确覆盖“默认不泄露、debug guard 才允许显示”的渲染断言。
    - 文件中存在显式 debug-only prop 或 guard，且默认值为关闭状态。
    - 现有 `stageBadge`、`stagePrompt`、进度 dots、图片容器仍保留在组件中。
  </acceptance_criteria>
  <done>课堂主屏只通过图片和课堂提示引导孩子，不再直接把答案写在屏幕上。</done>
</task>

</tasks>

<verification>
这一计划完成后，`teacher-script` 与 `lesson-board` 单测必须分别证明老师主流程英文化、以及 LessonBoard 默认 child-facing 不泄露目标词句；UI 变化保持在现有课堂壳体内完成。
</verification>

<success_criteria>
- Cora 的控场话术拥有明确脚本模块，而不是散落在 UI 组件内。
- LessonBoard 默认视图不直接显示目标词句，且存在 focused render test 保护 D-10/D-11/D-12。
- 后续 Bobby / shell wiring 计划可以直接消费老师脚本输出字段。
</success_criteria>

<output>
After completion, create `.planning/phases/02-cast-and-orchestration/02-02-SUMMARY.md`
</output>
