---
phase: 07-speech-recognition-wiring
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01-PLAN.md
  - 07-02-PLAN.md
files_modified:
  - src/features/classroom-shell/use-classroom-audio-runtime.ts
  - src/features/classroom-shell/classroom-shell.tsx
  - src/features/classroom-shell/podium-view-model.ts
  - test/unit/classroom-shell.test.tsx
  - test/unit/classroom-orchestrator.test.ts
  - test/unit/classroom-transcript-adapter.test.ts
  - test/e2e/helpers/fake-browser-audio.ts
  - test/e2e/audio-classroom-runtime.spec.ts
autonomous: true
requirements:
  - ASR-01
  - ASR-03
  - ASR-04
  - PLAT-05
must_haves:
  truths:
    - "07-03 在 repeat 基线之上接通 picture-talk，不重做 transcript adapter 或录音 CTA 主交互。"
    - "picture-talk 的宽松来自既有 `semanticAccepts` judgment，而不是 transcript adapter 的强改写。"
    - "识别失败、低质量或 timeout 时，picture 路径仍然回到老师主导的 retry/fallback。"
    - "focused regression 必须覆盖 preflight -> repeat recognition -> picture recognition 的网页闭环，但不依赖真实浏览器云识别。"
    - "dev-only HUD 可以保留给开发者看，但正式课堂页不能出现技术指标。"
  artifacts:
    - path: "test/e2e/helpers/fake-browser-audio.ts"
      provides: "可受控的 fake recognition / fake transcript browser test adapter"
      exports:
        - "installFakeBrowserAudio"
    - path: "test/e2e/audio-classroom-runtime.spec.ts"
      provides: "preflight -> repeat transcript -> picture transcript 的 focused browser smoke"
      exports:
        - "audio classroom recognition smoke"
  key_links:
    - from: "src/features/classroom-shell/use-classroom-audio-runtime.ts"
      to: "src/features/classroom-shell/classroom-judgment.ts"
      via: "picture transcript 继续复用 semanticAccepts judgment"
      pattern: "picture-talk|semanticAccepts|future_asr"
    - from: "test/e2e/helpers/fake-browser-audio.ts"
      to: "test/e2e/audio-classroom-runtime.spec.ts"
      via: "受控注入 repeat / picture transcript success and failure"
      pattern: "transcript queue|timeout|empty|unavailable"
    - from: "src/features/classroom-shell/classroom-shell.tsx"
      to: "test/unit/classroom-shell.test.tsx"
      via: "picture waiting / fallback 文案与 dev HUD 隐藏规则"
      pattern: "podium-status|teacher-audio-status|NODE_ENV"
---

<objective>
接通 `picture-talk` 的真实 transcript 路径，并完成 Phase 7 的 focused regression：preflight、repeat recognition、picture recognition、等待/失败/成功率观测都要有可执行证据。

Purpose: 07-03 是 Phase 7 的收口计划，证明 transcript 接线不仅能跑 repeat，也能跑 picture，而且不会把课堂再次做回工具页。
Output: picture recognition 闭环、focused unit/e2e smoke、Phase 7 execute-ready regression gate。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-speech-recognition-wiring/07-CONTEXT.md
@.planning/phases/07-speech-recognition-wiring/07-RESEARCH.md
@.planning/phases/07-speech-recognition-wiring/07-VALIDATION.md
@.planning/phases/07-speech-recognition-wiring/07-01-PLAN.md
@.planning/phases/07-speech-recognition-wiring/07-02-PLAN.md
@src/features/classroom-shell/use-classroom-audio-runtime.ts
@src/features/classroom-shell/classroom-judgment.ts
@src/features/classroom-shell/classroom-shell.tsx
@test/unit/classroom-shell.test.tsx
@test/unit/classroom-orchestrator.test.ts
@test/e2e/helpers/fake-browser-audio.ts
@test/e2e/audio-classroom-runtime.spec.ts

<interfaces>
Expected picture recognition browser contract:
```ts
type FakeRecognitionResult = {
  transcript: string | null;
  reason?: 'timeout' | 'empty' | 'unavailable' | 'error';
};
```

Expected picture wait UI contract:
```ts
type PictureRecognitionUiState = {
  podiumStatus: string;
  teacherStatus: string;
  transcriptStatus: 'waiting' | 'resolved' | 'failed';
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把 picture-talk transcript 接进既有 semantic judgment 与老师主导兜底</name>
  <files>src/features/classroom-shell/use-classroom-audio-runtime.ts, src/features/classroom-shell/classroom-shell.tsx, src/features/classroom-shell/podium-view-model.ts, test/unit/classroom-shell.test.tsx, test/unit/classroom-orchestrator.test.ts, test/unit/classroom-transcript-adapter.test.ts</files>
  <read_first>
    .planning/phases/07-speech-recognition-wiring/07-02-PLAN.md
    src/features/classroom-shell/classroom-judgment.ts
    src/features/classroom-shell/use-classroom-audio-runtime.ts
    src/features/classroom-shell/classroom-shell.tsx
    test/unit/classroom-orchestrator.test.ts
  </read_first>
  <behavior>
    - Test 1: 在 `picture-talk`，孩子第二次点 CTA 结束录音后会进入 transcript wait，并通过 `submitStudentAttempt({ source: 'future_asr' })` 走既有 `semanticAccepts` judgment。
    - Test 2: picture transcript 命中语义接受集合时，课堂进入既有简短 `teacher_feedback`，不会要求关键词完全一致。
    - Test 3: picture transcript timeout / empty / unavailable / low-quality 时，仍然走现有 `teacher_encourage -> second try -> teacher_fallback_model -> teacher_echo` 路径。
    - Test 4: adapter 不会为了 picture 阶段强改写 transcript；picture 的宽松判断仍然来自 `semanticAccepts`。
  </behavior>
  <action>先扩 `test/unit/classroom-orchestrator.test.ts`、`test/unit/classroom-shell.test.tsx` 和 `test/unit/classroom-transcript-adapter.test.ts` 写 RED。然后修改 `src/features/classroom-shell/use-classroom-audio-runtime.ts`，让 `picture-talk` 在 stop recording 后与 repeat 一样进入 recognition wait，但提交 transcript 时继续只走 `submitStudentAttempt({ transcript: cleanedTranscript, source: 'future_asr' })`。不能为 picture 单独添加关键词 shortcut，也不能在 adapter 里把 `red apple`、`apple`、`it is an apple` 之外的答案自动改成命中值。`src/features/classroom-shell/classroom-shell.tsx` 和 `podium-view-model.ts` 只补最小范围的课堂化 waiting / retry / fallback copy，保持老师主导、单 CTA、无大遮罩。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-shell.test.tsx test/unit/classroom-orchestrator.test.ts test/unit/classroom-transcript-adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `picture-talk` 的 transcript 最终通过 `future_asr` source 进入 orchestrator。
    - `test/unit/classroom-orchestrator.test.ts` 明确覆盖 picture transcript success / retry / fallback。
    - `test/unit/classroom-transcript-adapter.test.ts` 明确断言 adapter 不做 picture 阶段强纠错。
    - `classroom-shell.test.tsx` 明确断言 picture 等待/兜底仍然是老师主导和单 CTA。
  </acceptance_criteria>
  <done>picture-talk 已经和 repeat 一样进入真实 transcript 链路，同时保住语义宽松和课堂节奏。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用 fake browser recognition 补 focused smoke，证明 Phase 7 网页闭环</name>
  <files>test/e2e/helpers/fake-browser-audio.ts, test/e2e/audio-classroom-runtime.spec.ts</files>
  <read_first>
    .planning/phases/07-speech-recognition-wiring/07-RESEARCH.md
    test/e2e/helpers/fake-browser-audio.ts
    test/e2e/audio-classroom-runtime.spec.ts
    playwright.config.ts
  </read_first>
  <behavior>
    - Test 1: 课堂 preflight pass/skip 后，repeat 阶段可以在 fake browser recognition 下完成一次 transcript success。
    - Test 2: repeat 阶段还能在 fake recognition timeout / empty result 下进入课堂化 retry。
    - Test 3: picture-talk 阶段可以在 fake recognition transcript 下完成一次语义成功，也能在失败时进入老师兜底。
    - Test 4: focused e2e 不依赖真实浏览器云识别、真实麦克风硬件或外部网络。
  </behavior>
  <action>扩展 `test/e2e/helpers/fake-browser-audio.ts`，在现有 fake speech synthesis / fake media recorder 之外，再注入可编排 transcript 结果的 fake recognition provider。provider 至少支持三类受控输出：成功 transcript、`timeout`, `empty` 或 `unavailable` 失败。更新 `test/e2e/audio-classroom-runtime.spec.ts`，覆盖 `preflight -> repeat transcript success -> picture transcript success` 的主路径，并额外覆盖至少一条 timeout/empty 的 retry 路径。不要要求 Playwright 访问真实云识别服务，也不要新增依赖真实硬件麦克风的断言。</action>
  <verify>
    <automated>npm run test:e2e -- test/e2e/audio-classroom-runtime.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `fake-browser-audio.ts` 能受控地产出 recognition success 和至少一种 failure reason。
    - `audio-classroom-runtime.spec.ts` 覆盖 repeat 和 picture 两条 transcript 路径。
    - focused e2e 运行时不要求真实麦克风硬件或外部云识别。
  </acceptance_criteria>
  <done>Phase 7 已有最小网页闭环证据，后续 execute 和 verifier 可以围绕受控 fake recognition 稳定推进。</done>
</task>

</tasks>

<verification>
07-03 的失败信号是：picture 仍然走兼容性手动通过、e2e 还依赖真浏览器云识别、或者为了提高通过率把 transcript adapter 写成了纠错器。这三类问题任一存在，都说明 Phase 7 还没真正 ready for execute verification。
</verification>

<success_criteria>
- picture-talk 已走真实 transcript -> semantic judgment -> teacher-led retry/fallback 链路。
- focused browser smoke 能在 fake recognition 下证明 preflight、repeat 和 picture 的网页闭环。
- Phase 7 完成后，执行者可以直接进入 execute-phase，不需要再补 recognition test harness。
</success_criteria>

<output>
Focused transcript regression gate ready: Phase 7 can be executed without reopening the classroom runtime shell.
</output>
