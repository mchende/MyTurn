---
phase: 07-speech-recognition-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/classroom-shell/classroom-audio-runtime.ts
  - src/features/classroom-shell/use-classroom-audio-runtime.ts
  - src/features/classroom-shell/classroom-shell.tsx
  - src/features/classroom-shell/classroom-transcript-adapter.ts
  - src/features/classroom-shell/classroom-speech-recognition.ts
  - test/unit/classroom-audio-runtime.test.ts
  - test/unit/classroom-transcript-adapter.test.ts
  - test/unit/classroom-speech-recognition.test.ts
  - test/unit/classroom-shell.test.tsx
autonomous: true
requirements:
  - ASR-01
  - ASR-04
  - PLAT-05
must_haves:
  truths:
    - "Phase 7 先补 transcript/recognition 底座，不重做 Phase 6 的 preflight、自动播报和单 CTA 录音壳层。"
    - "录音结束方式已锁定为纯点按式：同一个 CTA 负责 start 和 stop。"
    - "transcript adapter 只能做轻去噪，不能把错误答案修正成正确答案。"
    - "识别等待必须是显式 runtime 状态，至少带 timeout / empty / unavailable / error 这些可测试 reason。"
    - "成功率与等待体验观测只出现在 dev/testing 环境，不进入孩子端正式课堂界面。"
  artifacts:
    - path: "src/features/classroom-shell/classroom-transcript-adapter.ts"
      provides: "儿童友好的 transcript 去噪与 recognition attempt payload contract"
      exports:
        - "adaptRecognizedTranscript"
        - "buildRecognitionAttemptPayload"
    - path: "src/features/classroom-shell/classroom-speech-recognition.ts"
      provides: "可注入的 browser recognition service contract 与失败原因映射"
      exports:
        - "ClassroomSpeechRecognitionService"
        - "createBrowserSpeechRecognitionService"
    - path: "src/features/classroom-shell/classroom-audio-runtime.ts"
      provides: "显式 recognition wait / telemetry contract"
      exports:
        - "ClassroomAudioStatus"
        - "ClassroomAudioErrorReason"
        - "createClassroomAudioRuntime"
  key_links:
    - from: "src/features/classroom-shell/use-classroom-audio-runtime.ts"
      to: "src/features/classroom-shell/classroom-speech-recognition.ts"
      via: "stop recording 后的 transcript wait 和 recognition service 生命周期"
      pattern: "studentAction|awaiting_transcript|stopStudentRecording"
    - from: "src/features/classroom-shell/classroom-transcript-adapter.ts"
      to: "src/features/classroom-shell/classroom-judgment.ts"
      via: "轻清洗后 transcript 继续走现有 repeat/picture judgment"
      pattern: "normalize|transcript|semanticAccepts|repeatAccepts"
    - from: "src/features/classroom-shell/classroom-shell.tsx"
      to: "src/features/classroom-shell/classroom-audio-runtime.ts"
      via: "dev-only recognition HUD 和等待文案只消费 runtime snapshot"
      pattern: "audioStateLabel|runtimeSnapshot|NODE_ENV"
---

<objective>
建立 Phase 7 的 transcript/recognition 底座：browser recognition service、儿童友好 transcript adapter、显式 timeout/失败 telemetry 和 dev-only 观测面都要先钉稳。

Purpose: 07-01 的任务不是立刻把 repeat/picture 全接通，而是先把“stop recording 后到底怎么拿 transcript、怎么等、怎么失败、怎么观测”这些底层不确定性消掉。
Output: 可注入的 recognition service、轻去噪 transcript adapter、waiting telemetry contract、Wave 0 单测基线。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-speech-recognition-wiring/07-CONTEXT.md
@.planning/phases/07-speech-recognition-wiring/07-RESEARCH.md
@.planning/phases/07-speech-recognition-wiring/07-VALIDATION.md
@src/features/classroom-shell/classroom-audio-runtime.ts
@src/features/classroom-shell/use-classroom-audio-runtime.ts
@src/features/classroom-shell/classroom-shell.tsx
@src/features/classroom-shell/classroom-judgment.ts
@test/unit/classroom-audio-runtime.test.ts
@test/unit/classroom-shell.test.tsx

<interfaces>
Expected new transcript adapter contract:
```ts
type RecognitionAttemptPayload = {
  cleanedTranscript: string;
  rawTranscript: string;
  source: 'future_asr';
};
```

Expected new recognition service contract:
```ts
interface ClassroomSpeechRecognitionService {
  start(): Promise<void>;
  stop(): Promise<void>;
  cancel(): void;
  getFinalResult(): Promise<{
    transcript: string | null;
    confidence?: number | null;
    reason?: 'timeout' | 'empty' | 'unavailable' | 'error' | 'low_confidence';
  }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 transcript adapter 与 browser recognition service 的合同测试</name>
  <files>src/features/classroom-shell/classroom-transcript-adapter.ts, src/features/classroom-shell/classroom-speech-recognition.ts, test/unit/classroom-transcript-adapter.test.ts, test/unit/classroom-speech-recognition.test.ts</files>
  <read_first>
    .planning/phases/07-speech-recognition-wiring/07-CONTEXT.md
    .planning/phases/07-speech-recognition-wiring/07-RESEARCH.md
    src/features/classroom-shell/classroom-judgment.ts
    src/features/classroom-shell/classroom-audio-runtime.ts
  </read_first>
  <behavior>
    - Test 1: transcript adapter 会保留核心词面，只去掉明确 filler：`um`, `uh`, `hmm`, `ah`, `er` 和连续重复起句，例如 `i i`, `it it`, `a a`, `the the`。
    - Test 2: adapter 不会把 `banana` 改成 `apple`，也不会把空 transcript 伪造成可通过答案。
    - Test 3: recognition service 能明确表达 `timeout`、`empty`、`unavailable`、`error` 四类失败原因，并支持受控 fake service 注入。
    - Test 4: browser provider 不可用时，service 会返回 `unavailable`，而不是在 UI 层静默 no-op。
  </behavior>
  <action>先新增 `test/unit/classroom-transcript-adapter.test.ts` 和 `test/unit/classroom-speech-recognition.test.ts` 写 RED。然后新增 `src/features/classroom-shell/classroom-transcript-adapter.ts` 与 `src/features/classroom-shell/classroom-speech-recognition.ts`。adapter 必须明确实现这些规则：统一 lower-case、去标点、合并空白、移除前后独立 filler `um|uh|hmm|ah|er`、收敛单词级重复起句 `i i|it it|a a|the the`，但不做同义词替换、拼写修复或语义改写。recognition service 必须是可注入 contract：浏览器 provider 读取 `window.SpeechRecognition ?? window.webkitSpeechRecognition`；若两者都不存在，直接返回 `unavailable`。测试里使用 fake service / fake constructor，不依赖真实浏览器云识别。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-transcript-adapter.test.ts test/unit/classroom-speech-recognition.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/classroom-shell/classroom-transcript-adapter.ts` 存在 `adaptRecognizedTranscript(`。
    - `src/features/classroom-shell/classroom-speech-recognition.ts` 存在 `ClassroomSpeechRecognitionService` 和 `createBrowserSpeechRecognitionService(`。
    - `test/unit/classroom-transcript-adapter.test.ts` 明确断言 filler 去除与“不过度改写”。
    - `test/unit/classroom-speech-recognition.test.ts` 明确断言 `timeout`、`empty`、`unavailable`、`error`。
  </acceptance_criteria>
  <done>Phase 7 已有稳定的 transcript 产出合同和轻清洗边界，后续 wiring 不需要边接边猜 recognition 行为。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展音频 runtime 的 recognition wait telemetry，并加 dev-only 观测面</name>
  <files>src/features/classroom-shell/classroom-audio-runtime.ts, src/features/classroom-shell/use-classroom-audio-runtime.ts, src/features/classroom-shell/classroom-shell.tsx, test/unit/classroom-audio-runtime.test.ts, test/unit/classroom-shell.test.tsx</files>
  <read_first>
    .planning/phases/07-speech-recognition-wiring/07-RESEARCH.md
    src/features/classroom-shell/classroom-audio-runtime.ts
    src/features/classroom-shell/use-classroom-audio-runtime.ts
    src/features/classroom-shell/classroom-shell.tsx
    test/unit/classroom-audio-runtime.test.ts
  </read_first>
  <behavior>
    - Test 1: stop recording 后 runtime 会进入显式 recognition wait，而不是直接调用 `onRecordingAccepted()` 或回到 ready。
    - Test 2: runtime snapshot 至少暴露 `transcriptStatus`, `transcriptLatencyMs`, `transcriptFailureReason`, `lastTranscript` 或等价字段。
    - Test 3: recognition timeout 超过 3 到 4 秒后会落到明确失败 reason，不会无限停在 `awaiting_transcript`。
    - Test 4: dev-only HUD 只在非 production 环境渲染；正式课堂界面只保留轻量课堂化等待文案。
  </behavior>
  <action>先扩 `test/unit/classroom-audio-runtime.test.ts` 和 `test/unit/classroom-shell.test.tsx` 写 RED。然后在 `src/features/classroom-shell/classroom-audio-runtime.ts` 把现有 `awaiting_transcript` 扩成可观测状态：新增 `transcriptStatus`（例如 `idle|waiting|resolved|failed`）、`transcriptLatencyMs`、`transcriptFailureReason`、`lastTranscript`。在 `src/features/classroom-shell/use-classroom-audio-runtime.ts` 里为后续 wiring 预留 recognition service 注入位，但本任务不接 repeat/picture judgment。`src/features/classroom-shell/classroom-shell.tsx` 只新增 dev-only HUD 或等价小型观测块，显式用 `process.env.NODE_ENV !== 'production'` gate 住；孩子侧正式界面仍只显示课堂化 waiting 文案，例如 `One more second...`，不能显示 `timeout`, `confidence`, `provider missing` 这类技术词。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-audio-runtime.test.ts test/unit/classroom-shell.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `classroom-audio-runtime.ts` 的 snapshot 包含 waiting transcript telemetry 字段。
    - `test/unit/classroom-audio-runtime.test.ts` 覆盖 waiting -> timeout/failed 的状态转换。
    - `classroom-shell.test.tsx` 明确断言 dev HUD 不进入正式课堂 children-facing surface。
    - 没有重做 Phase 6 的 preflight、播放态和 CTA 主交互。
  </acceptance_criteria>
  <done>Phase 7 执行时可以清楚知道 transcript 为什么成功或失败，后续 repeat/picture 接线有可观测底座。</done>
</task>

</tasks>

<verification>
本计划的失败信号很明确：如果 transcript adapter 会自动把错答润色成对答，或者 runtime 仍然把 `awaiting_transcript` 当黑箱，就说明 Phase 7 只是“看起来连上了”，并没有真正建立可执行底座。
</verification>

<success_criteria>
- transcript adapter 和 browser recognition service 已有稳定、可注入的单测合同。
- runtime 能显式表达 recognition wait、timeout 和失败原因。
- dev/testing 环境可以观察 success/latency/failure，正式孩子界面不暴露技术调试词。
</success_criteria>

<output>
Transcript adapter, recognition service contract and waiting telemetry ready for repeat/picture wiring.
</output>
