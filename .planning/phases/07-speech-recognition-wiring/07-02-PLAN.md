---
phase: 07-speech-recognition-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01-PLAN.md
files_modified:
  - src/features/classroom-shell/use-classroom-audio-runtime.ts
  - src/features/classroom-shell/classroom-shell.tsx
  - src/features/classroom-shell/podium-view-model.ts
  - test/unit/classroom-shell.test.tsx
  - test/unit/classroom-orchestrator.test.ts
  - test/unit/classroom-audio-runtime.test.ts
autonomous: true
requirements:
  - ASR-01
  - ASR-02
  - ASR-04
  - PLAT-05
must_haves:
  truths:
    - "07-02 只先接通 `repeat-after-teacher` 的真实 transcript 路径，不提前把 picture-talk 一起糅进去。"
    - "repeat 阶段的真实 transcript 仍然只能通过 `submitStudentAttempt({ source: 'future_asr' })` 进入 reducer。"
    - "Bobby 仍只在 `repeat-after-teacher` 的 `ai_model` 出现，不能因为 recognition 接线而改动角色边界。"
    - "孩子侧仍是一个 CTA：第一次点开始录音，第二次点结束录音并进入 recognition wait。"
    - "timeout / empty / unavailable 的 repeat 失败仍要走老师主导的 encourage / fallback，而不是工具式 toast。"
  artifacts:
    - path: "src/features/classroom-shell/use-classroom-audio-runtime.ts"
      provides: "repeat 阶段 stop -> recognize -> submit transcript 的桥接层"
      exports:
        - "useClassroomAudioRuntime"
    - path: "test/unit/classroom-orchestrator.test.ts"
      provides: "`future_asr` source 下的 repeat judgment / retry / fallback coverage"
      exports:
        - "repeat recognition coverage"
  key_links:
    - from: "src/features/classroom-shell/use-classroom-audio-runtime.ts"
      to: "src/features/classroom-shell/use-classroom-orchestrator.ts"
      via: "识别完成后调用 submitStudentAttempt({ transcript, source: 'future_asr' })"
      pattern: "submitStudentAttempt|future_asr|repeat-after-teacher"
    - from: "src/features/classroom-shell/podium-view-model.ts"
      to: "src/features/classroom-shell/classroom-shell.tsx"
      via: "repeat 阶段录音中 / 等待转写 / retry 的课堂化 copy"
      pattern: "podiumStatus|confirmationButtonLabel|turnLabel"
    - from: "src/features/classroom-shell/classroom-orchestrator.ts"
      to: "test/unit/classroom-orchestrator.test.ts"
      via: "future_asr source 触发既有 repeat judgment 和 teacher-led fallback"
      pattern: "student_attempt_submitted|source|turnResolution"
---

<objective>
把 `repeat-after-teacher` 先接成真实 transcript 路径：孩子点录音、点结束、等待识别、再进入既有 lexical judgment，且失败时仍由老师课堂化接住。

Purpose: 07-02 的目标是先把最可控的 repeat 路径跑顺，证明 Phase 7 不是另起一套新课堂逻辑，而是真正复用现有 orchestrator。
Output: repeat 语音判断闭环、`future_asr` source integration、老师主导的 retry/fallback 课堂反馈。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-speech-recognition-wiring/07-CONTEXT.md
@.planning/phases/07-speech-recognition-wiring/07-RESEARCH.md
@.planning/phases/07-speech-recognition-wiring/07-VALIDATION.md
@.planning/phases/07-speech-recognition-wiring/07-01-PLAN.md
@src/features/classroom-shell/use-classroom-audio-runtime.ts
@src/features/classroom-shell/classroom-orchestrator.ts
@src/features/classroom-shell/use-classroom-orchestrator.ts
@src/features/classroom-shell/podium-view-model.ts
@src/features/classroom-shell/classroom-shell.tsx
@test/unit/classroom-orchestrator.test.ts
@test/unit/classroom-shell.test.tsx

<interfaces>
Expected runtime-to-orchestrator handoff:
```ts
submitStudentAttempt({
  transcript: cleanedTranscript,
  source: 'future_asr',
});
```

Expected repeat recognition state shape:
```ts
type RepeatRecognitionUiState = {
  recordingButtonLabel: 'Tap to talk' | 'Listening... tap again' | 'Try again';
  transcriptStatus: 'waiting' | 'resolved' | 'failed';
  transcriptFailureReason: 'timeout' | 'empty' | 'unavailable' | 'error' | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把 repeat-after-teacher 的 stop -> recognize -> submit judgment 链接通</name>
  <files>src/features/classroom-shell/use-classroom-audio-runtime.ts, test/unit/classroom-audio-runtime.test.ts, test/unit/classroom-orchestrator.test.ts</files>
  <read_first>
    .planning/phases/07-speech-recognition-wiring/07-01-PLAN.md
    src/features/classroom-shell/use-classroom-audio-runtime.ts
    src/features/classroom-shell/use-classroom-orchestrator.ts
    src/features/classroom-shell/classroom-orchestrator.ts
    test/unit/classroom-orchestrator.test.ts
  </read_first>
  <behavior>
    - Test 1: 在 `repeat-after-teacher` 的 `student_wait`，第一次点 CTA 进入录音，第二次点 CTA 结束录音并进入 transcript wait。
    - Test 2: recognition 成功返回 transcript 后，桥接层会调用 `submitStudentAttempt({ source: 'future_asr' })`，而不是 `confirmStudentParticipation()`。
    - Test 3: repeat transcript 命中 lexical judgment 时，orchestrator 继续进入既有 `teacher_feedback`。
    - Test 4: timeout / empty / unavailable / error 时，repeat 仍然进入 `teacher_encourage` 或 `teacher_fallback_model`，而不是卡死在 waiting。
  </behavior>
  <action>先扩 `test/unit/classroom-audio-runtime.test.ts` 和 `test/unit/classroom-orchestrator.test.ts` 写 RED。然后修改 `src/features/classroom-shell/use-classroom-audio-runtime.ts`：只有在 `stageId === 'repeat-after-teacher'` 且孩子完成录音时，才启动 recognition wait，并在结果返回后调用 `submitStudentAttempt({ transcript: cleanedTranscript, source: 'future_asr' })`。不能再用 `onRecordingAccepted()` 直接推进课堂。对 `timeout|empty|unavailable|error`，必须明确走 repeat 的既有老师控场路径：第一次失败进入 `teacher_encourage`，第二次失败进入 `teacher_fallback_model`。Recognition 失败不允许把课堂留在 `awaiting_transcript` 超过既定 3 到 4 秒。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-audio-runtime.test.ts test/unit/classroom-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `use-classroom-audio-runtime.ts` 在 repeat 阶段调用 `submitStudentAttempt({ source: 'future_asr' })`。
    - `test/unit/classroom-orchestrator.test.ts` 明确覆盖 repeat transcript pass / retry / fallback。
    - `awaiting_transcript` 不会在 timeout 后无限停留。
  </acceptance_criteria>
  <done>repeat 语音作答已经不再是假通过，而是进入真实 transcript -> judgment -> teacher feedback 链路。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 保持 repeat 阶段的课堂感和 Bobby 边界，不把识别态做成工具 UI</name>
  <files>src/features/classroom-shell/classroom-shell.tsx, src/features/classroom-shell/podium-view-model.ts, test/unit/classroom-shell.test.tsx</files>
  <read_first>
    .planning/phases/07-speech-recognition-wiring/07-CONTEXT.md
    src/features/classroom-shell/classroom-shell.tsx
    src/features/classroom-shell/podium-view-model.ts
    test/unit/classroom-shell.test.tsx
  </read_first>
  <behavior>
    - Test 1: repeat 录音结束后，讲台区和老师区只出现轻量课堂化 waiting 文案，例如 `One more second...`，不出现技术调试词。
    - Test 2: Bobby 仍只在 `repeat-after-teacher` 的 `ai_model` 播放；进入识别等待后不会再次冒出 Bobby playback 状态。
    - Test 3: repeat transcript 失败时，孩子仍然只看到一个 CTA 和老师课堂化接住，不会出现多按钮或错误面板。
  </behavior>
  <action>先补 `test/unit/classroom-shell.test.tsx` 的 RED 断言，再在 `src/features/classroom-shell/classroom-shell.tsx` 和 `src/features/classroom-shell/podium-view-model.ts` 做最小范围 copy 修订。repeat 识别等待时，podium status / teacher status 只能使用课堂化文案，如 `One more second...`, `Cora is listening carefully.`, `Let's try that once more.`，不能出现 `timeout`, `provider missing`, `confidence low` 这类技术词。Bobby 的播放 pulse 和 copy 必须在进入 recognition wait 时立刻收口，不允许识别态继续占用 Bobby surface。孩子侧 CTA 仍然始终只有一个。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-shell.test.tsx test/unit/classroom-orchestrator.test.ts test/unit/classroom-audio-runtime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `classroom-shell.test.tsx` 明确断言 repeat 识别等待与失败时的课堂化 copy。
    - Bobby 只在 `repeat-after-teacher` 的 `ai_model` 出现，进入识别等待后不再显示 Bobby speaking 状态。
    - repeat 阶段没有新增第二个孩子 CTA、工具式错误卡或遮罩层。
  </acceptance_criteria>
  <done>repeat 语音判断已经像一节课，而不是像录音工具在上传/转码。</done>
</task>

</tasks>

<verification>
07-02 的失败信号不是“识别有点慢”，而是 repeat 路径重新退回了假通过、UI 出现多个按钮，或者 Bobby 在不该出现的时候重新抢镜。这三种情况任意一种出现，都视为计划未完成。
</verification>

<success_criteria>
- repeat-after-teacher 已走真实 transcript -> lexical judgment -> teacher feedback 链路。
- repeat 失败时仍然是老师主导的 encourage/fallback，不是工具式错误状态。
- Bobby 角色边界和单 CTA 护栏在 recognition 接线后保持不变。
</success_criteria>

<output>
Repeat-after-teacher recognition path is real, teacher-led, and still classroom-shaped.
</output>
