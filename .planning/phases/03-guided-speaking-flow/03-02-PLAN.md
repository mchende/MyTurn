---
phase: 03-guided-speaking-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - package.json
  - package-lock.json
  - src/features/classroom-shell/teacher-script.ts
  - src/features/classroom-shell/bobby-script.ts
  - src/features/classroom-shell/podium-view-model.ts
  - src/features/classroom-shell/use-classroom-orchestrator.ts
  - src/features/classroom-shell/classroom-shell.tsx
  - test/unit/teacher-script.test.ts
  - test/unit/bobby-script.test.ts
  - test/unit/classroom-shell.test.tsx
autonomous: true
requirements:
  - CONT-03
  - TEAC-02
  - SPKG-01
must_haves:
  truths:
    - "老师可以在复述轮提供 spoken model，但孩子主视图默认只看到 child-safe 英文提示，不会看到目标词句。"
    - "Bobby 只在 `repeat-after-teacher` 的示范位出现，不会漂移到 `picture-talk` 的重试或救场路径。"
    - "当轮到孩子时，课堂里会出现一个轻量但明确的 participation confirmation 控件，且按钮文案会随 stage 切换。"
  artifacts:
    - path: "src/features/classroom-shell/teacher-script.ts"
      provides: "visibleCaption / spokenModel 双通道老师话术合同"
      exports:
        - "getTeacherScriptLine"
        - "getTeacherHint"
    - path: "src/features/classroom-shell/bobby-script.ts"
      provides: "repeat-after-teacher-only Bobby 示范合同"
      exports:
        - "getBobbyScriptLine"
    - path: "src/features/classroom-shell/classroom-shell.tsx"
      provides: "保留现有课堂壳的前提下接入 participation confirmation 控件"
    - path: "test/unit/classroom-shell.test.tsx"
      provides: "visibleCaption 渲染、button 可见性与 child-safe regression"
  key_links:
    - from: "src/features/classroom-shell/teacher-script.ts"
      to: "src/features/classroom-shell/classroom-shell.tsx"
      via: "TeacherColumn 只渲染 visibleCaption"
      pattern: "visibleCaption"
    - from: "src/features/classroom-shell/bobby-script.ts"
      to: "src/features/classroom-shell/podium-view-model.ts"
      via: "stage-aware Bobby gating"
      pattern: "stageId.*repeat-after-teacher|ai_model"
    - from: "src/features/classroom-shell/classroom-shell.tsx"
      to: "src/features/classroom-shell/use-classroom-orchestrator.ts"
      via: "button click -> confirmStudentParticipation"
      pattern: "confirmStudentParticipation|I said it|I answered"
---

<objective>
把 guided speaking 的 stage 语义真正接到课堂文案和课堂壳上：解决老师可见文案与 spoken model 的合同冲突，并提供不破坏课堂感的手动 participation control。

Purpose: Phase 03 需要同时满足英语优先、图片主导和 child-safe 无答案泄露；如果脚本合同和壳体渲染仍混为一谈，复述轮和看图轮都无法正确表达。
Output: stage-aware teacher/Bobby script、轻量 confirmation UI、以及覆盖 child-safe regression 的 unit tests。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-guided-speaking-flow/03-CONTEXT.md
@.planning/phases/03-guided-speaking-flow/03-RESEARCH.md
@.planning/phases/03-guided-speaking-flow/03-VALIDATION.md
@.planning/phases/02-cast-and-orchestration/02-02-SUMMARY.md
@.planning/phases/02-cast-and-orchestration/02-03-SUMMARY.md
@src/features/classroom-shell/teacher-script.ts
@src/features/classroom-shell/bobby-script.ts
@src/features/classroom-shell/podium-view-model.ts
@src/features/classroom-shell/use-classroom-orchestrator.ts
@src/features/classroom-shell/classroom-shell.tsx
@test/unit/teacher-script.test.ts
@test/unit/bobby-script.test.ts
@test/unit/classroom-shell.test.tsx

<interfaces>
From src/features/classroom-shell/teacher-script.ts:
```ts
export type TeacherScriptRequest = {
  currentItemIndex: number;
  phase: ClassroomOrchestratorPhase;
  targetText: string;
};

export type TeacherScriptLine = {
  hintLabel: string;
  spokenLine: string;
  debugTargetText: string;
};
```

From src/features/classroom-shell/bobby-script.ts:
```ts
type BobbyScriptRequest = {
  currentItemIndex: number;
  phase: ClassroomOrchestratorPhase;
  targetText: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 拆分老师 spoken model 与 child-safe 可见文案，并收紧 Bobby 准入</name>
  <files>src/features/classroom-shell/teacher-script.ts, src/features/classroom-shell/bobby-script.ts, test/unit/teacher-script.test.ts, test/unit/bobby-script.test.ts</files>
  <read_first>
    .planning/phases/03-guided-speaking-flow/03-CONTEXT.md
    .planning/phases/03-guided-speaking-flow/03-RESEARCH.md
    src/features/classroom-shell/teacher-script.ts
    src/features/classroom-shell/bobby-script.ts
    test/unit/teacher-script.test.ts
    test/unit/bobby-script.test.ts
  </read_first>
  <behavior>
    - Test 1: per D-14/D-15，`TeacherScriptLine.visibleCaption` 与 `hintLabel` 保持英文 child-safe，不显示 target text；`spokenModel` 允许在 `repeat-after-teacher` 的 `teacher_prompt` 携带目标语言。
    - Test 2: per D-10/D-12，`picture-talk` 的老师提问使用直接问题和轻量第二次推动，不输出答案片段、首音提示或中文解释。
    - Test 3: per D-06/D-07/D-16，Bobby 仅在 `stageId === 'repeat-after-teacher' && phase === 'ai_model'` 返回脚本，其余包括 `picture-talk` 必须返回 `null`。
  </behavior>
  <action>重写 `src/features/classroom-shell/teacher-script.ts` 的合同，显式解决“teacher visible copy vs spoken model”冲突：`TeacherScriptRequest` 至少新增 `stageId`、`attemptIndex`、`participationState`；`TeacherScriptLine` 至少新增 `visibleCaption`、`spokenModel`、`hintLabel`、`debugTargetText`。在 `repeat-after-teacher` 的 `teacher_prompt`，`spokenModel` 可直接为目标词/短句或包含短 model cue，但 `visibleCaption` 必须保持 child-safe，例如 `Listen first. Then it is your turn.`；在 `picture-talk`，`spokenModel` 使用直接提问和一次轻 second prompt，不提供答案、语义判断或 Phase 4 hint。同步更新 `src/features/classroom-shell/bobby-script.ts`：请求参数新增 `stageId`，准入条件改为同时依赖 `stageId` 与 `phase`，禁止 Bobby 在 `picture-talk` 的任何 phase 露出。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/teacher-script.test.ts test/unit/bobby-script.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `teacher-script.ts` 中可找到 `visibleCaption` 与 `spokenModel` 两个字段，且 `visibleCaption` 默认不含目标词。
    - `bobby-script.ts` 的准入条件同时引用 `stageId` 与 `ai_model`。
    - 定向 unit tests 明确覆盖 repeat-after-teacher 和 picture-talk 两种 script 分支。
  </acceptance_criteria>
  <done>老师与 Bobby 的脚本语义已按 stage 分开，child-safe 展示和未来音频模型消费不会再共用同一字段。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在保留现有课堂壳的前提下接入 confirmation control 与 stage-aware copy</name>
  <files>package.json, package-lock.json, src/features/classroom-shell/podium-view-model.ts, src/features/classroom-shell/use-classroom-orchestrator.ts, src/features/classroom-shell/classroom-shell.tsx, test/unit/classroom-shell.test.tsx</files>
  <read_first>
    src/features/classroom-shell/classroom-shell.tsx
    src/features/classroom-shell/podium-view-model.ts
    src/features/classroom-shell/use-classroom-orchestrator.ts
    src/features/classroom-shell/teacher-script.ts
    test/unit/classroom-shell.test.tsx
    .planning/phases/03-guided-speaking-flow/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `TeacherColumn` 渲染 `visibleCaption`，不渲染 repeat-after-teacher 的 `spokenModel` target text。
    - Test 2: 当 `activeSeat === 'me' && phase === 'student_wait'` 时，课堂壳只出现一个确认按钮；repeat-after-teacher 文案为 `I said it`，picture-talk 文案为 `I answered`。
    - Test 3: 按钮点击通过真实 `user-event` 推进课堂，不直接调用内部 `dispatch`，且 Bobby 在 picture-talk 讲台状态中不可见。
  </behavior>
  <action>在 `package.json` / `package-lock.json` 中加入 `@testing-library/user-event`，用于 Phase 03 的真实交互测试。随后更新 `src/features/classroom-shell/use-classroom-orchestrator.ts`、`src/features/classroom-shell/podium-view-model.ts` 与 `src/features/classroom-shell/classroom-shell.tsx`：保留当前 approved classroom shell 布局、动效和栏目结构，不新增 UI-SPEC 或大改版式；只在现有讲台区域增加一个轻量 confirmation button，并把 `TeacherColumn` 文本改为消费 `teacherScriptLine.visibleCaption`。按钮仅在孩子上台且等待作答时出现，并调用 `confirmStudentParticipation()`；stage badge / podium status / teacher hint 统一切到短英文、图片导向、child-safe 文案。不要新增自动语音触发、correctness judging、semantic matching、分数 UI 或任何泄露目标词的文字提示。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-shell.test.tsx test/unit/teacher-script.test.ts test/unit/bobby-script.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` 出现 `@testing-library/user-event` dev dependency。
    - `classroom-shell.tsx` 中教师主文案来源是 `visibleCaption`，而不是直接渲染 `spokenModel` / 目标词。
    - `test/unit/classroom-shell.test.tsx` 使用 `userEvent.setup({ advanceTimers: ... })` 覆盖按钮可见性、按钮点击推进和 no-target-text leakage。
  </acceptance_criteria>
  <done>Phase 03 的课堂壳具备最小必要的参与确认交互，并且不牺牲现有 classroom shell 的已批准视觉结构。</done>
</task>

</tasks>

<verification>
这一步优先锁住“看得到什么”和“点击后如何推进”的合同。任何把目标词直接渲染回 child-facing UI 的实现都视为回归。
</verification>

<success_criteria>
- 老师可见文案与 spoken model 已分离，repeat-after-teacher 不再靠 UI 直接暴露答案文字。
- Bobby 的存在感被限制在复述示范位，不侵入 picture-talk。
- 课堂里只有一个轻量 confirmation control，且仍保留当前 approved shell 的视觉骨架。
</success_criteria>

<output>
After completion, create `.planning/phases/03-guided-speaking-flow/03-02-SUMMARY.md`
</output>
