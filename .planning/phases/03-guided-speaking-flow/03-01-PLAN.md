---
phase: 03-guided-speaking-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/classroom-shell/classroom-orchestrator.ts
  - src/features/classroom-shell/use-classroom-orchestrator.ts
  - test/unit/classroom-orchestrator.test.ts
autonomous: true
requirements:
  - CONT-04
  - SPKG-01
must_haves:
  truths:
    - "课堂先完整跑完 `repeat-after-teacher`，再进入 `picture-talk`，同一批图片内容会在两个 guided stages 中重复出现。"
    - "孩子的开口推进依赖课堂内显式确认，而不是自动语音触发或隐式计时猜测。"
    - "课堂壳体与脚本层可以稳定拿到正确的 stage、题目进度和尝试次数，并据此持续把课堂从复述推进到看图回答。"
  artifacts:
    - path: "src/features/classroom-shell/classroom-orchestrator.ts"
      provides: "stage-aware reducer、guided stage queue helpers、手动参与确认事件"
      exports:
        - "GUIDED_STAGE_IDS"
        - "buildGuidedStageRuns"
        - "classroomOrchestratorReducer"
        - "createInitialClassroomState"
    - path: "src/features/classroom-shell/use-classroom-orchestrator.ts"
      provides: "useReducer + 单 timer scheduler，并暴露 stage-aware shell API"
      exports:
        - "useClassroomOrchestrator"
    - path: "test/unit/classroom-orchestrator.test.ts"
      provides: "guided stage 队列、repeat->picture 切换、手动确认推进的回归测试"
  key_links:
    - from: "src/features/classroom-shell/use-classroom-orchestrator.ts"
      to: "src/features/classroom-shell/classroom-orchestrator.ts"
      via: "useReducer + dispatch(confirm event / timer event)"
      pattern: "useReducer|phase_timer_completed|student_participation_confirmed"
    - from: "src/features/classroom-shell/classroom-orchestrator.ts"
      to: "src/features/lesson-config/lesson-schema.ts"
      via: "lesson.stages filtered through GUIDED_STAGE_IDS"
      pattern: "lesson\\.stages|repeat-after-teacher|picture-talk"
    - from: "test/unit/classroom-orchestrator.test.ts"
      to: "src/features/classroom-shell/classroom-orchestrator.ts"
      via: "direct reducer assertions"
      pattern: "currentStageId|attemptIndex|buildGuidedStageRuns"
---

<objective>
建立 Phase 03 的基础 guided-speaking contract：把现有 item-only reducer 升级为 stage-aware speaking queue，并把孩子参与推进改成显式课堂确认。

Purpose: 先把 `repeat-after-teacher -> picture-talk` 的骨架和手动 participation signal 固定下来，后续 stage-aware 脚本、child-safe 文案与 retry 逻辑才有稳定依附点。
Output: 更新后的 orchestrator reducer、hook API，以及覆盖 guided stage 切换与手动确认的 unit tests。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-guided-speaking-flow/03-CONTEXT.md
@.planning/phases/03-guided-speaking-flow/03-RESEARCH.md
@.planning/phases/03-guided-speaking-flow/03-VALIDATION.md
@.planning/phases/02-cast-and-orchestration/02-01-SUMMARY.md
@src/features/lesson-config/lesson-schema.ts
@content/lessons/week-01/lesson-01.ts
@src/features/classroom-shell/classroom-orchestrator.ts
@src/features/classroom-shell/use-classroom-orchestrator.ts
@test/unit/classroom-orchestrator.test.ts

<interfaces>
From src/features/lesson-config/lesson-schema.ts:
```ts
export type LessonItem = z.infer<typeof lessonItemSchema>;
export type LessonStage = z.infer<typeof lessonStageSchema>;
export type Lesson = z.infer<typeof lessonSchema>;
```

From src/features/classroom-shell/classroom-orchestrator.ts:
```ts
export type ClassroomOrchestratorPhase =
  | 'teacher_prompt'
  | 'ai_model'
  | 'student_wait'
  | 'teacher_encourage'
  | 'teacher_echo'
  | 'teacher_feedback'
  | 'move_next'
  | 'wrap_up';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 guided stage queue 与基础 reducer contract</name>
  <files>src/features/classroom-shell/classroom-orchestrator.ts, test/unit/classroom-orchestrator.test.ts</files>
  <read_first>
    .planning/phases/03-guided-speaking-flow/03-CONTEXT.md
    .planning/phases/03-guided-speaking-flow/03-RESEARCH.md
    .planning/phases/03-guided-speaking-flow/03-VALIDATION.md
    src/features/lesson-config/lesson-schema.ts
    content/lessons/week-01/lesson-01.ts
    src/features/classroom-shell/classroom-orchestrator.ts
    test/unit/classroom-orchestrator.test.ts
  </read_first>
  <behavior>
    - Test 1: per D-01/D-02/D-03，reducer 只消费 `repeat-after-teacher` 与 `picture-talk` 两个 guided stages，并按 stage 顺序推进，不把 `warmup` / `wrap-up` 混进 speaking queue。
    - Test 2: per D-04/D-05/D-06，`repeat-after-teacher` item 走 `teacher_prompt -> ai_model -> student_wait`，`picture-talk` item 走 `teacher_prompt -> student_wait`，同一 lesson item 可在两个 stages 中复用。
    - Test 3: per D-08/D-09，孩子发言完成必须由显式确认事件推进，不能新增自动 speech trigger、correctness judging、semantic matching 或 hidden auto-pass。
  </behavior>
  <action>先在 `test/unit/classroom-orchestrator.test.ts` 写失败测试，再改 `src/features/classroom-shell/classroom-orchestrator.ts`。新增并导出 `GUIDED_STAGE_IDS`、`GuidedStageId = 'repeat-after-teacher' | 'picture-talk'`、`buildGuidedStageRuns(lesson: Lesson)`，让 reducer 从 `lesson.stages` 中解析 guided queue，而不是继续递增 `lesson.items`。把 `ClassroomOrchestratorState` 扩展为至少包含 `currentStageId`、`currentStageIndex`、`currentStageItemIndex`、`attemptIndex`、`currentItem`、`currentItemIndex`。新增 `student_participation_confirmed` 事件替代“孩子已说”的笼统推进信号；本计划只建立 stage-aware success path 和 stage 切换，不提前实现 `picture-talk` 两次机会的 retry 细节，也不要重定义 lesson schema 或引入新状态机库。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/classroom-shell/classroom-orchestrator.ts` 导出 `GUIDED_STAGE_IDS`、`buildGuidedStageRuns`、`createInitialClassroomState`、`classroomOrchestratorReducer`。
    - reducer state 中可直接读到 `currentStageId`、`currentStageIndex`、`currentStageItemIndex`、`attemptIndex`。
    - 单测能断言课程先跑完整个 `repeat-after-teacher`，再切到 `picture-talk`，而不是 item 内模式来回切换。
  </acceptance_criteria>
  <done>guided speaking 的基础状态图已固定，执行器无需再猜 Phase 03 的课程骨架。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 暴露 stage-aware hook API 与手动 participation handler</name>
  <files>src/features/classroom-shell/use-classroom-orchestrator.ts, test/unit/classroom-orchestrator.test.ts, test/unit/classroom-shell.test.tsx</files>
  <read_first>
    src/features/classroom-shell/classroom-orchestrator.ts
    src/features/classroom-shell/use-classroom-orchestrator.ts
    test/unit/classroom-orchestrator.test.ts
    test/unit/classroom-shell.test.tsx
    .planning/phases/03-guided-speaking-flow/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: shell-level hook consumer 能读到 `currentStageId`、`currentStageItemIndex`、`attemptIndex`，且第一题从 `repeat-after-teacher` 的 `teacher_prompt` 启动。
    - Test 2: `confirmStudentParticipation()` 只能在 `student_wait` 生效，且会把当前轮推进到 `teacher_feedback`。
    - Test 3: timer effect 仍保持单一调度入口；不要把 participation confirmation 写成组件内多段 `setTimeout` 链，并要用 shell/hook 层自动化断言覆盖这一合同。
  </behavior>
  <action>先扩展 `test/unit/classroom-shell.test.tsx` 或新增等价 hook consumer 测试，再更新 `src/features/classroom-shell/use-classroom-orchestrator.ts`。测试必须显式断言 shell/hook consumer 可以读到 `currentStageId`、`currentStageIndex`、`currentStageItemIndex`、`attemptIndex`，并通过 `confirmStudentParticipation()` 在 `student_wait` 时把当前轮推进到 `teacher_feedback`；同时用 fake timers 验证仍只有一条集中 timer 调度路径，而不是组件里新增多段 `setTimeout` 链。实现时让 public API 至少返回上述 stage metadata、`teacherScriptLine` 未来所需字段和明确命名的 `confirmStudentParticipation()` handler。把现有 `markStudentSpoke()` 调用位迁移到新命名，避免 Phase 03 继续暴露“系统知道你说了什么”的暗示。保留 `showReward()` / `hideReward()` 显式 gate 兼容已有课堂壳体验证，但不要在这个计划里新增自动发言检测、评分或提示答案路径。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-orchestrator.test.ts test/unit/classroom-shell.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - hook 返回值包含 `currentStageId`、`currentStageItemIndex`、`attemptIndex` 与 `confirmStudentParticipation`。
    - `use-classroom-orchestrator.ts` 仍只有一个基于 `CLASSROOM_TIMINGS` 的集中 timer effect。
    - `test/unit/classroom-shell.test.tsx` 或等价 hook consumer 测试明确断言 stage-aware fields 与 `confirmStudentParticipation()` 的 shell-level 合同。
    - 定向 unit tests 通过，且没有新增自动语音触发相关事件名。
  </acceptance_criteria>
  <done>shell 和脚本在 Phase 03 可以依赖统一的 stage-aware hook contract，而不是自行判断 item/phase 组合。</done>
</task>

</tasks>

<verification>
优先用 reducer/hook 单测锁住 guided stage 骨架与手动确认信号。Phase 03 的第一步是合同稳定，不是先做 UI 表面变化。
</verification>

<success_criteria>
- `repeat-after-teacher` 与 `picture-talk` 的队列推进由 reducer 明确定义，并直接消费 `lesson.stages`。
- 手动 participation confirmation 已成为唯一课堂推进信号，Phase 03 不依赖自动语音触发。
- 后续计划可以直接基于 `currentStageId`、`attemptIndex` 和 `confirmStudentParticipation()` 写脚本与界面。
</success_criteria>

<output>
After completion, create `.planning/phases/03-guided-speaking-flow/03-01-SUMMARY.md`
</output>
