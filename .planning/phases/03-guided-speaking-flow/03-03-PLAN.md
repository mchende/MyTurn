---
phase: 03-guided-speaking-flow
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/features/classroom-shell/classroom-orchestrator.ts
  - src/features/classroom-shell/use-classroom-orchestrator.ts
  - src/features/classroom-shell/teacher-script.ts
  - src/features/classroom-shell/lesson-board.tsx
  - test/unit/classroom-orchestrator.test.ts
  - test/unit/classroom-shell.test.tsx
  - test/unit/lesson-board.test.tsx
  - test/e2e/guided-speaking-flow.spec.ts
autonomous: true
requirements:
  - CONT-03
  - CONT-04
  - TEAC-02
must_haves:
  truths:
    - "同一批图片内容会先经历复述，再进入更高要求的看图回答，阶段升级在课堂里可见且不突兀。"
    - "picture-talk 中第一次没接上时只有一次轻 second prompt，然后给第二次机会；第二次仍未完成时老师快速收束并切下一题。"
    - "整个 guided flow 仍然以图片和英文短提示为主，不新增答案泄露、自动判断或更重提示。"
  artifacts:
    - path: "src/features/classroom-shell/classroom-orchestrator.ts"
      provides: "picture-talk 二次机会、收束推进与 stage-aware move_next"
    - path: "src/features/classroom-shell/lesson-board.tsx"
      provides: "阶段升级后的 board badge/prompt 呈现，继续保持无 target 泄露"
    - path: "test/e2e/guided-speaking-flow.spec.ts"
      provides: "repeat-after-teacher -> picture-talk 过渡的 focused browser regression"
    - path: "test/unit/classroom-shell.test.tsx"
      provides: "retry/button label/stage copy 的集成回归"
  key_links:
    - from: "src/features/classroom-shell/classroom-orchestrator.ts"
      to: "src/features/classroom-shell/teacher-script.ts"
      via: "attemptIndex + participationState drive second prompt / close copy"
      pattern: "attemptIndex|participationState"
    - from: "src/features/classroom-shell/use-classroom-orchestrator.ts"
      to: "src/features/classroom-shell/lesson-board.tsx"
      via: "stageBadge + stagePrompt derived from currentStageId and attemptIndex"
      pattern: "stageBadge|stagePrompt"
    - from: "test/e2e/guided-speaking-flow.spec.ts"
      to: "src/features/classroom-shell/classroom-shell.tsx"
      via: "button click + visible prompt assertions"
      pattern: "I said it|I answered|Picture talk|Repeat"
---

<objective>
完成 Phase 03 的 retry 与验证收尾：把 picture-talk 的两次机会与老师收束做实，并用 focused tests 证明同一批内容已经从复述升级到看图回答。

Purpose: 前两份计划给出 guided stage 骨架和 shell 语义，这一份把课堂推进打磨到可验证状态，防止 Phase 03 演变成“只是换了几句文案”的假升级。
Output: 带 second-attempt 逻辑的 reducer、对应的 board/shell copy polish，以及 focused unit + e2e regression。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-guided-speaking-flow/03-CONTEXT.md
@.planning/phases/03-guided-speaking-flow/03-RESEARCH.md
@.planning/phases/03-guided-speaking-flow/03-VALIDATION.md
@src/features/classroom-shell/classroom-orchestrator.ts
@src/features/classroom-shell/use-classroom-orchestrator.ts
@src/features/classroom-shell/teacher-script.ts
@src/features/classroom-shell/lesson-board.tsx
@test/unit/classroom-orchestrator.test.ts
@test/unit/classroom-shell.test.tsx
@test/unit/lesson-board.test.tsx
@test/e2e/classroom-entry.spec.ts

<interfaces>
From src/features/classroom-shell/lesson-board.tsx:
```ts
type LessonBoardProps = {
  currentItem: {
    imageAlt: string;
    imageSrc: string;
    text: string;
  };
  currentItemIndex: number;
  progressCount: number;
  stageBadge: string;
  stagePrompt: string;
  sessionStatus: string;
  debugTargetText?: string;
  showDebugTarget?: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 picture-talk 二次机会与老师收束路径</name>
  <files>src/features/classroom-shell/classroom-orchestrator.ts, src/features/classroom-shell/use-classroom-orchestrator.ts, src/features/classroom-shell/teacher-script.ts, test/unit/classroom-orchestrator.test.ts, test/unit/classroom-shell.test.tsx</files>
  <read_first>
    .planning/phases/03-guided-speaking-flow/03-CONTEXT.md
    .planning/phases/03-guided-speaking-flow/03-RESEARCH.md
    src/features/classroom-shell/classroom-orchestrator.ts
    src/features/classroom-shell/use-classroom-orchestrator.ts
    src/features/classroom-shell/teacher-script.ts
    test/unit/classroom-orchestrator.test.ts
    test/unit/classroom-shell.test.tsx
  </read_first>
  <behavior>
    - Test 1: per D-10/D-11/D-12，picture-talk 第一次 `student_wait` 超时后进入 `teacher_encourage`，随后回到 `student_wait` 且 `attemptIndex` 变为 `1`。
    - Test 2: per D-13，第二次 picture-talk 超时后不进入 `teacher_echo`、不出现 Bobby rescue，而是走老师收束 -> `move_next`。
    - Test 3: 孩子在 picture-talk 的第一次或第二次确认都能正常完成当前题，不新增 correctness judging、semantic matching 或答案提示。
  </behavior>
  <action>在 `src/features/classroom-shell/classroom-orchestrator.ts` 中把 `picture-talk` 的 retry 路径显式建模：当 `currentStageId === 'picture-talk'` 且第一次 `student_wait` 超时，状态应转入 `teacher_encourage`；`teacher_encourage` 的 timer 完成后必须回到 `student_wait` 并允许第二次作答；若第二次仍超时，则进入老师收束分支并在下一次 timer 后 `move_next`。复述轮继续沿用既有 `teacher_prompt -> ai_model -> student_wait -> teacher_feedback` 主链，不把两次机会逻辑扩散到 repeat stage。同步更新 `teacher-script.ts`，让 `attemptIndex + participationState + stageId` 决定 picture-talk 的 first prompt、second prompt 与 close copy；轻 second prompt 只能是课堂推进语，不能泄露答案、半提示或进入 Phase 4 hints。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-orchestrator.test.ts test/unit/classroom-shell.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - reducer 测试能断言 picture-talk 的第一次超时返回二次机会，第二次超时直接收束到下一题。
    - `teacher_echo` 仅保留在复述/沉默带读链路，不用于 picture-talk 第二次机会。
    - `classroom-shell.test.tsx` 覆盖 `I answered` 点击成功、轻 second prompt、Bobby 缺席三类分支。
  </acceptance_criteria>
  <done>picture-talk 的课堂节奏具备“一次轻推动 + 第二次机会 + 快速收束”的明确规则，Phase 03 不再停留在单轮伪流程。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 打磨 board 文案升级并补齐 focused validation</name>
  <files>src/features/classroom-shell/lesson-board.tsx, src/features/classroom-shell/use-classroom-orchestrator.ts, test/unit/lesson-board.test.tsx, test/e2e/guided-speaking-flow.spec.ts</files>
  <read_first>
    src/features/classroom-shell/lesson-board.tsx
    src/features/classroom-shell/use-classroom-orchestrator.ts
    test/unit/lesson-board.test.tsx
    test/e2e/classroom-entry.spec.ts
    .planning/phases/03-guided-speaking-flow/03-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: board 的 `stageBadge` / `stagePrompt` 能明确区分 `repeat-after-teacher` 与 `picture-talk`，并在 picture-talk 第二次机会时显示更轻的再试一次提示。
    - Test 2: board 在任何 guided stage 下都仍以图片为主，不显示 target text，除非显式 debug guard 打开。
    - Test 3: 浏览器 smoke 能覆盖 repeat-after-teacher 完成一题、切到 picture-talk、按钮文案从 `I said it` 切到 `I answered` 的全过程。
  </behavior>
  <action>更新 `src/features/classroom-shell/use-classroom-orchestrator.ts` 与 `src/features/classroom-shell/lesson-board.tsx` 的 board copy 衍生逻辑，让 `stageBadge` 至少体现当前模式与 stage 内进度，例如 `Repeat after Cora · 1/5`、`Picture talk · 1/5`；`stagePrompt` 必须保持英文优先、图片导向，且第二次 picture-talk 机会只使用轻 `Try once more.` 级别提示。不要在 board 上加入 target text、打分、答案 chips 或新的辅导面板。新增 `test/e2e/guided-speaking-flow.spec.ts`，从课堂首轮复述出发，点击确认按钮推进，直到进入 picture-talk 并验证模式升级与 child-safe copy；保留现有 `classroom-entry.spec.ts` 作为入场 smoke，不要把所有 Phase 03 断言都塞回原文件。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/lesson-board.test.tsx test/unit/classroom-orchestrator.test.ts test/unit/classroom-shell.test.tsx && npm run test:e2e -- test/e2e/guided-speaking-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lesson-board.test.tsx` 断言 repeat-after-teacher / picture-talk 的 badge 或 prompt 差异，同时继续覆盖 no-target-text leakage。
    - 仓库中新增 `test/e2e/guided-speaking-flow.spec.ts`，而不是只扩写入场 smoke。
    - 目标验证命令可以证明同一批图片已经从复述升级为看图回答，且没有出现答案泄露或自动评分 UI。
  </acceptance_criteria>
  <done>Phase 03 的“复述 -> 看图回答”升级已在 board copy 和浏览器回归里可见、可验证。</done>
</task>

</tasks>

<verification>
Phase 03 的最终验证必须同时覆盖 reducer 分支和真实浏览器交互。只跑 unit 而不验证 repeat->picture 过渡，不足以证明 CONT-03 / CONT-04 已实现。
</verification>

<success_criteria>
- picture-talk 的两次机会与老师收束路径稳定存在，且不借用 Phase 4 的提示体系。
- board/shell 文案能明确表达从复述到看图回答的升级，同时继续依赖图片和英文短句。
- focused unit + e2e tests 能证明 Phase 03 的核心课堂合同已落地。
</success_criteria>

<output>
After completion, create `.planning/phases/03-guided-speaking-flow/03-03-SUMMARY.md`
</output>
