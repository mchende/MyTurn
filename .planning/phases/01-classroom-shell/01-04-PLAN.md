---
phase: 01-classroom-shell
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - src/app/lesson/[sessionId]/page.tsx
  - src/features/classroom-shell/classroom-shell.tsx
  - src/features/classroom-shell/student-seat-strip.tsx
  - src/features/classroom-shell/stage-panel.tsx
  - src/features/classroom-shell/teacher-panel.tsx
  - src/features/classroom-shell/lesson-board.tsx
  - test/unit/classroom-shell.test.tsx
  - test/e2e/classroom-entry.spec.ts
autonomous: true
requirements:
  - CLAS-01
  - PLAT-01
must_haves:
  truths:
    - "用户可以从首页的 `进入课堂` CTA 进入真实的课堂路由，而不是停留在静态展示页。"
    - "进入课堂页后，顶部学生席、左侧课件主屏、右侧讲台区和教师区会同时出现，并在平板横屏下保持明确主次。"
    - "课堂布局在手机和桌面端也能保留席位到讲台的关系表达，而不是只在主断点上成立。"
  artifacts:
    - path: "src/app/lesson/[sessionId]/page.tsx"
      provides: "课堂路由页面"
    - path: "src/features/classroom-shell/classroom-shell.tsx"
      provides: "三段式课堂空间组件"
    - path: "test/unit/classroom-shell.test.tsx"
      provides: "PLAT-01 组件验证"
    - path: "test/e2e/classroom-entry.spec.ts"
      provides: "CLAS-01 冒烟验证"
  key_links:
    - from: "src/app/lesson/[sessionId]/page.tsx"
      to: "src/features/lesson-config/load-lesson.ts"
      via: "lessonId to lesson content"
      pattern: "loadLesson"
    - from: "src/app/lesson/[sessionId]/page.tsx"
      to: "src/features/classroom-shell/classroom-shell.tsx"
      via: "route composition"
      pattern: "ClassroomShell"
    - from: "test/e2e/classroom-entry.spec.ts"
      to: "src/app/(marketing)/page.tsx"
      via: "homepage assertion before lesson entry"
      pattern: "今日课表"
    - from: "test/e2e/classroom-entry.spec.ts"
      to: "src/app/lesson/[sessionId]/page.tsx"
      via: "enter class flow"
      pattern: "/lesson/"
---

<objective>
实现课堂壳体、课堂路由与自动化验证激活，打通从首页进入一节课的完整浏览器链路。

Purpose: 把首页入口接上真实课堂页，并把 Wave 0 的 classroom/unit/e2e 占位测试替换成可执行验证。
Output: 课堂路由、三段式课堂组件、响应式课堂布局测试，以及首页到课堂的 Playwright 冒烟链路。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@DESIGN.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-classroom-shell/01-CONTEXT.md
@.planning/phases/01-classroom-shell/01-UI-SPEC.md
@.planning/phases/01-classroom-shell/01-VALIDATION.md
@.planning/phases/01-classroom-shell/01-02-SUMMARY.md
@.planning/phases/01-classroom-shell/01-03-SUMMARY.md

<interfaces>
From `src/features/lesson-config/lesson-schema.ts`:
```ts
export type LessonStageId =
  | 'warmup'
  | 'repeat-after-teacher'
  | 'picture-talk'
  | 'wrap-up'

export interface LessonItem {
  id: string
  text: string
  imageSrc: string
  imageAlt: string
  imageWidth: number
  imageHeight: number
}
```

From `src/features/schedule/build-day-sessions.ts`:
```ts
export interface DaySessionViewModel {
  sessionId: string
  lessonId: string
  startsAt: string
  endsAt: string
  durationMinutes: number
  entryOpensAt: string
  accessState: SessionAccessState
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the classroom surface and activate automated verification</name>
  <read_first>
    .planning/phases/01-classroom-shell/01-CONTEXT.md
    .planning/phases/01-classroom-shell/01-UI-SPEC.md
    .planning/phases/01-classroom-shell/01-VALIDATION.md
    .planning/phases/01-classroom-shell/01-03-SUMMARY.md
    src/app/lesson/[sessionId]/page.tsx
    src/features/classroom-shell/classroom-shell.tsx
    src/features/classroom-shell/student-seat-strip.tsx
    src/features/classroom-shell/stage-panel.tsx
    src/features/classroom-shell/teacher-panel.tsx
    src/features/classroom-shell/lesson-board.tsx
    src/features/lesson-config/load-lesson.ts
    test/unit/classroom-shell.test.tsx
    test/e2e/classroom-entry.spec.ts
  </read_first>
  <files>src/app/lesson/[sessionId]/page.tsx, src/features/classroom-shell/classroom-shell.tsx, src/features/classroom-shell/student-seat-strip.tsx, src/features/classroom-shell/stage-panel.tsx, src/features/classroom-shell/teacher-panel.tsx, src/features/classroom-shell/lesson-board.tsx, test/unit/classroom-shell.test.tsx, test/e2e/classroom-entry.spec.ts</files>
  <behavior>
    - Test 1: the classroom page renders the student seat strip, lesson board, stage panel, and teacher panel together.
    - Test 2: the lesson board uses the seed lesson image and keeps stable alt text.
    - Test 3: the unit test and e2e flow both verify responsive classroom behavior across tablet-first layout plus phone/desktop compatibility.
  </behavior>
  <action>Replace the Wave 0 placeholders in `test/unit/classroom-shell.test.tsx` and `test/e2e/classroom-entry.spec.ts` first, then implement the tablet-first classroom shell defined by D-17 through D-27 and UI-SPEC `Classroom Surface` / `Responsive Rules`. In `src/features/classroom-shell/classroom-shell.tsx`, compose the three-part stage: top `StudentSeatStrip`, left `LessonBoard`, right stacked `StagePanel` and `TeacherPanel`. Use the exact seat lineup `You`, `Milo`, `Seat 3`, `Seat 4`, where `Seat 3` and `Seat 4` are visual atmosphere placeholders only per D-21. The `StagePanel` must render a default waiting state with exact copy `待上台` when no one is on stage yet per D-22. The `TeacherPanel` must render a fixed teacher identity block with exact label `Teacher Mia`. The `LessonBoard` must render the first lesson item image from `loadLesson(lessonId)` using the parsed local asset path and the item alt text. In `src/app/lesson/[sessionId]/page.tsx`, resolve the session from the day schedule, load its lesson, and render the classroom shell; if the session id is unknown, call `notFound()`. Implement responsive behavior explicitly for PLAT-01: tablet/desktop keep the top-strip plus left/right split, phone compresses the top strip and stacks stage/teacher while keeping the teacher panel continuously visible, desktop only expands margins without adding new panels. Replace `test/unit/classroom-shell.test.tsx` with assertions for `Teacher Mia`, `待上台`, `You`, `Milo`, the lesson image alt text, and responsive class markers or render variants that prove phone/tablet/desktop support. Update `test/e2e/classroom-entry.spec.ts` by removing `test.skip`, first asserting the exact homepage contract `今日课表`, then clicking the exact lesson-entry CTA wired in Plan 03 (`进入课堂` linking to `/lesson/[sessionId]`), and finally asserting the URL matches `/lesson/` and the text `Teacher Mia` is visible. This task closes CLAS-01 and PLAT-01 while preserving the classroom-first feel from D-01, D-17, D-18, D-19, D-20, D-21, D-22 and D-23.</action>
  <acceptance_criteria>
    `src/features/classroom-shell/student-seat-strip.tsx` contains the exact strings `You`, `Milo`, `Seat 3`, and `Seat 4`.
    `src/features/classroom-shell/stage-panel.tsx` contains the exact string `待上台`.
    `src/features/classroom-shell/teacher-panel.tsx` contains the exact string `Teacher Mia`.
    `src/features/classroom-shell/classroom-shell.tsx` contains responsive layout markers for phone and tablet/desktop, such as `md:` or `lg:`.
    `test/unit/classroom-shell.test.tsx` asserts `Teacher Mia` and `待上台` and no longer contains `Wave 0 placeholder`.
    `test/e2e/classroom-entry.spec.ts` no longer contains `test.skip(` and contains assertions for `今日课表`, plus `getByRole('link', { name: '进入课堂' })` or equivalent link-based navigation.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/unit/classroom-shell.test.tsx && npx playwright test test/e2e/classroom-entry.spec.ts</automated>
  </verify>
  <done>用户可以从今日课表进入真实课堂页，且课堂页在平板优先布局中同时展示学生席、课件区、讲台区和教师区，并在 phone/desktop 上保持课堂关系表达与自动化验证覆盖。</done>
</task>

</tasks>

<verification>
执行 `npx vitest run test/unit/classroom-shell.test.tsx && npx playwright test test/e2e/classroom-entry.spec.ts` 必须通过。随后手动在手机、平板横屏和桌面三个视口确认课堂页都保持“顶部学生席 + 主课件区 + 讲台/教师区”的课堂语义。
</verification>

<success_criteria>
Phase 1 的浏览器入口和课堂外壳已经打通：用户能从首页进入一节课，并在 tablet-first 布局下感受到明确的三段式小班课空间，同时 phone/desktop 兼容要求也被显式实现与验证。
</success_criteria>

<output>
After completion, create `.planning/phases/01-classroom-shell/01-04-SUMMARY.md`
</output>
