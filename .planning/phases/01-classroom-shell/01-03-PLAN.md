---
phase: 01-classroom-shell
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/app/(marketing)/page.tsx
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/lib/utils.ts
  - src/features/schedule/get-today-schedule-view-model.ts
  - src/features/schedule/entry-countdown.tsx
  - src/features/schedule/session-card.tsx
  - test/unit/homepage-shell.test.tsx
autonomous: true
requirements:
  - CLAS-01
  - PLAT-01
must_haves:
  truths:
    - "用户打开首页后，先看到的是带有左侧轻导航和课程卡片网格的 `我的课堂` 页面，而不是通用 dashboard。"
    - "允许入场的课次提供明确的 `进入课堂` CTA；过了窗口则显示 `已开课，停止入场`。"
    - "首页在平板横屏保留左导航 + 右内容区结构，同时在手机和桌面端仍能保留完整课次顺序与可读性。"
  artifacts:
    - path: "src/app/(marketing)/page.tsx"
      provides: "`我的课堂` 首页"
    - path: "src/features/schedule/get-today-schedule-view-model.ts"
      provides: "首页 schedule view-model"
    - path: "src/features/schedule/session-card.tsx"
      provides: "课次卡片状态与 CTA 呈现"
    - path: "src/features/schedule/entry-countdown.tsx"
      provides: "倒计时组件"
    - path: "test/unit/homepage-shell.test.tsx"
      provides: "`我的课堂` 首页渲染合同测试"
  key_links:
    - from: "src/app/(marketing)/page.tsx"
      to: "src/features/schedule/get-today-schedule-view-model.ts"
      via: "server-side view model load"
      pattern: "getTodayScheduleViewModel"
    - from: "src/features/schedule/session-card.tsx"
      to: "src/lib/time/session-access-state.ts"
      via: "SessionAccessState rendering"
      pattern: "open_for_entry"
    - from: "src/features/schedule/session-card.tsx"
      to: "src/app/lesson/[sessionId]/page.tsx"
      via: "lesson-entry Link for open session cards"
      pattern: "/lesson/"
    - from: "src/features/schedule/entry-countdown.tsx"
      to: "src/app/(marketing)/page.tsx"
      via: "countdown surface"
      pattern: "距开场"
---

<objective>
实现 Phase 1 的首页课表入口：Stitch 原型风格的 `我的课堂` 页面、可入场状态、课程卡片网格，以及兼容手机/平板/桌面的课堂入口体验。

Purpose: 先把“今天有课、现在能不能进去”的产品入口做成，并按原型建立左导航 + 课程卡片网格的首页骨架，避免继续把首页与课堂页耦合在同一 plan 中。
Output: 首页课表路由、轻导航骨架、共享 UI primitives、倒计时交互与响应式课程卡片。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@DESIGN.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-classroom-shell/01-CONTEXT.md
@.planning/phases/01-classroom-shell/01-UI-SPEC.md
@.planning/phases/01-classroom-shell/01-VALIDATION.md
@.planning/phases/01-classroom-shell/01-01-SUMMARY.md
@.planning/phases/01-classroom-shell/01-02-SUMMARY.md

<interfaces>
From `src/lib/time/session-access-state.ts`:
```ts
export type SessionAccessState =
  | 'upcoming'
  | 'open_for_entry'
  | 'in_progress_locked'
  | 'completed'
```

From `src/features/schedule/build-day-sessions.ts`:
```ts
export interface DaySessionViewModel {
  sessionId: string
  lessonId: string
  startsAt: string
  endsAt: string
  durationMinutes: number
  entryOpensAt: string
  accessState: SessionAccessState
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the schedule homepage and entry-state UI</name>
  <read_first>
    .planning/phases/01-classroom-shell/01-CONTEXT.md
    .planning/phases/01-classroom-shell/01-UI-SPEC.md
    .planning/phases/01-classroom-shell/01-02-SUMMARY.md
    src/app/(marketing)/page.tsx
    src/features/schedule/get-today-schedule-view-model.ts
    src/features/schedule/entry-countdown.tsx
    src/features/schedule/session-card.tsx
    test/unit/homepage-shell.test.tsx
    src/components/ui/button.tsx
    src/components/ui/card.tsx
    src/components/ui/badge.tsx
    src/lib/time/session-access-state.ts
    content/schedules/default-weekday.ts
  </read_first>
  <files>src/app/(marketing)/page.tsx, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/badge.tsx, src/lib/utils.ts, src/features/schedule/get-today-schedule-view-model.ts, src/features/schedule/entry-countdown.tsx, src/features/schedule/session-card.tsx, test/unit/homepage-shell.test.tsx</files>
  <behavior>
    - Test 1: the homepage renders the full day schedule as a responsive card grid, not just one featured lesson.
    - Test 2: an `open_for_entry` session shows the CTA `进入课堂`; an `in_progress_locked` session shows `已开课，停止入场`.
    - Test 3: upcoming sessions display a concrete start time and countdown text on tablet, phone, and desktop breakpoints while preserving the Stitch left-nav structure.
  </behavior>
  <action>Build the homepage experience required by D-04 through D-10 and D-17 through D-23 using UI-SPEC `Schedule Surface`, `Responsive Rules`, and the authoritative Stitch homepage prototype. Add `button`, `card`, and `badge` via shadcn/ui plus a `cn` helper in `src/lib/utils.ts`. In `src/features/schedule/get-today-schedule-view-model.ts`, read `content/schedules/default-weekday.ts`, build the day sessions using the Plan 02 builder, and expose a server-safe function `getTodayScheduleViewModel(now?: Date)` that returns `todayLabel`, `currentTimeLabel`, `sessions`, and `nextSession`. In `src/features/schedule/session-card.tsx`, render Stitch-style course cards with course title, time meta, learner/camp info, and exact state labels `即将开始`, `可入场`, `已开课，停止入场`, `已结束`; render the exact primary CTA `进入课堂` only for `open_for_entry`; wire that CTA with `next/link` to the exact path pattern `/lesson/${session.sessionId}` so homepage cards already own the homepage -> lesson route handoff before Plan 04. Non-open states must not navigate. Upcoming cards should surface both countdown text and a larger start-time emphasis inspired by the prototype. In `src/features/schedule/entry-countdown.tsx`, make a minimal client component that updates once per second without layout shift. In `src/app/(marketing)/page.tsx`, render a two-zone page: left navigation rail with brand `MyTurn` plus nav items `主页` and `设置`, and right main content area with exact heading `我的课堂` followed by a responsive session-card grid. The `设置` nav item is placeholder-only in Phase 1 and must not expand scope into a real settings page. Implement responsive rules explicitly: tablet/desktop keep the left rail and right content split, phone may collapse navigation into a compact top bar or drawer while preserving access to `主页` and `设置`, and desktop may expand outer whitespace without introducing new product panels. Add `test/unit/homepage-shell.test.tsx` as a render-level contract test that mocks or injects a stable schedule view-model and asserts the visible homepage contract: `MyTurn`, `主页`, `设置`, `我的课堂`, a rendered course-card grid, at least one `进入课堂` CTA for `open_for_entry`, and a locked-state string `已开课，停止入场` for an in-progress session. This task implements D-01, D-02, D-03, D-04, D-08, D-09, D-10 and addresses PLAT-01 for the homepage.</action>
  <acceptance_criteria>
    `src/app/(marketing)/page.tsx` contains `MyTurn`, `主页`, `设置`, and `我的课堂`.
    `src/features/schedule/session-card.tsx` contains the exact strings `进入课堂`, `已开课，停止入场`, `即将开始`, and `已结束`.
    `src/features/schedule/session-card.tsx` contains a `Link` or `href` targeting `/lesson/` for the `进入课堂` CTA.
    `src/app/(marketing)/page.tsx` contains grid layout markers such as `grid`, `lg:grid-cols-2`, or `xl:grid-cols-3` for the course-card layout.
    `src/features/schedule/get-today-schedule-view-model.ts` exports `getTodayScheduleViewModel`.
    `test/unit/homepage-shell.test.tsx` contains assertions for `我的课堂`, `主页`, `设置`, `进入课堂`, and `已开课，停止入场`.
    `src/app/(marketing)/page.tsx` contains responsive class markers for phone and tablet/desktop layouts such as `md:` or `lg:` for the left-rail + main-content adaptation.
    `src/components/ui/button.tsx`, `card.tsx`, and `badge.tsx` all exist.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const page=fs.readFileSync('src/app/(marketing)/page.tsx','utf8');for(const token of ['MyTurn','主页','设置','我的课堂']){if(!page.includes(token)) throw new Error('missing '+token)};if(!/(md:|lg:)/.test(page)) throw new Error('missing responsive markers');if(!/(grid|grid-cols|lg:grid-cols-2|xl:grid-cols-3)/.test(page)) throw new Error('missing grid layout');const card=fs.readFileSync('src/features/schedule/session-card.tsx','utf8');for(const token of ['进入课堂','已开课，停止入场','即将开始','已结束']){if(!card.includes(token)) throw new Error('missing '+token)};if(!(/href=\\{?`?['\\\"]?\\/lesson\\//.test(card)||card.includes('from \\x27next/link\\x27')||card.includes('from \"next/link\"'))) throw new Error('missing lesson link wiring');const vm=fs.readFileSync('src/features/schedule/get-today-schedule-view-model.ts','utf8');if(!vm.includes('getTodayScheduleViewModel')) throw new Error('missing view model export');const home=fs.readFileSync('test/unit/homepage-shell.test.tsx','utf8');for(const token of ['我的课堂','主页','设置','进入课堂','已开课，停止入场']){if(!home.includes(token)) throw new Error('missing homepage test token '+token)};for(const file of ['src/components/ui/button.tsx','src/components/ui/card.tsx','src/components/ui/badge.tsx','test/unit/homepage-shell.test.tsx']){if(!fs.existsSync(file)) throw new Error('missing '+file)}" && npx vitest run test/unit/homepage-shell.test.tsx</automated>
  </verify>
  <done>首页能基于真实 schedule view-model 渲染 Stitch 风格的课程卡片网格、可入场状态与课堂入口，并在 phone/tablet/desktop 上保持同一信息层级与课堂感。</done>
</task>

</tasks>

<verification>
执行 Task 1 的自动校验命令必须通过。随后在手机、平板横屏和桌面三个视口手动确认首页首屏是 Stitch 原型定义的 `我的课堂` 入口页：左侧轻导航、右侧课程卡片网格、无额外后台统计噪音。
</verification>

<success_criteria>
Phase 1 已具备稳定的课程入口页：用户能看到全天课表、识别当前能否入场，并在 Stitch 风格的 `我的课堂` 结构中于不同视口保持一致的信息优先级与课堂入口语义。
</success_criteria>

<output>
After completion, create `.planning/phases/01-classroom-shell/01-03-SUMMARY.md`
</output>
