---
phase: 01-classroom-shell
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/app/(marketing)/page.tsx
  - src/app/lesson/[sessionId]/page.tsx
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/lib/utils.ts
  - src/features/schedule/get-today-schedule-view-model.ts
  - src/features/schedule/entry-countdown.tsx
  - src/features/schedule/session-card.tsx
  - src/features/classroom-shell/classroom-shell.tsx
  - src/features/classroom-shell/student-seat-strip.tsx
  - src/features/classroom-shell/stage-panel.tsx
  - src/features/classroom-shell/teacher-panel.tsx
  - src/features/classroom-shell/lesson-board.tsx
  - test/unit/classroom-shell.test.tsx
  - test/e2e/classroom-entry.spec.ts
autonomous: true
requirements:
  - CLAS-01
  - PLAT-01
must_haves:
  truths:
    - "用户打开首页后，先看到的是今日课表、当前可否入场和下一节课时间，而不是通用 dashboard。"
    - "允许入场的课次提供明确的 `进入课堂` CTA；过了窗口则显示 `已开课，停止入场`。"
    - "进入课堂页后，顶部学生席、左侧课件主屏、右侧讲台区和教师区会同时出现，并在平板横屏下保持明确主次。"
  artifacts:
    - path: "src/app/(marketing)/page.tsx"
      provides: "今日课表首页"
    - path: "src/app/lesson/[sessionId]/page.tsx"
      provides: "课堂壳子页面"
    - path: "src/features/classroom-shell/classroom-shell.tsx"
      provides: "三段式课堂空间组件"
    - path: "test/unit/classroom-shell.test.tsx"
      provides: "PLAT-01 组件验证"
    - path: "test/e2e/classroom-entry.spec.ts"
      provides: "CLAS-01 冒烟验证"
  key_links:
    - from: "src/app/(marketing)/page.tsx"
      to: "src/features/schedule/get-today-schedule-view-model.ts"
      via: "server-side view model load"
      pattern: "getTodayScheduleViewModel"
    - from: "src/app/lesson/[sessionId]/page.tsx"
      to: "src/features/lesson-config/load-lesson.ts"
      via: "lessonId to lesson content"
      pattern: "loadLesson"
    - from: "test/e2e/classroom-entry.spec.ts"
      to: "src/app/lesson/[sessionId]/page.tsx"
      via: "enter class flow"
      pattern: "进入课堂"
---

<objective>
实现 Phase 1 的用户可见体验：今日课表入口、可入场课次 CTA，以及平板优先的课堂壳子页面。

Purpose: 把前两份 plan 产出的工程底座和内容 contracts 接成一个真实可打开、可进入、可观察的课堂场景。
Output: 首页课表路由、课堂路由、三段式课堂组件、倒计时交互、组件测试和可执行 E2E 冒烟测试。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@DESIGN.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-classroom-shell/01-CONTEXT.md
@.planning/phases/01-classroom-shell/01-UI-SPEC.md
@.planning/phases/01-classroom-shell/01-VALIDATION.md
@.planning/phases/01-classroom-shell/01-01-SUMMARY.md
@.planning/phases/01-classroom-shell/01-02-SUMMARY.md

<interfaces>
From `src/features/lesson-config/lesson-schema.ts`:
```ts
export type LessonStageId =
  | 'warmup'
  | 'repeat-after-teacher'
  | 'picture-talk'
  | 'wrap-up'

export interface LessonItem {
  id: string
  text: string
  imageSrc: string
  imageAlt: string
  imageWidth: number
  imageHeight: number
}
```

From `src/lib/time/session-access-state.ts`:
```ts
export type SessionAccessState =
  | 'upcoming'
  | 'open_for_entry'
  | 'in_progress_locked'
  | 'completed'
```

From `src/features/schedule/build-day-sessions.ts`:
```ts
export interface DaySessionViewModel {
  sessionId: string
  lessonId: string
  startsAt: string
  endsAt: string
  durationMinutes: number
  entryOpensAt: string
  accessState: SessionAccessState
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the schedule homepage and entry-state UI</name>
  <read_first>
    .planning/phases/01-classroom-shell/01-CONTEXT.md
    .planning/phases/01-classroom-shell/01-UI-SPEC.md
    .planning/phases/01-classroom-shell/01-02-SUMMARY.md
    src/app/(marketing)/page.tsx
    src/features/schedule/get-today-schedule-view-model.ts
    src/features/schedule/entry-countdown.tsx
    src/features/schedule/session-card.tsx
    src/components/ui/button.tsx
    src/components/ui/card.tsx
    src/components/ui/badge.tsx
    src/lib/time/session-access-state.ts
    content/schedules/default-weekday.ts
  </read_first>
  <files>src/app/(marketing)/page.tsx, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/badge.tsx, src/lib/utils.ts, src/features/schedule/get-today-schedule-view-model.ts, src/features/schedule/entry-countdown.tsx, src/features/schedule/session-card.tsx</files>
  <behavior>
    - Test 1: the homepage renders the full day schedule, not just one featured lesson.
    - Test 2: an `open_for_entry` session shows the CTA `进入课堂`; an `in_progress_locked` session shows `已开课，停止入场`.
    - Test 3: upcoming sessions display a concrete start time and countdown text.
  </behavior>
  <action>Build the homepage experience required by D-04 through D-10 and D-17 through D-23. Add `button`, `card`, and `badge` via shadcn/ui plus a `cn` helper in `src/lib/utils.ts`. In `src/features/schedule/get-today-schedule-view-model.ts`, read `content/schedules/default-weekday.ts`, build the day sessions using the Plan 02 builder, and expose a server-safe function `getTodayScheduleViewModel(now?: Date)` that returns `todayLabel`, `currentTimeLabel`, `sessions`, and `nextSession`. In `src/features/schedule/session-card.tsx`, render exact state labels: `即将开始`, `可入场`, `已开课，停止入场`, `已结束`; render the exact primary CTA `进入课堂` only for `open_for_entry`; render the next-lesson helper with exact time text. In `src/features/schedule/entry-countdown.tsx`, make a minimal client component that shows `距开场` and updates once per second without layout shift. In `src/app/(marketing)/page.tsx`, render a single-purpose schedule surface with three layers from UI-SPEC: top status region, middle full schedule list in chronological order, bottom next-session guidance. The page must keep the visual emphasis on “Today / 今日课表 / 下一节课” rather than dashboard metrics, implementing D-01, D-02, D-03, D-04, D-08, D-09 and D-10.</action>
  <acceptance_criteria>
    `src/app/(marketing)/page.tsx` contains `今日课表` and `下一节课`.
    `src/features/schedule/session-card.tsx` contains the exact strings `进入课堂`, `已开课，停止入场`, `即将开始`, and `已结束`.
    `src/features/schedule/entry-countdown.tsx` contains the exact label `距开场`.
    `src/features/schedule/get-today-schedule-view-model.ts` exports `getTodayScheduleViewModel`.
    `src/components/ui/button.tsx`, `card.tsx`, and `badge.tsx` all exist.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/unit/classroom-shell.test.tsx</automated>
  </verify>
  <done>首页能基于真实 schedule view-model 渲染全天课表、可入场状态和下一节引导，且 CTA/锁定规则不散落在页面里。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the classroom surface and activate automated verification</name>
  <read_first>
    .planning/phases/01-classroom-shell/01-CONTEXT.md
    .planning/phases/01-classroom-shell/01-UI-SPEC.md
    .planning/phases/01-classroom-shell/01-VALIDATION.md
    src/app/lesson/[sessionId]/page.tsx
    src/features/classroom-shell/classroom-shell.tsx
    src/features/classroom-shell/student-seat-strip.tsx
    src/features/classroom-shell/stage-panel.tsx
    src/features/classroom-shell/teacher-panel.tsx
    src/features/classroom-shell/lesson-board.tsx
    src/features/lesson-config/load-lesson.ts
    test/unit/classroom-shell.test.tsx
    test/e2e/classroom-entry.spec.ts
  </read_first>
  <files>src/app/lesson/[sessionId]/page.tsx, src/features/classroom-shell/classroom-shell.tsx, src/features/classroom-shell/student-seat-strip.tsx, src/features/classroom-shell/stage-panel.tsx, src/features/classroom-shell/teacher-panel.tsx, src/features/classroom-shell/lesson-board.tsx, test/unit/classroom-shell.test.tsx, test/e2e/classroom-entry.spec.ts</files>
  <behavior>
    - Test 1: the classroom page renders the student seat strip, lesson board, stage panel, and teacher panel together.
    - Test 2: the lesson board uses the seed lesson image and keeps stable alt text.
    - Test 3: the e2e flow can open `/`, click `进入课堂`, and land on `/lesson/{sessionId}`.
  </behavior>
  <action>Implement the tablet-first classroom shell defined by D-17 through D-27. In `src/features/classroom-shell/classroom-shell.tsx`, compose the three-part stage: top `StudentSeatStrip`, left `LessonBoard`, right stacked `StagePanel` and `TeacherPanel`. Use the exact seat lineup `You`, `Milo`, `Seat 3`, `Seat 4`, where `Seat 3` and `Seat 4` are visual atmosphere placeholders only per D-21. The `StagePanel` must render a default waiting state with exact copy `待上台` when no one is on stage yet per D-22. The `TeacherPanel` must render a fixed teacher identity block with exact label `Teacher Mia`. The `LessonBoard` must render the first lesson item image from `loadLesson(lessonId)` using the parsed local asset path and the item alt text. In `src/app/lesson/[sessionId]/page.tsx`, resolve the session from the day schedule, load its lesson, and render the classroom shell; if the session id is unknown, call `notFound()`. Add `test/unit/classroom-shell.test.tsx` that asserts the presence of `Teacher Mia`, `待上台`, `You`, `Milo`, and the lesson image alt text. Update `test/e2e/classroom-entry.spec.ts` by removing `test.skip`, navigating to `/`, clicking the exact CTA `进入课堂`, and asserting the URL matches `/lesson/` and the text `Teacher Mia` is visible. This task closes CLAS-01 and PLAT-01 while preserving the classroom-first feel from D-01, D-17, D-18, D-19, D-20, D-21, D-22 and D-23.</action>
  <acceptance_criteria>
    `src/features/classroom-shell/student-seat-strip.tsx` contains the exact strings `You`, `Milo`, `Seat 3`, and `Seat 4`.
    `src/features/classroom-shell/stage-panel.tsx` contains the exact string `待上台`.
    `src/features/classroom-shell/teacher-panel.tsx` contains the exact string `Teacher Mia`.
    `test/unit/classroom-shell.test.tsx` asserts `Teacher Mia` and `待上台`.
    `test/e2e/classroom-entry.spec.ts` no longer contains `test.skip(` and contains `getByRole('button', { name: '进入课堂' })`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/unit/classroom-shell.test.tsx && npx playwright test test/e2e/classroom-entry.spec.ts</automated>
  </verify>
  <done>用户可以从今日课表进入真实课堂页，且课堂页在平板优先布局中同时展示学生席、课件区、讲台区和教师区，自动化测试覆盖首页入口与课堂结构。</done>
</task>

</tasks>

<verification>
执行 `npx vitest run test/unit/classroom-shell.test.tsx && npx playwright test test/e2e/classroom-entry.spec.ts` 必须通过。随后手动在平板横屏视口确认首页首屏像“今天有课”，课堂页则像“正在准备进入一节课”，而不是工具页。
</verification>

<success_criteria>
Phase 1 的浏览器入口和课堂外壳已经打通：用户能看到全天课表、识别能否入场、进入一节 15 分钟课程，并在课堂页感受到明确的三段式小班课空间。
</success_criteria>

<output>
After completion, create `.planning/phases/01-classroom-shell/01-03-SUMMARY.md`
</output>
