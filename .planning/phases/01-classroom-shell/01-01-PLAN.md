---
phase: 01-classroom-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - components.json
  - src/app/layout.tsx
  - src/app/globals.css
  - vitest.config.ts
  - playwright.config.ts
  - test/setup.ts
  - test/unit/lesson-schema.test.ts
  - test/unit/classroom-shell.test.tsx
  - test/e2e/classroom-entry.spec.ts
autonomous: true
requirements:
  - PLAT-01
must_haves:
  truths:
    - "执行者可以直接启动一个面向课堂体验的 Next.js Web 壳层，而不是先补基础配置。"
    - "Phase 1 后续计划引用的三类测试入口都已经落盘，因此不会再出现验证命令先于测试文件存在的问题。"
    - "全局视觉 token 已锁定为平板横屏课堂语义，后续首页和课堂页可以直接复用同一套 light/dark 节奏与断点。"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "App Router root layout with classroom metadata and html shell"
    - path: "src/app/globals.css"
      provides: "课堂设计 token、Tailwind v4 theme 与全局基础样式"
    - path: "vitest.config.ts"
      provides: "unit/component 测试入口"
    - path: "playwright.config.ts"
      provides: "e2e 测试入口"
    - path: "test/unit/lesson-schema.test.ts"
      provides: "CONT-01 / CONT-02 的 Wave 0 测试占位"
    - path: "test/unit/classroom-shell.test.tsx"
      provides: "PLAT-01 的 Wave 0 组件测试占位"
    - path: "test/e2e/classroom-entry.spec.ts"
      provides: "CLAS-01 的 Wave 0 冒烟测试占位"
  key_links:
    - from: "package.json"
      to: "vitest.config.ts"
      via: "test:unit script"
      pattern: "\"test:unit\""
    - from: "package.json"
      to: "playwright.config.ts"
      via: "test:e2e script"
      pattern: "\"test:e2e\""
    - from: "test/setup.ts"
      to: "test/unit/classroom-shell.test.tsx"
      via: "jest-dom registration"
      pattern: "@testing-library/jest-dom/vitest"
---

<objective>
建立 Phase 1 的工程底座与 Wave 0 验证基线：Next.js App Router、全局设计 token、shadcn preset、以及所有下游 plan 需要的测试占位文件。

Purpose: 先把课堂 Web 壳层和验证入口一次性立住，避免后续 plans 再补基础设施或引用尚不存在的测试文件。
Output: 可运行的前端工程骨架、全局样式系统、Vitest/Playwright 配置，以及 unit/component/e2e 占位测试。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@DESIGN.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-classroom-shell/01-CONTEXT.md
@.planning/phases/01-classroom-shell/01-RESEARCH.md
@.planning/phases/01-classroom-shell/01-UI-SPEC.md
@.planning/phases/01-classroom-shell/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the web app shell and classroom design system</name>
  <read_first>
    AGENTS.md
    CLAUDE.md
    DESIGN.md
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/phases/01-classroom-shell/01-CONTEXT.md
    .planning/phases/01-classroom-shell/01-RESEARCH.md
    .planning/phases/01-classroom-shell/01-UI-SPEC.md
    package.json
    components.json
    src/app/layout.tsx
    src/app/globals.css
  </read_first>
  <files>package.json, package-lock.json, tsconfig.json, next.config.ts, postcss.config.mjs, components.json, src/app/layout.tsx, src/app/globals.css</files>
  <action>Initialize a Next.js 16.2.3 App Router project at repo root per D-02, D-17, D-28 and D-29 using exact dependency versions `next@16.2.3`, `react@19.2.5`, `react-dom@19.2.5`, `typescript@6.0.2`, `tailwindcss@4.2.2`, `@tailwindcss/postcss`, `postcss`, `zod@4.3.6`, `lucide-react`, `class-variance-authority`, `clsx`, and `tailwind-merge`. Add scripts `dev`, `build`, `start`, `lint`, `test:unit`, `test:e2e`. Create `components.json` with shadcn style `new-york`, `tsx: true`, `tailwind.css: src/app/globals.css`, `tailwind.baseColor: slate`, `aliases.components: @/components`, `aliases.lib: @/lib`, `aliases.utils: @/lib/utils`, `aliases.ui: @/components/ui`. Create `src/app/layout.tsx` with `lang="en"`, metadata title `MyTurn`, description `Tablet-first classroom shell for a 15-minute English class`, and a body class that uses the classroom font tokens. In `src/app/globals.css`, import Tailwind with `@import "tailwindcss";` and declare tokens that hard-code the approved visual contract: `--breakpoint-tablet: 64rem`, `--font-display`, `--font-body`, `--color-classroom-light: #FFFFFF`, `--color-classroom-muted: #F8FAFC`, `--color-classroom-dark: #111827`, `--color-classroom-ink: #111827`, `--color-classroom-accent: #28C76F`, `--color-classroom-accent-strong: #006A36`, `--radius-panel: 1.5rem`, `--radius-pill: 999px`, plus base body/background/text rules that make the app feel like a classroom surface instead of a dashboard per D-01, D-03, D-17, D-23, DESIGN.md Phase 1 overrides, and UI-SPEC `Responsive Rules`.</action>
  <acceptance_criteria>
    `package.json` contains the exact strings `"next": "16.2.3"`, `"react": "19.2.5"`, `"react-dom": "19.2.5"`, `"tailwindcss": "4.2.2"`, `"zod": "4.3.6"`.
    `package.json` contains scripts named `"test:unit"` and `"test:e2e"`.
    `components.json` contains `"style": "new-york"` and `"css": "src/app/globals.css"`.
    `src/app/layout.tsx` exports `metadata` and includes `<html lang="en">`.
    `src/app/globals.css` contains `@import "tailwindcss";`, `--breakpoint-tablet: 64rem;`, `--color-classroom-accent: #28C76F;`, `--color-classroom-accent-strong: #006A36;`, and `--radius-panel: 1.5rem;`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const pkg=require('./package.json');const need={next:'16.2.3',react:'19.2.5','react-dom':'19.2.5',tailwindcss:'4.2.2',zod:'4.3.6'};for(const [k,v] of Object.entries(need)){const got=(pkg.dependencies&&pkg.dependencies[k])||(pkg.devDependencies&&pkg.devDependencies[k]);if(got!==v){throw new Error(k+':'+got)}};if(!pkg.scripts['test:unit']||!pkg.scripts['test:e2e']) throw new Error('missing test scripts');const c=JSON.parse(fs.readFileSync('components.json','utf8'));if(c.style!=='new-york'||c.tailwind.css!=='src/app/globals.css') throw new Error('bad components.json');const css=fs.readFileSync('src/app/globals.css','utf8');for(const token of ['@import \"tailwindcss\";','--breakpoint-tablet: 64rem;','--color-classroom-accent: #28C76F;','--color-classroom-accent-strong: #006A36;','--radius-panel: 1.5rem;']){if(!css.includes(token)) throw new Error('missing '+token)}"</automated>
  </verify>
  <done>工程依赖、全局样式、root layout 和 shadcn preset 全部存在，后续计划可以直接在 `src/app`、`src/features`、`src/components/ui` 与 `test/` 上继续开发。</done>
</task>

<task type="auto">
  <name>Task 2: Establish Wave 0 test harness and placeholder specs</name>
  <read_first>
    .planning/phases/01-classroom-shell/01-RESEARCH.md
    .planning/phases/01-classroom-shell/01-VALIDATION.md
    package.json
    vitest.config.ts
    playwright.config.ts
    test/setup.ts
    test/unit/lesson-schema.test.ts
    test/unit/classroom-shell.test.tsx
    test/e2e/classroom-entry.spec.ts
  </read_first>
  <files>package.json, vitest.config.ts, playwright.config.ts, test/setup.ts, test/unit/lesson-schema.test.ts, test/unit/classroom-shell.test.tsx, test/e2e/classroom-entry.spec.ts</files>
  <action>Install the Phase 1 validation stack from research with exact devDependencies `vitest@4.1.4`, `@testing-library/react@16.3.2`, `@testing-library/jest-dom@6.9.1`, `playwright@1.59.1`, `typescript-eslint`, and any minimal Next-compatible test helpers required by official docs. Create `vitest.config.ts` that uses `environment: 'jsdom'`, `setupFiles: ['./test/setup.ts']`, and path alias `@` to `./src`. Create `test/setup.ts` that imports `@testing-library/jest-dom/vitest`. Create `playwright.config.ts` with `testDir: './test/e2e'`, `use.baseURL: 'http://127.0.0.1:3000'`, and a `webServer` command `npm run dev -- --hostname 127.0.0.1 --port 3000`. Add `test/unit/lesson-schema.test.ts` as a placeholder spec with `describe.skip('lesson schema contracts (Wave 0 placeholder)'` and TODO comments for exact checks on `5` items, required `imageSrc`/`imageAlt`, and access-state strings `upcoming`, `open_for_entry`, `in_progress_locked`, `completed`; include the exact comment `// Replace placeholder in 01-02 after lesson schema and seed content exist.`. Add `test/unit/classroom-shell.test.tsx` as a placeholder spec with `describe.skip('classroom shell layout (Wave 0 placeholder)'` and TODO comments for responsive assertions around tablet, phone, and desktop visibility; include the exact comment `// Replace placeholder in 01-04 after classroom shell and responsive layout exist.`. Add `test/e2e/classroom-entry.spec.ts` as a deliberate Wave 0 placeholder using `test.skip('home schedule allows lesson entry', async ({ page }) => { ... })` with assertions that target the future visible strings `今日课表` and `进入课堂`; include a top comment `// Remove test.skip in 01-04 after homepage, lesson-entry link, and lesson route exist.` so downstream verifies reference real files from the first wave onward.</action>
  <acceptance_criteria>
    `vitest.config.ts` contains `environment: 'jsdom'` and `setupFiles: ['./test/setup.ts']`.
    `test/setup.ts` contains `@testing-library/jest-dom/vitest`.
    `playwright.config.ts` contains `baseURL: 'http://127.0.0.1:3000'` and `npm run dev -- --hostname 127.0.0.1 --port 3000`.
    `test/unit/lesson-schema.test.ts` contains `describe.skip('lesson schema contracts (Wave 0 placeholder)'` and the exact comment `Replace placeholder in 01-02`.
    `test/unit/classroom-shell.test.tsx` contains `describe.skip('classroom shell layout (Wave 0 placeholder)'` and the exact comment `Replace placeholder in 01-04`.
    `test/e2e/classroom-entry.spec.ts` contains `test.skip('home schedule allows lesson entry'` and the exact comment `Remove test.skip in 01-04`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['vitest.config.ts','playwright.config.ts','test/setup.ts','test/unit/lesson-schema.test.ts','test/unit/classroom-shell.test.tsx','test/e2e/classroom-entry.spec.ts'];for(const file of files){if(!fs.existsSync(file)) throw new Error('missing '+file)};const v=fs.readFileSync('vitest.config.ts','utf8');if(!v.includes(\"environment: 'jsdom'\")||!v.includes(\"setupFiles: ['./test/setup.ts']\")) throw new Error('bad vitest config');const p=fs.readFileSync('playwright.config.ts','utf8');if(!p.includes(\"baseURL: 'http://127.0.0.1:3000'\")||!p.includes('npm run dev -- --hostname 127.0.0.1 --port 3000')) throw new Error('bad playwright config');const l=fs.readFileSync('test/unit/lesson-schema.test.ts','utf8');if(!l.includes(\"describe.skip('lesson schema contracts (Wave 0 placeholder)'\")||!l.includes('Replace placeholder in 01-02')) throw new Error('bad lesson placeholder');const c=fs.readFileSync('test/unit/classroom-shell.test.tsx','utf8');if(!c.includes(\"describe.skip('classroom shell layout (Wave 0 placeholder)'\")||!c.includes('Replace placeholder in 01-04')) throw new Error('bad classroom placeholder');const e=fs.readFileSync('test/e2e/classroom-entry.spec.ts','utf8');if(!e.includes(\"test.skip('home schedule allows lesson entry'\")||!e.includes('Remove test.skip in 01-04')) throw new Error('bad e2e placeholder')"</automated>
  </verify>
  <done>Wave 0 测试运行器和三类占位测试已经建立，后续 plan 的自动校验命令都只会引用已存在的测试文件。</done>
</task>

</tasks>

<verification>
运行 `npm install` 后，执行 Task 1 与 Task 2 的自动校验命令都应通过；此阶段不要求业务页面已完成，但要求下游所有验证文件已经存在且具备明确 handoff 注释。
</verification>

<success_criteria>
仓库拥有稳定的 App Router 工程骨架、Tailwind v4 课堂 token、shadcn new-york preset、Vitest/Playwright 配置，以及供 01-02/01-03/01-04 继续替换的 Wave 0 测试占位文件。
</success_criteria>

<output>
After completion, create `.planning/phases/01-classroom-shell/01-01-SUMMARY.md`
</output>
