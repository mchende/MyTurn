---
phase: 01-classroom-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/features/lesson-config/lesson-schema.ts
  - src/features/lesson-config/load-lesson.ts
  - src/features/schedule/schedule-schema.ts
  - src/features/schedule/build-day-sessions.ts
  - src/lib/time/session-access-state.ts
  - content/lessons/week-01/lesson-01.ts
  - content/schedules/default-weekday.ts
  - public/lessons/week-01/apple.svg
  - public/lessons/week-01/banana.svg
  - public/lessons/week-01/cat.svg
  - public/lessons/week-01/dog.svg
  - public/lessons/week-01/sun.svg
  - test/unit/lesson-schema.test.ts
autonomous: true
requirements:
  - CONT-01
  - CONT-02
must_haves:
  truths:
    - "一节课的配置可以稳定表达 5 个目标项和固定课堂阶段，而不是散落在组件里的临时对象。"
    - "每个目标项都绑定了图片路径和图片替代文本，后续课堂主屏可以直接消费。"
    - "今日课表的时间模板与入场状态计算已经有独立 contracts，首页和课堂路由不需要自己拼规则。"
  artifacts:
    - path: "src/features/lesson-config/lesson-schema.ts"
      provides: "Lesson、LessonItem、LessonStage 的 Zod schema 与导出类型"
    - path: "src/features/schedule/schedule-schema.ts"
      provides: "课表模板与会话实例 schema"
    - path: "content/lessons/week-01/lesson-01.ts"
      provides: "5 个目标项和固定课堂阶段的种子课程"
    - path: "test/unit/lesson-schema.test.ts"
      provides: "CONT-01 / CONT-02 自动验证"
  key_links:
    - from: "content/lessons/week-01/lesson-01.ts"
      to: "src/features/lesson-config/lesson-schema.ts"
      via: "lessonSchema.parse"
      pattern: "lessonSchema.parse"
    - from: "content/schedules/default-weekday.ts"
      to: "src/features/schedule/schedule-schema.ts"
      via: "scheduleTemplateSchema.parse"
      pattern: "scheduleTemplateSchema.parse"
    - from: "src/lib/time/session-access-state.ts"
      to: "src/features/schedule/build-day-sessions.ts"
      via: "SessionAccessState selector"
      pattern: "open_for_entry"
---

<objective>
把课程内容和课表规则建模为可校验 contracts，并提供一周课程的本地种子数据与图片资产。

Purpose: Phase 1 的首页和课堂页需要真实数据骨架，而不是把 5 个目标项和时间规则硬编码在 JSX 里。
Output: lesson/schedule schema、时间状态 selector、本地 seed 内容、图片占位资源和可执行 unit test。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@DESIGN.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-classroom-shell/01-CONTEXT.md
@.planning/phases/01-classroom-shell/01-RESEARCH.md
@.planning/phases/01-classroom-shell/01-VALIDATION.md
@.planning/phases/01-classroom-shell/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define lesson, schedule, and access-state contracts</name>
  <read_first>
    .planning/phases/01-classroom-shell/01-CONTEXT.md
    .planning/phases/01-classroom-shell/01-RESEARCH.md
    .planning/phases/01-classroom-shell/01-VALIDATION.md
    test/unit/lesson-schema.test.ts
    src/features/lesson-config/lesson-schema.ts
    src/features/lesson-config/load-lesson.ts
    src/features/schedule/schedule-schema.ts
    src/features/schedule/build-day-sessions.ts
    src/lib/time/session-access-state.ts
  </read_first>
  <files>src/features/lesson-config/lesson-schema.ts, src/features/lesson-config/load-lesson.ts, src/features/schedule/schedule-schema.ts, src/features/schedule/build-day-sessions.ts, src/lib/time/session-access-state.ts, test/unit/lesson-schema.test.ts</files>
  <behavior>
    - Test 1: `lessonSchema` only accepts exactly 5 `items`.
    - Test 2: each `LessonItem` requires `imageSrc` and `imageAlt`.
    - Test 3: schedule templates produce session states `upcoming`, `open_for_entry`, `in_progress_locked`, and `completed` from time inputs.
  </behavior>
  <action>Replace the Wave 0 placeholder in `test/unit/lesson-schema.test.ts` first, then implement the content contracts required by D-05 through D-16. In `src/features/lesson-config/lesson-schema.ts`, export `lessonItemSchema`, `lessonStageSchema`, and `lessonSchema` plus inferred types. Use the exact stage id enum `['warmup','repeat-after-teacher','picture-talk','wrap-up']`. `lessonItemSchema` must require `id`, `text`, `imageSrc`, `imageAlt`, `imageWidth`, and `imageHeight`. `lessonSchema` must require `items` length exactly `5`, `weekKey`, `lessonId`, `title`, and `stages`. In `src/features/schedule/schedule-schema.ts`, export `timeSlotTemplateSchema` and `scheduleTemplateSchema`; each slot must include `sessionId`, `startTime`, `durationMinutes`, `entryOpensMinutesBeforeStart`, and `lessonId`. In `src/lib/time/session-access-state.ts`, export the exact union type `SessionAccessState = 'upcoming' | 'open_for_entry' | 'in_progress_locked' | 'completed'` plus a pure function `getSessionAccessState({ now, startTime, durationMinutes, entryOpensMinutesBeforeStart })`. In `src/features/schedule/build-day-sessions.ts`, export a pure builder that combines the schedule template with `getSessionAccessState` and returns view-model entries containing `sessionId`, `lessonId`, `startsAt`, `endsAt`, `durationMinutes`, `entryOpensAt`, and `accessState`. In `src/features/lesson-config/load-lesson.ts`, export a `loadLesson(lessonId: string)` function that loads the local seed map and validates it with `lessonSchema.parse(...)`. This task implements D-11, D-12, D-13, D-14, D-15 and D-16 without adding teacher scripting or scoring fields.</action>
  <acceptance_criteria>
    `src/features/lesson-config/lesson-schema.ts` contains `z.enum(['warmup','repeat-after-teacher','picture-talk','wrap-up'])`.
    `src/features/lesson-config/lesson-schema.ts` contains `.length(5)` on the lesson items array.
    `src/features/lesson-config/lesson-schema.ts` contains both `imageSrc` and `imageAlt`.
    `src/lib/time/session-access-state.ts` exports the exact string union `open_for_entry` and `in_progress_locked`.
    `src/features/schedule/build-day-sessions.ts` returns fields named `entryOpensAt` and `accessState`.
    `test/unit/lesson-schema.test.ts` no longer contains `Wave 0 placeholder`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/unit/lesson-schema.test.ts</automated>
  </verify>
  <done>课程 contract、课表 contract 与入场状态 selector 全部独立存在，后续页面层只消费这些导出，不再自行定义阶段枚举或时间状态。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed one reusable weekly lesson package with assets</name>
  <read_first>
    .planning/phases/01-classroom-shell/01-CONTEXT.md
    .planning/phases/01-classroom-shell/01-RESEARCH.md
    src/features/lesson-config/lesson-schema.ts
    src/features/schedule/schedule-schema.ts
    content/lessons/week-01/lesson-01.ts
    content/schedules/default-weekday.ts
    public/lessons/week-01/apple.svg
    public/lessons/week-01/banana.svg
    public/lessons/week-01/cat.svg
    public/lessons/week-01/dog.svg
    public/lessons/week-01/sun.svg
    test/unit/lesson-schema.test.ts
  </read_first>
  <files>content/lessons/week-01/lesson-01.ts, content/schedules/default-weekday.ts, public/lessons/week-01/apple.svg, public/lessons/week-01/banana.svg, public/lessons/week-01/cat.svg, public/lessons/week-01/dog.svg, public/lessons/week-01/sun.svg, test/unit/lesson-schema.test.ts</files>
  <behavior>
    - Test 1: the seed lesson parses successfully and exposes 5 items.
    - Test 2: every seed item has a local `/lessons/week-01/*.svg` image path and non-empty alt text.
    - Test 3: the weekday schedule template exposes 15-minute sessions and a configurable entry-open window before start.
  </behavior>
  <action>Create a concrete weekly lesson package that later plans can render without guessing values. In `content/lessons/week-01/lesson-01.ts`, export one lesson object parsed by `lessonSchema.parse(...)` with `lessonId: 'week-01-lesson-01'`, `weekKey: 'week-01'`, title `Week 1 · Everyday Nouns`, five items exactly: `apple`, `banana`, `cat`, `dog`, `sun`, and image paths `/lessons/week-01/apple.svg`, `/lessons/week-01/banana.svg`, `/lessons/week-01/cat.svg`, `/lessons/week-01/dog.svg`, `/lessons/week-01/sun.svg`. Stages must reference those item ids across all four fixed stages so the same content repeats per D-13. In `content/schedules/default-weekday.ts`, export one parsed schedule template with at least three sessions: `weekday-1000`, `weekday-1400`, `weekday-1930`; each has `durationMinutes: 15` and `entryOpensMinutesBeforeStart: 5` to satisfy D-06, D-07, D-08, D-09 and D-10. Create five simple local SVGs under `public/lessons/week-01/` with visible subject labels so `next/image` can render stable assets in later plans. Extend `test/unit/lesson-schema.test.ts` with assertions for exact item count `5`, required image fields, valid stage ids, and access-state outcomes for `upcoming`, `open_for_entry`, `in_progress_locked`, and `completed` time samples.</action>
  <acceptance_criteria>
    `content/lessons/week-01/lesson-01.ts` contains the exact ids `apple`, `banana`, `cat`, `dog`, `sun`.
    `content/lessons/week-01/lesson-01.ts` contains `lessonSchema.parse`.
    `content/schedules/default-weekday.ts` contains `durationMinutes: 15` and `entryOpensMinutesBeforeStart: 5`.
    Files `public/lessons/week-01/apple.svg`, `banana.svg`, `cat.svg`, `dog.svg`, `sun.svg` all exist.
    `test/unit/lesson-schema.test.ts` contains the exact access-state strings `upcoming`, `open_for_entry`, `in_progress_locked`, and `completed`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/unit/lesson-schema.test.ts</automated>
  </verify>
  <done>Phase 1 已有一周课程种子数据、可复用的时间模板和稳定图片资源，且 CONT-01 / CONT-02 由自动化测试直接覆盖。</done>
</task>

</tasks>

<verification>
执行 `npx vitest run test/unit/lesson-schema.test.ts` 必须通过，并证明 lesson schema、图片绑定、15 分钟时长与入场状态都符合约定。
</verification>

<success_criteria>
课程内容层与时间模板层已经独立成型：页面层未来只需要消费 `loadLesson` 和日程 builder，不再重写 5 个目标项、阶段结构或状态规则。
</success_criteria>

<output>
After completion, create `.planning/phases/01-classroom-shell/01-02-SUMMARY.md`
</output>
