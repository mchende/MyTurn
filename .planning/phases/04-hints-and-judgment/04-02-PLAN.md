---
phase: 04-hints-and-judgment
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - package.json
  - package-lock.json
  - src/features/lesson-config/lesson-schema.ts
  - content/lessons/week-01/lesson-01.ts
  - src/features/classroom-shell/classroom-judgment.ts
  - src/features/classroom-shell/classroom-orchestrator.ts
  - src/features/classroom-shell/use-classroom-orchestrator.ts
  - test/unit/classroom-judgment.test.ts
  - test/unit/lesson-schema.test.ts
  - test/unit/classroom-orchestrator.test.ts
autonomous: true
requirements:
  - SPKG-03
  - SPKG-04
  - SPKG-05
must_haves:
  truths:
    - "复述轮不再是“只要点按钮就通过”，而是按短文本近似匹配规则判断 pass / retry / fallback。"
    - "看图轮的通过标准来自 lesson content 中作者提供的语义 accept 集，而不是 reducer 写死词表或在线模型。"
    - "判断结果只返回 `pass / retry / fallback` 三态，不会在儿童界面出现分数、错误码或答案泄露。"
  artifacts:
    - path: "src/features/classroom-shell/classroom-judgment.ts"
      provides: "repeat lexical closeness 与 picture semantic accepts 的纯函数 judgment adapter"
      exports:
        - "judgeStudentAttempt"
        - "normalizeStudentTranscript"
        - "buildCanonicalManualTranscript"
    - path: "src/features/lesson-config/lesson-schema.ts"
      provides: "Phase 4 的可选 lesson hint/judgment metadata contract"
      exports:
        - "lessonItemSchema"
        - "lessonSchema"
    - path: "content/lessons/week-01/lesson-01.ts"
      provides: "week-01 lesson 的 repeatAccepts / pictureTalk metadata seed"
    - path: "test/unit/classroom-judgment.test.ts"
      provides: "Wave 0 judgment threshold 与 outcome contract 测试"
  key_links:
    - from: "src/features/classroom-shell/classroom-judgment.ts"
      to: "src/features/lesson-config/lesson-schema.ts"
      via: "`repeatAccepts` 和 `pictureTalk.semanticAccepts` 驱动判断"
      pattern: "repeatAccepts|semanticAccepts|fallbackModel"
    - from: "src/features/classroom-shell/use-classroom-orchestrator.ts"
      to: "src/features/classroom-shell/classroom-orchestrator.ts"
      via: "`submitStudentAttempt` / compatibility alias dispatch `student_attempt_submitted`"
      pattern: "submitStudentAttempt|student_attempt_submitted"
    - from: "test/unit/classroom-judgment.test.ts"
      to: "src/features/classroom-shell/classroom-judgment.ts"
      via: "pure unit sample cases"
      pattern: "pass|retry|fallback"
---

<objective>
为 Phase 4 建立真正的 judgment contract：把 repeat/picture 的通过标准从 UI 按钮里拆出来，落到可测试的纯函数和可配置的 lesson metadata 上。

Purpose: 如果 `04-02` 不把判断与内容合同分层，`04-03` 的 fallback close-out 只能继续依赖 reducer 中的硬编码猜测，后续接语音时也会整层返工。
Output: `classroom-judgment.ts`、升级后的 lesson schema/seed、Wave 0 judgment/schema tests，以及 reducer/hook 对 judgment adapter 的接线。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hints-and-judgment/04-CONTEXT.md
@.planning/phases/04-hints-and-judgment/04-RESEARCH.md
@.planning/phases/04-hints-and-judgment/04-VALIDATION.md
@.planning/phases/04-hints-and-judgment/04-01-PLAN.md
@src/features/lesson-config/lesson-schema.ts
@content/lessons/week-01/lesson-01.ts
@src/features/classroom-shell/classroom-orchestrator.ts
@src/features/classroom-shell/use-classroom-orchestrator.ts
@test/unit/lesson-schema.test.ts
@test/unit/classroom-orchestrator.test.ts

<interfaces>
From src/features/lesson-config/lesson-schema.ts:
```ts
export const lessonItemSchema = z.object({
  id: z.string().min(1),
  text: z.string().min(1),
  imageSrc: z.string().min(1),
  imageAlt: z.string().min(1),
  imageWidth: z.number().int().positive(),
  imageHeight: z.number().int().positive(),
});
```

Recommended Phase 04 extension from research:
```ts
const lessonItemJudgmentSchema = z.object({
  repeatAccepts: z.array(z.string().min(1)).min(1).optional(),
  pictureTalk: z
    .object({
      observeHint: z.string().min(1),
      narrowedQuestion: z.string().min(1),
      semanticAccepts: z.array(z.string().min(1)).min(1),
      fallbackModel: z.string().min(1),
    })
    .optional(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先补 Wave 0 judgment/schema 测试护栏</name>
  <files>test/unit/classroom-judgment.test.ts, test/unit/lesson-schema.test.ts, test/unit/classroom-orchestrator.test.ts</files>
  <read_first>
    .planning/phases/04-hints-and-judgment/04-RESEARCH.md
    .planning/phases/04-hints-and-judgment/04-VALIDATION.md
    src/features/lesson-config/lesson-schema.ts
    content/lessons/week-01/lesson-01.ts
    test/unit/lesson-schema.test.ts
    test/unit/classroom-orchestrator.test.ts
  </read_first>
  <behavior>
    - Test 1: `classroom-judgment.test.ts` 必须覆盖 repeat lexical 样本集：`apple -> apple` pass、`apple -> aple` pass、`apple -> banana` 在第一次输出为 retry、第二次输出为 fallback。
    - Test 2: `classroom-judgment.test.ts` 必须覆盖 picture semantic 样本集：`apple` item 的 `an apple` 与 `red apple` pass，`banana` 第一次 retry、第二次 fallback。
    - Test 3: `lesson-schema.test.ts` 必须断言 lesson item 可选接受 `repeatAccepts` 与 `pictureTalk.observeHint / narrowedQuestion / semanticAccepts / fallbackModel`，且 parser 不会要求所有旧 lesson 立即补齐字段。
  </behavior>
  <action>先创建新的 `test/unit/classroom-judgment.test.ts`，再扩展 `test/unit/lesson-schema.test.ts` 与 `test/unit/classroom-orchestrator.test.ts`。测试文件中直接写死 Phase 4 样本，不要留“以后再补”的 TODO：repeat 至少锁住 `apple` / `banana` / `sun` 三组 canonical + near miss；picture 至少锁住 `apple` 的 `semanticAccepts` 与 fallback outcome；结果类型只允许 `pass`、`retry`、`fallback` 三态，不要断言任何 score、confidence badge、error code 或 UI-facing correction label。`classroom-orchestrator.test.ts` 需要额外增加一组 wiring expectation，证明 reducer 消费的是 `student_attempt_submitted` / judgment outcome，而不是把 `student_participation_confirmed` 直接当成功。</action>
  <verify>
    <automated>MISSING — Wave 0 must create test/unit/classroom-judgment.test.ts first, then run npm run test:unit -- test/unit/classroom-judgment.test.ts test/unit/lesson-schema.test.ts test/unit/classroom-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 仓库新增 `test/unit/classroom-judgment.test.ts`，且文件内可 grep 到 `apple`, `aple`, `retry`, `fallback`。
    - `test/unit/lesson-schema.test.ts` 可 grep 到 `repeatAccepts`, `observeHint`, `narrowedQuestion`, `semanticAccepts`, `fallbackModel`。
    - `test/unit/classroom-orchestrator.test.ts` 可 grep 到 `student_attempt_submitted`，不再只覆盖 `student_participation_confirmed` 直通成功。
  </acceptance_criteria>
  <done>Wave 0 测试先把 judgment 和 schema 合同钉死，后续实现不会悄悄退回“点按钮即过题”。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 lesson metadata contract 并补齐 week-01 种子内容</name>
  <files>src/features/lesson-config/lesson-schema.ts, content/lessons/week-01/lesson-01.ts, test/unit/lesson-schema.test.ts</files>
  <read_first>
    .planning/phases/04-hints-and-judgment/04-CONTEXT.md
    .planning/phases/04-hints-and-judgment/04-RESEARCH.md
    src/features/lesson-config/lesson-schema.ts
    content/lessons/week-01/lesson-01.ts
    test/unit/lesson-schema.test.ts
  </read_first>
  <behavior>
    - Test 1: schema 接受可选 `repeatAccepts`，用于 repeat lexical matching；未提供时仍回退到 `item.text` canonical。
    - Test 2: schema 接受可选 `pictureTalk` 对象，字段必须完整包括 `observeHint`、`narrowedQuestion`、`semanticAccepts`、`fallbackModel`。
    - Test 3: `week-01-lesson-01` 的五个 item 都补齐 Phase 4 metadata，且不会改变原有图片、item 顺序或 stage 顺序。
  </behavior>
  <action>修改 `src/features/lesson-config/lesson-schema.ts`，新增 `lessonItemJudgmentSchema` 并把它 merge 到 `lessonItemSchema`。字段定义必须明确为：`repeatAccepts?: string[]`；`pictureTalk?: { observeHint: string; narrowedQuestion: string; semanticAccepts: string[]; fallbackModel: string; }`。随后更新 `content/lessons/week-01/lesson-01.ts` 的五个 noun items：`repeatAccepts` 至少包含 canonical 与 article form，例如 `apple -> ['apple', 'an apple']`、`banana -> ['banana', 'a banana']`、`cat -> ['cat', 'a cat']`、`dog -> ['dog', 'a dog']`、`sun -> ['sun', 'the sun']`；每个 item 的 `pictureTalk` 都要写入具体 `observeHint`、二选一 `narrowedQuestion`、`semanticAccepts` 和 declarative `fallbackModel`，不要用占位字符串、不要把答案泄露到 child-facing visible copy 字段里，因为这些元数据只供 judgment/script 使用。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/lesson-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lesson-schema.ts` 可 grep 到 `repeatAccepts`、`pictureTalk`、`observeHint`、`narrowedQuestion`、`semanticAccepts`、`fallbackModel`。
    - `content/lessons/week-01/lesson-01.ts` 的五个 item 都包含 `repeatAccepts`，且至少 `apple` item 包含 `semanticAccepts: ['apple', 'an apple', 'red apple']` 或等价集合。
    - `lesson-schema.test.ts` 继续断言五个 item、固定 stages 和本地图片资产合同未破坏。
  </acceptance_criteria>
  <done>判断所需的 hint/judgment 数据已经进入内容层，后续 reducer 和 script 不必再硬编码 item 级英文。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现 judgment adapter 并把 hook/reducer 从确认按钮切到 judged attempt</name>
  <files>package.json, package-lock.json, src/features/classroom-shell/classroom-judgment.ts, src/features/classroom-shell/classroom-orchestrator.ts, src/features/classroom-shell/use-classroom-orchestrator.ts, test/unit/classroom-judgment.test.ts, test/unit/classroom-orchestrator.test.ts</files>
  <read_first>
    package.json
    src/features/classroom-shell/classroom-orchestrator.ts
    src/features/classroom-shell/use-classroom-orchestrator.ts
    src/features/lesson-config/lesson-schema.ts
    content/lessons/week-01/lesson-01.ts
    test/unit/classroom-judgment.test.ts
    test/unit/classroom-orchestrator.test.ts
    .planning/phases/04-hints-and-judgment/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `judgeStudentAttempt` 对 repeat 使用规范化文本 + `fastest-levenshtein` 距离阈值，长度 1-4 允许 1 个编辑距离、5-8 允许 2 个、9+ 允许 3 个。
    - Test 2: `judgeStudentAttempt` 对 picture 只看 `semanticAccepts` 集合命中；不命中时第一次给 retry，第二次给 fallback。
    - Test 3: reducer 在 `student_attempt_submitted` 时消费 judgment outcome：`pass -> teacher_feedback`，`retry -> teacher_encourage`，`fallback ->` 为 `04-03` 预留 fallback transition；兼容 alias `confirmStudentParticipation()` 必须通过 `submitStudentAttempt()` 走同一条 judgment 路径。
  </behavior>
  <action>先安装 `fastest-levenshtein` 并更新 `package.json` / `package-lock.json`。新增 `src/features/classroom-shell/classroom-judgment.ts`，至少导出 `normalizeStudentTranscript(text: string | null | undefined)`、`buildCanonicalManualTranscript({ currentItem, stageId })`、`judgeStudentAttempt({ lessonItem, stageId, transcript, attemptIndex })`。`normalizeStudentTranscript` 要做 lowercase、trim、punctuation strip、collapse whitespace；repeat 判断优先比对 `repeatAccepts`，无则回退 `item.text`；picture 判断只比对 `pictureTalk.semanticAccepts`。结果类型固定为 `{ outcome: 'pass' | 'retry' | 'fallback'; normalizedTranscript: string; matchedAgainst: string | null; }`。随后改 `classroom-orchestrator.ts` 与 `use-classroom-orchestrator.ts`：新增 `student_attempt_submitted` 事件和 `submitStudentAttempt(input?: { transcript?: string | null; source?: 'manual' | 'mock_transcript' | 'future_asr'; })` hook API；移除 reducer 中“`student_participation_confirmed` 直接成功”的主路径。为了保住现有 `classroom-shell.tsx`，保留 `confirmStudentParticipation()` 作为兼容别名，但它必须调用 `submitStudentAttempt({ source: 'manual', transcript: buildCanonicalManualTranscript(...) })`，不能绕过 judgment adapter。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-judgment.test.ts test/unit/lesson-schema.test.ts test/unit/classroom-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` 出现 `fastest-levenshtein` dependency。
    - `src/features/classroom-shell/classroom-judgment.ts` 可 grep 到 `judgeStudentAttempt`, `normalizeStudentTranscript`, `outcome: 'pass' | 'retry' | 'fallback'`。
    - `use-classroom-orchestrator.ts` 可 grep 到 `submitStudentAttempt` 与 `confirmStudentParticipation` 兼容别名。
    - reducer 不再把 `student_participation_confirmed` 直接映射为 `teacher_feedback`。
  </acceptance_criteria>
  <done>Phase 4 的判断逻辑已经从 UI 手势中剥离，未来接 mock transcript 或真实 ASR 时不需要再重写 orchestrator 主干。</done>
</task>

</tasks>

<verification>
这一份计划的通过标准是“判断合同存在且可测”，不是“马上做出完美 ASR”。如果实现里出现 score、confidence label 或 child-facing answer leakage，直接视为偏离 SPKG-05。
</verification>

<success_criteria>
- repeat 近似匹配与 picture 语义匹配都有独立纯函数判断，且受测试保护。
- lesson content 已显式携带 Phase 4 所需的 hint/judgment metadata。
- reducer/hook 已改为消费 judged attempt，而不是把按钮点击硬编码成成功。
</success_criteria>

<output>
After completion, create `.planning/phases/04-hints-and-judgment/04-02-SUMMARY.md`
</output>
