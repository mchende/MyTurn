---
phase: 04-hints-and-judgment
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/features/classroom-shell/classroom-orchestrator.ts
  - src/features/classroom-shell/use-classroom-orchestrator.ts
  - src/features/classroom-shell/teacher-script.ts
  - src/features/classroom-shell/podium-view-model.ts
  - test/unit/classroom-orchestrator.test.ts
  - test/unit/teacher-script.test.ts
  - test/unit/classroom-shell.test.tsx
  - test/e2e/guided-speaking-flow.spec.ts
autonomous: true
requirements:
  - TEAC-04
  - SPKG-05
must_haves:
  truths:
    - "如果孩子在轻提示和第二次机会之后仍接不上，老师会先给出 fallback model，再保留一次最低门槛的 final follow 开口机会，然后才切到下一题。"
    - "repeat 与 picture 两个 stage 都能完成 teacher-owned 的 fallback close-out，但 Bobby 仍然只在 repeat 的 `ai_model` 出现。"
    - "fallback 后课堂仍保持单 CTA、英文优先、无评分 UI、无答案直出到 child-facing copy，Phase 3 的 repeat -> picture 主路径也不会被打断。"
  artifacts:
    - path: "src/features/classroom-shell/classroom-orchestrator.ts"
      provides: "`teacher_fallback_model` + final-follow close-out phase graph"
    - path: "src/features/classroom-shell/teacher-script.ts"
      provides: "repeat/picture fallback spokenModel 与 child-safe visibleCaption 合同"
    - path: "test/unit/classroom-shell.test.tsx"
      provides: "fallback 后仍保留单 CTA 与无答案泄露的 shell 回归"
    - path: "test/e2e/guided-speaking-flow.spec.ts"
      provides: "baseline repeat->picture + fallback branch 的 browser regression gate"
  key_links:
    - from: "src/features/classroom-shell/classroom-orchestrator.ts"
      to: "src/features/classroom-shell/classroom-judgment.ts"
      via: "`fallback` outcome or second failure drives `teacher_fallback_model`"
      pattern: "teacher_fallback_model|fallback"
    - from: "src/features/classroom-shell/podium-view-model.ts"
      to: "src/features/classroom-shell/classroom-shell.tsx"
      via: "single CTA label switches to final-follow copy during fallback"
      pattern: "I said it with Cora|teacher_echo"
    - from: "test/e2e/guided-speaking-flow.spec.ts"
      to: "src/app/lesson/[sessionId]/page.tsx"
      via: "browser fallback path without breaking baseline progression"
      pattern: "I said it|I answered|I said it with Cora"
---

<objective>
完成 Phase 4 的 close-out：把老师最终兜底和孩子最后一次开口机会做实，并用 focused unit/e2e 回归证明这条链路没有破坏课堂感。

Purpose: `04-01` 和 `04-02` 解决了 light hint 与 judgment contract，但如果没有最终 fallback close-out，Phase 4 仍然不能满足 TEAC-04，也无法证明“信心优先而不是过题优先”真的落地。
Output: fallback state graph、teacher fallback/final-follow script、单 CTA fallback regression，以及保留 Phase 3 baseline 的 Playwright smoke。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hints-and-judgment/04-CONTEXT.md
@.planning/phases/04-hints-and-judgment/04-RESEARCH.md
@.planning/phases/04-hints-and-judgment/04-VALIDATION.md
@.planning/phases/04-hints-and-judgment/04-01-PLAN.md
@.planning/phases/04-hints-and-judgment/04-02-PLAN.md
@src/features/classroom-shell/classroom-orchestrator.ts
@src/features/classroom-shell/use-classroom-orchestrator.ts
@src/features/classroom-shell/teacher-script.ts
@src/features/classroom-shell/podium-view-model.ts
@test/unit/classroom-orchestrator.test.ts
@test/unit/teacher-script.test.ts
@test/unit/classroom-shell.test.tsx
@test/e2e/guided-speaking-flow.spec.ts

<interfaces>
From src/features/classroom-shell/classroom-judgment.ts (from 04-02):
```ts
export function judgeStudentAttempt(input: {
  lessonItem: LessonItem;
  stageId: GuidedStageId;
  transcript?: string | null;
  attemptIndex: number;
}): {
  outcome: 'pass' | 'retry' | 'fallback';
  normalizedTranscript: string;
  matchedAgainst: string | null;
};
```

From src/features/classroom-shell/podium-view-model.ts:
```ts
export type PodiumViewModel = {
  confirmationButtonLabel: string | null;
  showConfirmationButton: boolean;
  podiumCaption: string;
  podiumStatus: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 repeat/picture 的 fallback model + final follow close-out</name>
  <files>src/features/classroom-shell/classroom-orchestrator.ts, src/features/classroom-shell/use-classroom-orchestrator.ts, src/features/classroom-shell/teacher-script.ts, src/features/classroom-shell/podium-view-model.ts, test/unit/classroom-orchestrator.test.ts, test/unit/teacher-script.test.ts</files>
  <read_first>
    .planning/phases/04-hints-and-judgment/04-CONTEXT.md
    .planning/phases/04-hints-and-judgment/04-RESEARCH.md
    src/features/classroom-shell/classroom-orchestrator.ts
    src/features/classroom-shell/use-classroom-orchestrator.ts
    src/features/classroom-shell/teacher-script.ts
    src/features/classroom-shell/podium-view-model.ts
    test/unit/classroom-orchestrator.test.ts
    test/unit/teacher-script.test.ts
  </read_first>
  <behavior>
    - Test 1: per D-02/D-10，第二次失败或 judgment outcome `fallback` 时，reducer 必须进入新 phase `teacher_fallback_model`，而不是直接 `move_next`。
    - Test 2: per D-02/D-11，`teacher_fallback_model` timer 结束后进入 final follow phase，并重新给孩子一次最低门槛 CTA 机会；按钮 label 固定为 `I said it with Cora`。
    - Test 3: per SPKG-05，repeat fallback 的 target text 与 picture fallback 的 `fallbackModel` 只能出现在 `spokenModel` / debug channel，不得出现在 `visibleCaption`、podium status 或 board 文案里。
  </behavior>
  <action>先在 reducer/script 单测写失败用例，再改实现。具体要求：1) 在 `ClassroomOrchestratorPhase` 中新增 `teacher_fallback_model`，并把现有 `teacher_echo` 统一收敛为 final follow phase；2) 当 repeat/picture 在第二次 learner attempt 后仍收到 `fallback` outcome，或 timer-based second failure 明确需要 close-out 时，reducer 进入 `teacher_fallback_model`；3) `phase_timer_completed` 从 `teacher_fallback_model` 必须转到 `teacher_echo`，此时 `activeSeat: 'me'`、`activeSpeaker: 'student'`、`showConfirmationButton: true`，CTA label 统一为 `I said it with Cora`；4) 在 `teacher_echo`（final follow）中点击 CTA 或等待该 phase 定时结束都应直接进入 `move_next`，不要再额外增加奖励面板或第二个按钮；5) `teacher-script.ts` 中 repeat fallback 的 `spokenModel` 使用 `currentItem.text` / `repeatAccepts[0]`，picture fallback 的 `spokenModel` 使用 `currentItem.pictureTalk.fallbackModel`，`visibleCaption` 统一保持 child-safe，例如 `Listen once more. Then say it with me.` / `Say it with Cora, then we go on.`；6) `podium-view-model.ts` 仅在 final follow phase 切换 CTA label，继续保持单按钮 contract，不新增辅助面板。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-orchestrator.test.ts test/unit/teacher-script.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/classroom-shell/classroom-orchestrator.ts` 可 grep 到 `teacher_fallback_model`。
    - `podium-view-model.ts` 可 grep 到 `I said it with Cora`，且仍只有一个 `confirmationButtonLabel` 字段。
    - `teacher-script.test.ts` 明确断言 fallback answer 不出现在 `visibleCaption` 中。
  </acceptance_criteria>
  <done>老师最终兜底和孩子最后一次跟说机会已经成为 reducer 的一等路径，而不是散落在 copy 里的隐含行为。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 补齐 fallback-focused shell/e2e 回归门，并保留 Phase 3 baseline</name>
  <files>test/unit/classroom-shell.test.tsx, test/e2e/guided-speaking-flow.spec.ts, src/features/classroom-shell/use-classroom-orchestrator.ts, src/features/classroom-shell/podium-view-model.ts</files>
  <read_first>
    .planning/phases/04-hints-and-judgment/04-VALIDATION.md
    src/features/classroom-shell/use-classroom-orchestrator.ts
    src/features/classroom-shell/podium-view-model.ts
    test/unit/classroom-shell.test.tsx
    test/e2e/guided-speaking-flow.spec.ts
  </read_first>
  <behavior>
    - Test 1: shell integration 必须证明 fallback 前后都只有单 CTA；进入 final follow 时 label 切成 `I said it with Cora`，看不到 score、correct/incorrect 文案或 target text。
    - Test 2: Playwright 文件必须同时保留原有 repeat -> picture baseline 测试，并新增一条显式走 fallback branch 的 test，不能用新路径替换掉旧基线。
    - Test 3: fallback branch 中 Bobby 不能在 picture-talk 出现，repeat/picture 都必须在 final follow 后继续推进到下一题或下一 stage。
  </behavior>
  <action>扩展 `test/unit/classroom-shell.test.tsx` 与 `test/e2e/guided-speaking-flow.spec.ts`。shell 测试要用真实 `userEvent` + fake timers 走出两条路径：1) repeat fallback：先错过第一次机会进入 co-speak，再第二次失败进入 `teacher_fallback_model`，然后看到单个 `I said it with Cora` CTA；2) picture fallback：先 observe hint、再 narrowed re-ask、再进入 fallback model/final follow，期间不能出现 Bobby rescue、score、`apple`/`banana` 这类 target text。Playwright 文件保留现有 baseline test 名称与断言，再新增第二个 test，例如 `guided speaking fallback path keeps one CTA and no answer leakage`，脚本必须用现有 8 秒窗口或更精确 timeout，确保新增 hint/fallback 定时不会把旧 spec 打碎。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-orchestrator.test.ts test/unit/teacher-script.test.ts test/unit/classroom-shell.test.tsx test/unit/classroom-judgment.test.ts test/unit/lesson-schema.test.ts && npm run test:e2e -- test/e2e/guided-speaking-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test/unit/classroom-shell.test.tsx` 可 grep 到 `I said it with Cora`，并继续包含 `/I said it|I answered/` 的单按钮断言。
    - `test/e2e/guided-speaking-flow.spec.ts` 至少有两个 `test(` 定义：一个 baseline，一个 fallback branch。
    - e2e/shell 断言中看不到 `score`, `correct`, `incorrect` 或 target text 直出到 child-facing copy。
  </acceptance_criteria>
  <done>Phase 4 的 teacher fallback close-out 既有 focused 回归门，也没有打断已存在的 repeat -> picture 课堂主路径。</done>
</task>

</tasks>

<verification>
最终验证必须同时证明两件事：fallback 真的存在，以及它没有把课堂变成答题器。只有 reducer 单测通过而 browser regression 缺失，不能算 Phase 4 规划完成。
</verification>

<success_criteria>
- repeat 和 picture 两个 stage 都能从 light hint 走到 fallback model + final follow，再自然切到下一题。
- child-facing UI 继续保持单 CTA、英文优先、无分数/错误标签、无目标词直出。
- Playwright 仍保留 Phase 3 baseline，并新增 fallback-focused smoke 覆盖。
</success_criteria>

<output>
After completion, create `.planning/phases/04-hints-and-judgment/04-03-SUMMARY.md`
</output>
