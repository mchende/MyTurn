---
phase: 04-hints-and-judgment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/classroom-shell/classroom-orchestrator.ts
  - src/features/classroom-shell/use-classroom-orchestrator.ts
  - src/features/classroom-shell/teacher-script.ts
  - src/features/classroom-shell/podium-view-model.ts
  - test/unit/classroom-orchestrator.test.ts
  - test/unit/teacher-script.test.ts
  - test/unit/classroom-shell.test.tsx
autonomous: true
requirements:
  - TEAC-03
  - SPKG-05
must_haves:
  truths:
    - "孩子第一次接不上时，课堂不会立刻过题或掉进最终兜底，而是先进入一层轻提示，再给第二次机会。"
    - "复述轮的第一层轻提示是老师带着一起说的 co-speak；看图轮的第一层轻提示是观察型提示，第二次机会的问题会比第一次更窄。"
    - "课堂主流程仍然只有单 reducer、单 scheduler、单 podium CTA，Bobby 不会插手 picture-talk 的提示链路。"
  artifacts:
    - path: "src/features/classroom-shell/classroom-orchestrator.ts"
      provides: "repeat/picture 的 light-hint 状态图、`hintLevel` / `turnResolution` 字段与第二次机会推进"
      exports:
        - "classroomOrchestratorReducer"
        - "createInitialClassroomState"
        - "CLASSROOM_TIMINGS"
    - path: "src/features/classroom-shell/use-classroom-orchestrator.ts"
      provides: "保持单 timer 调度的 hook contract，并暴露 hint-aware 阶段文案"
      exports:
        - "useClassroomOrchestrator"
    - path: "src/features/classroom-shell/teacher-script.ts"
      provides: "repeat co-speak 与 picture observe/narrowed copy 的 stage-aware 脚本"
      exports:
        - "getTeacherScriptLine"
    - path: "test/unit/classroom-shell.test.tsx"
      provides: "单 CTA、Bobby repeat-only、no-answer-leak 的 shell 回归"
  key_links:
    - from: "src/features/classroom-shell/use-classroom-orchestrator.ts"
      to: "src/features/classroom-shell/classroom-orchestrator.ts"
      via: "单一 `setTimeout` + `phase_timer_completed` / `student_silent_timeout`"
      pattern: "CLASSROOM_TIMINGS|phase_timer_completed|student_silent_timeout"
    - from: "src/features/classroom-shell/teacher-script.ts"
      to: "src/features/classroom-shell/podium-view-model.ts"
      via: "repeat co-speak 与 picture narrowed re-ask 的提示文案联动"
      pattern: "teacher_encourage|student_wait|picture-talk|repeat-after-teacher"
    - from: "test/unit/classroom-orchestrator.test.ts"
      to: "src/features/classroom-shell/classroom-orchestrator.ts"
      via: "first-failure hint -> retry assertions"
      pattern: "hintLevel|turnResolution|attemptIndex"
---

<objective>
把 Phase 4 的第一层课堂护栏先钉死：为复述轮和看图轮加上显式轻提示状态图，并在不新增第二套控制层的前提下保住现有单 CTA 课堂骨架。

Purpose: `04-02` 的 judgment adapter 和 `04-03` 的 fallback close-out 都依赖一个稳定的 hint ladder；如果这里不先把“第一次接不上怎么办”说清楚，后两份计划只会把逻辑继续塞进 if/else。
Output: hint-aware reducer/hook、repeat co-speak 与 picture observe/narrowed 脚本文案、以及 focused reducer/shell 回归测试。
</objective>

<execution_context>
@C:/Users/mchende/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mchende/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hints-and-judgment/04-CONTEXT.md
@.planning/phases/04-hints-and-judgment/04-RESEARCH.md
@.planning/phases/04-hints-and-judgment/04-VALIDATION.md
@.planning/phases/03-guided-speaking-flow/03-02-SUMMARY.md
@.planning/phases/03-guided-speaking-flow/03-03-SUMMARY.md
@src/features/classroom-shell/classroom-orchestrator.ts
@src/features/classroom-shell/use-classroom-orchestrator.ts
@src/features/classroom-shell/teacher-script.ts
@src/features/classroom-shell/podium-view-model.ts
@test/unit/classroom-orchestrator.test.ts
@test/unit/teacher-script.test.ts
@test/unit/classroom-shell.test.tsx

<interfaces>
From src/features/classroom-shell/classroom-orchestrator.ts:
```ts
export type ClassroomOrchestratorPhase =
  | 'teacher_prompt'
  | 'ai_model'
  | 'student_wait'
  | 'teacher_encourage'
  | 'teacher_echo'
  | 'teacher_feedback'
  | 'move_next'
  | 'wrap_up';
```

From src/features/classroom-shell/use-classroom-orchestrator.ts:
```ts
return {
  ...state,
  stageBadge,
  stagePrompt,
  teacherHint,
  teacherMessage,
  confirmStudentParticipation: () => void,
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固定 repeat/picture 的第一次失败 hint 状态图</name>
  <files>src/features/classroom-shell/classroom-orchestrator.ts, src/features/classroom-shell/use-classroom-orchestrator.ts, test/unit/classroom-orchestrator.test.ts</files>
  <read_first>
    .planning/phases/04-hints-and-judgment/04-CONTEXT.md
    .planning/phases/04-hints-and-judgment/04-RESEARCH.md
    .planning/phases/04-hints-and-judgment/04-VALIDATION.md
    src/features/classroom-shell/classroom-orchestrator.ts
    src/features/classroom-shell/use-classroom-orchestrator.ts
    test/unit/classroom-orchestrator.test.ts
  </read_first>
  <behavior>
    - Test 1: per D-01/D-03，`repeat-after-teacher` 第一次 `student_wait` 超时后进入一层 light hint，而不是直接 `teacher_echo -> move_next`；timer 结束后要回到第二次 `student_wait`。
    - Test 2: per D-04/D-05/D-06，`picture-talk` 第一次超时后进入 observe hint，再给一次 narrowed re-ask 的 `student_wait`，且 Bobby 不会重新进入示范位。
    - Test 3: `attemptIndex` 只表示 learner attempt 次数；hint/fallback 层级必须由单独字段表示，不能靠 `attemptIndex === 2` 这类魔法数硬猜。
  </behavior>
  <action>先在 `test/unit/classroom-orchestrator.test.ts` 写失败用例，再改 reducer/hook。将 `ClassroomOrchestratorState` 扩展为显式包含 `hintLevel: 'none' | 'light' | 'fallback'` 与 `turnResolution: 'idle' | 'retry' | 'pass' | 'fallback'`，并保持 `attemptIndex` 只记录 learner attempt 次数。保留现有单 reducer / 单 `CLASSROOM_TIMINGS` scheduler，不新增并行时序。实现具体转移：1) `repeat-after-teacher` 第一次 `student_silent_timeout` 时进入 `teacher_encourage`，设置 `hintLevel: 'light'`、`turnResolution: 'retry'`；`teacher_encourage` timer 完成后回到 `student_wait`，`attemptIndex` 变为 `1`。2) `picture-talk` 第一次 `student_silent_timeout` 也进入 `teacher_encourage`，但 timer 完成后回到 `student_wait` 时必须保留 `hintLevel: 'light'`，供 narrowed re-ask copy 使用。不要在本任务实现最终 fallback；第二次失败的 close-out 留给 `04-03`。不要重写第二套 hook，也不要新增额外按钮事件。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/classroom-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/classroom-shell/classroom-orchestrator.ts` 中可 grep 到 `hintLevel` 和 `turnResolution` 两个状态字段。
    - reducer 测试明确断言 repeat 的 `teacher_encourage -> student_wait` 与 picture 的 `teacher_encourage -> student_wait` 首次重试路径。
    - `use-classroom-orchestrator.ts` 仍只保留一个基于 `CLASSROOM_TIMINGS` 的 timer effect。
  </acceptance_criteria>
  <done>第一次失败后的课堂动作已被 reducer 清晰定义，后续计划不需要再猜“提示层”和“最终兜底层”的边界。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接通 repeat co-speak 与 picture observe/narrowed 文案，同时保住单 CTA 护栏</name>
  <files>src/features/classroom-shell/teacher-script.ts, src/features/classroom-shell/podium-view-model.ts, test/unit/teacher-script.test.ts, test/unit/classroom-shell.test.tsx</files>
  <read_first>
    .planning/phases/04-hints-and-judgment/04-CONTEXT.md
    .planning/phases/04-hints-and-judgment/04-RESEARCH.md
    src/features/classroom-shell/teacher-script.ts
    src/features/classroom-shell/podium-view-model.ts
    test/unit/teacher-script.test.ts
    test/unit/classroom-shell.test.tsx
  </read_first>
  <behavior>
    - Test 1: per D-01，repeat 的 `teacher_encourage` 使用 very light co-speak，`spokenModel` 可含目标词，`visibleCaption` 必须继续 child-safe。
    - Test 2: per D-04/D-05/D-06，picture 的 `teacher_encourage` 只给 observe hint；回到第二次 `student_wait` 后，老师与 podium status 要切成 narrowed re-ask，而不是原题重播或半答案。
    - Test 3: per SPKG-05，classroom shell 在 hint 之后仍只有一个 podium CTA，不出现评分文案、target text 泄露或 Bobby 的 picture-talk rescue。
  </behavior>
  <action>先补 `test/unit/teacher-script.test.ts` 与 `test/unit/classroom-shell.test.tsx`。然后修改 `src/features/classroom-shell/teacher-script.ts` 与 `src/features/classroom-shell/podium-view-model.ts`：1) repeat 的 `teacher_encourage` 具体文案定为 co-speak 语义，例如 `Say it with me. Nice and slow.`，`spokenModel` 继续走 target-bearing channel；2) picture 的 `teacher_encourage` 文案必须是观察型提示，例如围绕 color/animal/object feature 的引导，不允许出现目标词、首音、中文解释或句型模板；3) picture 第二次 `student_wait` 的 prompt/podium status 要切到 narrowed re-ask，例如 `Look again and choose.` / `Answer the smaller question.` 这类更窄问题语气；4) 保持 `getConfirmationButtonLabel` 仍只输出 `I said it` 与 `I answered`，本计划不增加第二个按钮或 fallback label。不要改 `classroom-shell.tsx` 布局，不要把 `spokenModel` 渲染到 child-facing UI。</action>
  <verify>
    <automated>npm run test:unit -- test/unit/teacher-script.test.ts test/unit/classroom-shell.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `teacher-script.ts` 中的 repeat `teacher_encourage` 分支包含 `Say it with me` 或等价 co-speak 语义，且 `visibleCaption` 不含 target text。
    - `teacher-script.ts` / `podium-view-model.ts` 中的 picture 重试链可 grep 到 observe 与 narrowed 两套不同 copy，不是同一句原题重复。
    - `test/unit/classroom-shell.test.tsx` 继续断言只有一个 `/I said it|I answered/` 按钮，且 picture hint 阶段看不到 Bobby rescue 与答案文字。
  </acceptance_criteria>
  <done>轻提示已经在老师话术和 podium copy 层真实出现，同时课堂壳继续保持 child-safe、单 CTA、老师主导的契约。</done>
</task>

</tasks>

<verification>
优先锁住第一次失败的真实课堂动作。只改 copy 不改状态图，或者只改 reducer 不补 shell 回归，都会让 Phase 4 后半段继续返工。
</verification>

<success_criteria>
- repeat 轮第一次困难后会进入 co-speak，再给孩子第二次机会。
- picture-talk 第一次困难后会出现 observe hint，第二次机会的提问比第一次更窄。
- 单 reducer、单 scheduler、单 podium CTA、Bobby repeat-only 和 child-safe UI 合同保持不变。
</success_criteria>

<output>
After completion, create `.planning/phases/04-hints-and-judgment/04-01-SUMMARY.md`
</output>
